# 明鉴 · 项目介绍

> **明鉴（Míngjiàn）** — 复用型税务风险研判系统  
> 一句话：**能对话、可溯源、可出报告的企业税务与经营风险研判平台**；以聚合切片洞察群体风险，以个体深度研判支撑尽调与复核。

---

## 一、项目概述

明鉴是一套面向税务稽查、风控合规、尽调与内审场景的 **B 端风险研判产品**。系统将税务发票、申报、违法记录、行业基准等多源数据统一到 **指标语义层** 之上，通过确定性引擎完成评分、真实性交叉验证、舞弊信号检测、趋势与对标分析，并以 **自然语言对话** 作为交互入口，以 **可下载 PDF 报告** 作为交付出口。

与「聊天查数」类产品不同，明鉴的设计红线是：

- **数字只由算法产生**，大模型只负责理解问法、组织语言，不得编造指标；
- **每个结论带 Claim 与溯源**，支持追问与报告附录核对；
- **公共数据严格匿名**，不暴露真实企业名与税号，个体输出仅用匿名编号。

当前工程实现包含完整前后端、Docker 一键部署、200+ 量级活数据样机、226+ 项自动化测试及 CI/E2E 流水线，严格审计综合评分约 **83/100**（详见 `AUDIT_REPORT.md`）。

---

## 二、产品定位

| 维度 | 定位 |
|------|------|
| **品类** | 税务 / 经营风险 **研判** 系统（非通用 BI、非单纯 ChatBI） |
| **形态** | 复用型数据分析平台：核心资产是 **口径统一的指标库 + 研判引擎 + 报告工厂** |
| **交互** | AI 对话为入口，仪表盘与个体画像为辅助，报告为卖点 |
| **分析层级** | **两级**：① 全部聚合切片（行业 / 地区 / 时间 / 信号）；② 个体深度研判（匿名样本画像 + 深度 PDF） |
| **不做** | 具名企业检索、个体关系图谱、以探索数据为主的「数据浏览器」 |
| **做** | 可解释的风险结论、可追问的研判链路、可行动的组合报告 |

**与 PRINCIPLES 对齐的三句话**

1. **计算定结论、模型定表达、溯源定可信**  
2. **报告是卖点、口径是地基**  
3. **规则优先，LLM 兜底**（意图与语义解析、回复润色、字段映射辅助）

---

## 三、目标用户与典型场景

| 用户 | 场景 |
|------|------|
| 税务机关 / 稽查人员 | 行业与地区风险分布、异常开票与进销错配信号、组合研判报告 |
| 银行 / 尽调团队 | 匿名样本群体对标、真实性交叉验证、个体深度报告导出 |
| 企业内控 / 财税顾问 | 指标口径查询、多维度趋势与预警、会话内追问下钻 |
| 产品演示 / 科研比赛 | mock / live 双模式、可复现 Docker 栈、审计文档完备 |

---

## 四、功能概括

### 4.1 AI 对话研判（核心入口）

- **自然语言问法**：趋势、评分、对比、排名、分布、分段、FAQ、指标口径等（SemanticQuery 语义 IR + 规则降级）。
- **多轮追问**：会话内继承行业 / 省份 / 查询类型（如「分析制造业信用分」→「那江苏呢」）。
- **结论呈现**：文字 + 内联图表（折线 / 柱状 / 饼图 / 雷达 / 热力 / 漏斗等）；气泡展示查询类型、样本量、规则/LLM 来源标签。
- **追问建议**：按查询类型生成可点击续问（非静态死模板）。

### 4.2 五维风险引擎（数字唯一来源）

| 能力 | 说明 |
|------|------|
| **综合评分** | 税务健康、经营真实性、行业地位、法律合规、财务健康五维加权 |
| **趋势** | 行业 / 地区营收同比与走向 |
| **真实性** | 增值税 / 发票 / 财报多口径交叉验证，Benford 等辅助 |
| **舞弊 / 异常信号** | 预计算 `engine_features`、进销错配、序列缺口、集中度等 |
| **对标 / 预警** | 同业基准、风险信号分布与行业×信号热力 |

### 4.3 报告中心（最大卖点）

- **切片组合报告**：按会话覆盖维度自动组章（趋势 / 评分 / 舞弊 / 信号 / 综合研判等）。
- **场景模板**：行业趋势、欺诈舞弊、尽调组合、基本面等；支持 HTML 预览与 PDF 下载。
- **排版双轨**：WeasyPrint（主）+ FPDF 降级；图表 matplotlib 嵌入，含 KPI 表、归因图、五维雷达章。
- **抗幻觉**：报告正文数字锚定 Claim；LLM 仅可选章节解读/摘要，且受数字校验。
- **邮件发送**：对话内 `email_report` 意图触发 SMTP 投递（可配置）。
- **报告历史**：中文场景标题、列表与下载。

### 4.4 工作台与预警

- 活数据 KPI：样本量、均分、高风险家数、等级分布。
- 风险预警列表与对话侧栏统一数据源（live 失败显式报错，不静默 mock）。

### 4.5 个体画像（二级分析）

- 匿名企业 `/enterprise/:id`：画像 claims、同业定位、风险成因摘要。
- **个体深度报告 PDF**：对话或画像页触发生成。

### 4.6 数据接入与指标语义层

- Excel 上传、字段分层映射（精确 / 模糊 / LLM 语义）、写入指标层或会话临时表。
- `metric_registry`：指标定义、公式、粒度、维度；方法论问答与 LLM 字典同源。
- ETL 管线：MySQL → PostgreSQL `core_metrics`，`engine_features` 预计算舞弊与 Benford 快照。

### 4.7 安全与运维

- 可选 JWT 全端点鉴权、CORS 白名单、生产配置强校验。
- Redis 限流与 LLM 日配额；配额耗尽软降级为规则/模板。
- Docker Compose 全栈；health 自检 DB / Redis / 编码 / 脱敏 / engine 计数。

---

## 五、技术架构（概要）

```
用户（Web）
    ↓
React 对话 / 仪表盘 / 报告 / 接入 / 个体画像
    ↓
FastAPI（chat / risk / report / ingest / metrics）
    ↓
┌─────────────────┬──────────────────┬─────────────────┐
│ 语义层          │ 研判引擎         │ 报告工厂        │
│ Intent +        │ 评分/趋势/       │ slice_report +  │
│ SemanticQuery   │ 真实性/舞弊/     │ WeasyPrint/FPDF │
│ + FAQ KB        │ 信号/对标        │ + 邮件          │
└─────────────────┴──────────────────┴─────────────────┘
    ↓
PostgreSQL（指标、会话、结论、报告元数据）
MySQL（税务源表，应用层脱敏）
Redis（缓存、限流、配额）
LiteLLM + Instructor（可选：解析与润色，不算数）
```

**数据模式**：`mock` / `mock_with_llm` / `live` 三态；前端按 health 自动切换，live 失败不伪造成功响应。

---

## 六、商业价值

| 价值点 | 说明 |
|--------|------|
| **降本增效** | 稽查 / 尽调中重复性的「拉数—算指标—写段落」收敛为对话 + 一键报告 |
| **可审计** | 结论可回溯到表字段与 query_id，满足监管与内控留痕要求 |
| **可复用** | 同一套引擎与口径服务多场景报告（趋势、舞弊、尽调），非一次性脚本 |
| **隐私可交付** | 匿名聚合 + 个体脱敏输出，适合演示、联调与涉敏环境试点 |
| **可扩展** | 指标语义层 + ingest 映射，新客户字段可接入而不推倒重写 |
| **演示可信** | 与「纯 LLM 胡编」产品区分：数字来自引擎，模型只讲故事 |

---

## 七、创新点

### 7.1 方法论创新

1. **「计算定结论、模型定表达」双轨架构**  
   业界常见 ChatBI 让模型直接生成数字；明鉴将 **SemanticQuery IR + 确定性引擎** 与 **Claim 溯源链** 分离，LLM 仅在授权结论上润色，从架构上压低幻觉风险。

2. **指标语义层（Metric Registry）作为地基**  
   借鉴 MetricFlow / LookML 思路，将「指标定义、公式、粒度、维度」产品化，支撑 NL 问法、方法论 FAQ、报告章节与 ETL 口径一致。

3. **两级分析产品形态**  
   聚合切片回答「群体风险长什么样」；个体深度报告回答「这家匿名样本为何异常」——同一数据库，两种交付深度。

4. **会话级组合报告**  
   报告不是静态模板填空，而是按对话 **已覆盖维度** 动态组章 + 综合研判章，覆盖度可追问、可提示「生成报告」。

### 7.2 工程创新

5. **SemanticQuery 八类查询 + 规则/LLM 双路径**  
   comparison / ranking / segmentation / FAQ / methodology 等统一 IR；无 LLM 时 FAQ 前置、双省对比规则、趋势不走 hint 死胡同。

6. **严格审计与信任线**  
   live API 失败 503；synthesis 不入库防自污染；舞弊预计算缺失显式报错；226+ pytest + Playwright E2E + 多轮审计文档闭环。

7. **可视化与报告一体化**  
   对话内联 ECharts（按需注册）；报告侧 matplotlib PNG + 雷达/热力/漏斗；前后端图表类型对齐。

8. **隐私红线产品化**  
   个体企业图谱下线；公共源 MD5 脱敏；UI 仅匿名标签（地区·行业·规模）。

---

## 八、数据与合规说明

- 演示样机约 **193 家** 匿名主体（活库），指标与 `engine_features` 全量预计算。
- 源库企业名 / 税号等 **不可逆脱敏**；应用 API 不返回 PII 列。
- 法律维度当前以 **税务违法与稽查事件** 为主；失信 / 诉讼等需额外数据源扩展。
- 生产部署需开启 `AUTH_REQUIRED`、强 JWT、CORS 白名单（见 `README.md`）。

---

## 九、成熟度与文档索引

| 项 | 状态 |
|----|------|
| 核心对话 + 报告链路 | ✅ 可演示、可 E2E |
| 严格审计 | ✅ `AUDIT_REPORT.md`（第十九轮 ~83/100） |
| 产品最高准则 | `PRINCIPLES.md` |
| 结构说明 | `jiegou.md` |
| 对话模拟测试 | `scripts/SYSTEM_DIALOGUE_SIMULATION.md`、`simulate_system_dialogue.ps1` |
| 开源子模块 | `opensource/`（算法与 LLM 工具参考） |

---

## 十、快速体验

```bash
docker compose up -d --build
```

| 入口 | 地址 |
|------|------|
| 前端 | http://localhost:5173 |
| 对话 | http://localhost:5173/chat |
| 数据接入 | http://localhost:5173/ingest |
| API 文档 | http://localhost:8000/docs |
| 健康检查 | http://localhost:8000/api/v1/health |

配置 `LLM_API_KEY` 后重启 backend，可体验语义解析 + LLM 润色路径；未配置时仍可使用规则研判与模板回复。

---

## 十一、总结

明鉴不是「又一个税务大屏」，而是 **口径统一、引擎算数、对话入口、报告出口** 一体化的风险研判产品。其商业价值在于：在敏感数据环境下，仍能提供 **可追问、可溯源、可出报告** 的专业研判体验；其创新在于：用 **语义 IR + Claim 红线** 把大模型关在「表达层」，把信任建立在 **确定性计算与审计闭环** 之上。

---

*文档版本：2026-08-27 · 与工程现状及 AUDIT_REPORT 第十九轮对齐*
