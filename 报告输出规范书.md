# 明鉴·财税票风控报告系统 输出规范书

> 文档版本：V1.1（V1.0 基础上补全「机制改造红线」+ 管道契约 + 结构化输出 + 全中文净化 + 后置校验强化）
> 文档目的：统一对话交互、规则计算、报告渲染、LLM 文本生成全链路标准；解决历史问题：数据前后矛盾、模板硬编码、图表与正文割裂、校验后置、LLM 幻觉、两类报告字段混用。
> 适用角色：产品、后端开发、测试、大模型工程、**接手的 AI 编码助手（Cursor 等）**。

---

## 核心原则

1. **规则计算交给代码，文本润色交给 LLM**；业务逻辑绝不依赖提示词实现。
2. **机制改造优先于打补丁**：所有修复必须让「某一类错误在结构上不可能再发生」，而不是堵住当前这一个特例。详见 **第〇节（最高优先级）**。
3. **数字只来自 L0 数据层，评级只来自 L1 规则引擎，LLM 不碰数值**。
4. **0 = 弃权，空数据不出现，不编造，弃权优先于编造**。
5. **唯一事实源**：指标、阈值、口径、标准措辞一处定义，其余全引用，禁止散落硬编码。

---

## 第〇节 机制改造红线（Cursor 必读，最高优先级）

> 这一节是给接手改造的 AI 编码助手的行为约束。**我们做的是机制改造，不是打补丁。**

### 0.1 一条判断标准

**问一句：这个改动是让「某一类错误在结构上不可能再发生」，还是只堵住了「当前这一个特例」？**

- 前者 → 机制改造，继续。
- 后者 → 打补丁，**立即停止**。

### 0.2 打补丁的 6 个可检测信号（命中任一即停止）

1. 在**渲染层 / 模板**里新增硬编码文案，用 if-else 绕过 claim 的条件判定。
2. **新增一个散落的阈值/数字**（如 `25%`），而不是收敛到指标字典。
3. 用「**提示词里加一句指令**」去堵一个 LLM 幻觉，而不是在代码层加结构化约束或校验。
4. 只修了 **A 章节 / A 信号的特例**，同类模式在 B、C 处仍然原样存在（这就是「改一版又冒一堆问题」的直接来源）。
5. 在**用户可见输出已经出错之后**，才在后置加 if-else 拦截（校验后置、亡羊补牢）。
6. 为了「改对」某个测试，去**改测试的期望值**，而不是改生产代码的根因。

### 0.3 机制改造的 4 个特征

1. **唯一事实源**：阈值 / 指标 / 措辞只有一处定义，其余全引用（指标字典）。
2. **管道契约**：渲染层唯一输入是 Payload（claim 列表），文本只能是 keyed-by-claim 的条件片段；没有 claim 就没有文本。
3. **校验作为管道一环**：渲染前钩子 + 后置校验 + CI 门禁，而非散落的特例 if-else。
4. **一处改动覆盖一类问题**（例：`_dimension_analysis` 一处修复覆盖全部 7 个维度）。

### 0.4 停止 → 检测 → 破局 程序

当发现自己正在做符合 0.2 任一信号的事，执行：

1. **立即停止编码**。
2. **检测**：定位这个 bug 的根因属于哪一层（L0 数据 / L1 指标字典 / 规则引擎 / Payload / 渲染 / LLM / 校验）。它暴露了哪个「唯一事实源缺失」，或哪条「管道边界可被绕过」？
3. **破局**：先给出机制化方案（收敛事实源 / 收紧管道 / 加校验门禁），再动手。若判定当前架构下无法机制化，**明确写出「无法机制化的原因」**，回到本规范书第三节（指标字典）与第三节（口径）寻找更底层的破局点，**而不是继续堆补丁**。

---

## 术语定义

| 术语 | 说明 |
|---|---|
| 单主体报告 | 针对单个企业生成财务风控报告 |
| 聚合总览报告 | 行业/地区筛选后的多家企业集合群体报告，禁止复用单主体评级概念 |
| 规则引擎 | 代码层，负责指标计算、风险判定、预览预校验，输出结构化 JSON |
| 指标字典 | 中心化元数据表，统一维护全部指标阈值、释义、行业基准（唯一事实源） |
| Payload | 规则引擎输出给 LLM 的标准化结构化输入 JSON，LLM 唯一数据源 |
| 文本契约（Document Plan） | 渲染层唯一输入：由带条件的 claim/message 列表 + 顺序 + 结构组成；文本片段 keyed by claim |
| 约束解码 | 生成时按 schema 过滤 next-token 的结构化输出机制，使 LLM 无法产出 schema 之外的字段/数字 |
| 后置校验 | LLM 输出文本完成之后，代码自动校验，对抗幻觉的兜底机制 |

---

## 一、交互层（规则引擎预览校验规范）【P0 强制】

目标：问题前置，不要等到用户点击生成后才报错。

用户完成筛选条件（行业、地区、目标企业、选择输出章节），立刻执行元数据预校验。

**校验内容**
1. 校验当前筛选条件 + 所选章节是否存在可用数据；
2. 校验图表展示维度集合必须属于用户选中输出章节（即「雷达 ⊆ 章节」在预览阶段即校验，而非渲染后才裁）；
3. 聚合报告校验样本覆盖率，覆盖率过低给出黄色预警提示。

**校验结果处理**
- 无可用数据：隐藏「确认生成」按钮，直接提示无法生成，附带快捷调整选项；
- ❌ 禁止文案：生成时将被跳过；
- ✅ 标准文案：`当前筛选条件组合无对应可用数据，无法生成报告，你可以选择调整章节/放宽筛选条件。`

不允许用户提交生成请求之后才抛出无数据报错，避免用户输入沉没成本。

> 【V1.1 补全】本条（预览预校验）与第五节（图表⊆章节）、第八节（后置校验）构成**三层校验管道**：预览预校验（前置）→ 渲染前校验钩子（claim 级，`validate_report_chapters`）→ 后置校验（LLM 输出后）。三者缺一不可，缺一即回到「校验后置」老路。

---

## 二、报告分类与章节结构规范

两类报告结构、字段完全隔离，禁止混用字段。

### 2.1 单主体：企业财务分析报告

章节顺序固定，不可随意删减调整：

1. **封面页**：报告名称、企业全称、报告年度、报告生成日期
2. **企业基本信息**：企业名称、统一社会信用代码、法定代表人、注册资本、成立日期、企业类型、注册地址、所属行业、经营范围
3. **总体风险评估**
   - 整体财务健康状况：风险等级由规则引擎输出（良好 / 中等风险 / 较高风险 / 高风险），LLM 不得修改、不得自行判断；
   - 理由：引用核心指标客观陈述；
   - 主要风险点总结：仅归纳异常指标、同比恶化项；
   - 核心优势：归纳指标达标、优于行业基准项；
   - 整体建议：风险点和建议一一对应，输出业务可落地建议，禁止空泛套话。
4. **分维度风险分析**（盈利风险 / 偿债风险 / 运营风险 / 现金流风险）
   每个维度固定子结构：
   - 风险等级：高 / 中等 / 低（来自规则引擎，LLM 不可改动）
   - 指标表格：指标名称、本年值、上年值、变化方向、参考/行业标准
   - 智能分析：客观描述指标表现，对比自身历史、行业基准
   - 指标变化趋势及其含义：存在 2 期及以上数据才可写趋势；仅单年度输出固定文字：`本报告仅提供单年度数据，无可对比上年历史数据，无法开展同比趋势分析`
   - 潜在风险点识别：客观事实直接描述；推测推演必须带限定词「可能、若、推测」；全部指标达标写：`本维度指标未见显著风险`，禁止强行制造风险。
   - 简要建议：建议匹配本维度风险；指标全部达标，不输出风险类建议。
5. **主要财务数据**：利润表、资产负债表、现金流量表；字段缺失单元格填写【无数据】，禁止编造数值。
6. **免责声明**（固定模板，见 9.2）

### 2.2 聚合总览报告（多企业群体报告）

❗ 严禁使用单主体字段：风险等级、评级展望；替换为群体风险判断，必须标注样本口径。

章节顺序：
1. **封面页**：报告名称、筛选条件、总样本数、有效样本数、报告生成日期
2. **执行摘要**：样本规模、群体整体判断、群体主要风险、群体优势、业务处置建议
3. **六维综合画像**：图表下方标注统计子集
4. **风险信号总览**：风险信号分布、多重风险叠加主体统计
5. **分模块摘要**：财务健康摘要、税务合规摘要、发票舞弊摘要、经营真实性摘要

每一个子章节头部必须标注：`统计子集：XX家`，各模块样本不一致必须显式提示。

> 【V1.1 补全】单主体与聚合的「字段隔离」必须**在代码层用两套独立 Schema/Payload 强制**，而非靠提示词约定或开发自觉。混用字段（聚合报告里出现「风险等级/评级展望」）应在后置校验层被机器拦下，而不是靠人眼核对。

---

## 三、指标与口径规范

全部指标阈值、达标标准、行业基准统一维护在中心化指标字典；单主体报告、聚合报告统一引用字典，禁止模板、代码各处硬编码数字。

指标字典字段：指标 ID、指标中文名、适用报告类型、预警阈值、达标阈值、行业基准来源、标准输出措辞。

对外报告禁止透传代码字段名、内部开发标记，全部映射为业务中文名称。

同一业务指标只能存在一套阈值标准，不允许单企业、聚合报告两套口径。

> 【V1.1 补全】「唯一事实源」的落地 = dbt MetricFlow 的「define once, use everywhere」思想（[dbt MetricFlow](https://github.com/dbt-labs/metricflow)）：指标 + 维度 + 阈值 + 标签**一处声明式定义**，其余全引用。本项目落点：`metric_registry.py`（唯一指标字典）+ `financial_benchmarks.py`（财务比率，含 warn_dir/warn_threshold/good_threshold/label）。
>
> **加一条自动化防回归**：CI 里加「硬编码阈值检测」测试——grep 渲染层/模板/提示词里出现的百分比、阈值数字，凡不在指标字典引用即失败（参照现有 `test_signal_thresholds.py` 锁定 `REVENUE_DEVIATION_WARN=0.30` 的做法）。这是「阈值只来自字典」从 code-review 规则升级为**机器门禁**的关键一步。

---

## 四、文本措辞强制约束【P0】

1. **时序词汇约束**：「持续、逐年、不断」必须 ≥2 期历史数据才允许使用；仅单年度数据严禁使用。
2. **事实与推测隔离**：有数据支撑直接陈述事实；推演猜测必须增加限定词：可能、或将、若；禁止把推测描述为既定事实。
3. **条件文案规则**：指标全部达标的维度，不输出风险类负面描述与风险建议。
4. **自洽约束**：摘要、总体段落、正文、表格中同一指标数值必须完全一致，禁止多处出现矛盾数据。
5. **行文风格**：专业风控客观文风，拒绝华丽修辞；风险点、优势、建议三者相互呼应。

> 【V1.1 补全】**全中文净化（P0）**：对外报告正文/封面/图表/图例禁止出现英文/技术符号。锁定 `FORBIDDEN_MARKERS`，用测试断言输出不含：`Benford`、`χ²`、`MAD`、`n=`、`None`、`ROE`、`ROA`、`vs`、`N=`、`IT软件`、`scbm_mismatch`、`red_invoice`、`concentration`、`sequence_gap`。统一映射为业务中文（本福特定律、卡方值、平均绝对偏差、样本量、净资产收益率、对比、进销错配…）。此清单在 `report_templates.py` 的 `ZH_TERMS` 集中维护。
>
> 【V1.1 补全】**结论方向约束**（对应「达标却输出风险话术」根因）：一章 claim 全部达标（无风险结论）时，该章解读段出现「承压/越线/预警/需核查」等风险方向词即判矛盾。由 `hallucination_guard.claims_have_risk_verdict` / `sentence_has_risk_direction` + `_sanitize_narration` 强制执行，不是提示词软约束。

---

## 五、图表渲染规范

强约束：**图表展示维度集合 ⊆ 用户选中输出章节集合**

- 方案 A：裁剪图表，只保留用户选中的维度；
- 方案 B：自动把图表新增维度加入输出章节；
- ❌ 严禁：图表展示风险维度，但正文没有对应解析章节。

所有图表必须标注样本 / 统计子集。

聚合报告样本量 <10 时，图表增加提示：`样本数量较少，统计结果仅供参考`。

> 【V1.1 补全】「雷达 ⊆ 章节」首选**方案 A 裁剪，且在渲染前按章节覆盖维度裁剪**（`radar_dimensions_for_chapters` + `attribution_radar_chart(dims=…)`），使它**在构造上就不可能出错**，而不是渲染后再查一遍。只有裁剪语义无法满足时才用方案 B。

---

## 六、边界场景统一处理规则

| 业务场景 | 标准处理方式 |
|---|---|
| 无上年同比数据 | 趋势段落固定文案：`本报告仅提供单年度数据，无可对比的上年历史数据，无法开展同比趋势分析` |
| 部分指标缺失为空 | 表格保留该行，单元格填写【无数据】；不编造数值，不解读缺失指标 |
| 章节无可用数据 | 交互预览阶段拦截生成；若特殊情况渲染报告：保留章节标题，正文标注【暂无可用数据】，不能直接消失 |
| 聚合报告样本覆盖率低 | 报告首页增加提示：`本报告部分模块有效样本覆盖率较低，结论仅供参考` |
| Benford 样本量不足 | 输出纯中文说明：`首位数字分布检验（{scope}）：样本量不足（{N} 家），本次未检验`；禁止拼出 `χ²=None, MAD=None` 这类非业务结论 |

> 【V1.1 补全】边界场景的「标准文案」必须**集中一处维护**（指标字典/措辞表），渲染层引用，禁止同一句固定文案在多处写死（写死即打补丁的信号 2）。

---

## 七、LLM（Claude）生成约束规范

1. **LLM 职责边界**：仅完成结构化 JSON 数据 → 业务文本润色、拼接；不做计算，不做风险判定，不修改任何输入数值。
2. **LLM 唯一输入来源**：规则引擎输出标准化 Payload JSON，禁止直接访问数据库。
3. **数字透传（passthrough）**：金额、日期、指标值、状态等事实字段由**确定性代码直接填入**，LLM 只生成 `commentary/解读` 文本；LLM 不负责转写数字（防止静默四舍五入/篡改真实数据）。参考生产实践「structural separation / passthrough」原则（[AgentPatterns — Data Fidelity Guardrails](https://agentpatterns.ai/verification/data-fidelity-guardrails/)）。

**LLM 禁止行为清单**
- ❌ 禁止计算财务指标、修改风险等级
- ❌ 禁止编造指标数值、行业阈值
- ❌ 禁止修改输入 JSON 内任何数字
- ❌ 禁止输出代码字段、内部调试标记
- ❌ 禁止新增输入不存在的风险点

**输出要求**：只输出纯报告文本；禁止输出图片、HTML、图片标签。

**链路顺序**：规则引擎计算 → 组装 Payload JSON → Claude 生成文本 → 后置校验层校验。

> 【V1.1 补全】**结构化输出（约束解码）落地**：把「禁止修改数字/禁止编造」从提示词软约束升级为**机器硬约束**——「**约束输出空间，而不是用散文去描述它**」（constrain the output space, don't describe it in prose）。分两档，按模型能力选择：
>
> - **应用层结构化输出（当前已落地，A.4）**：LLM 只输出**句计划 JSON**（`NarrationPlan`/`SummaryPlan`，每句引用 claim id / 数字），代码再 `materialize_plan_sentences` 确定性拼装 + 本地 `sanitize` 剥句。适用于 **DeepSeek 托管 API**（无 token 级 JSON Schema/grammar）。
> - **token 级约束解码（需自托管模型，暂不引入）**：XGrammar / Outlines / Guidance / LMQL 在生成期按 schema 过滤 next-token（[Awesome-LLM-Constrained-Decoding](https://github.com/Saibo-creator/Awesome-LLM-Constrained-Decoding)）。**仅当模型支持 token 级 grammar（自托管）时才引入**；托管 API 用上一档即可，勿硬套。

---

## 八、后置自动化校验规范（代码层，对抗 LLM 幻觉，P0 强制）

LLM 输出文本完成后机器自动校验，任意一项校验失败，丢弃本次结果，触发重生成，不对外输出报告。

**校验清单**
1. **数值一致性校验**：提取报告全部数字，和上游 Payload JSON 比对，不允许数值偏差；
2. **脏字段校验**：检测报告是否包含内部代码字段；
3. **语义规则校验**：单年度数据是否非法使用「持续」等时序词汇；
4. **渲染参数校验**：图表维度集合与输出章节匹配校验。

> 【V1.1 补全】按生产 LLM guardrails 的 **defense-in-depth** 分层（[When the Model Is Confident and Wrong](https://sdtimes.com/ai/when-the-model-is-confident-and-wrong-a-practitioner-guide-to-llm-output-reliability/)、[NeMo/Guardrails AI](https://github.com/yonatangross/orchestkit/blob/ba4b3a820fb55e96ee78fb0e0efff928ff83a18a/plugins/ork/skills/security-patterns/rules/llm-guardrails.md)），`run_guardrails` 依次校验：
>
> 1. **Schema** — 输出结构匹配预期（Pydantic v2）；
> 2. **无幻觉 ID/字段** — 正则检测内部代码字段、UUID、`slice_`/`ent_` 前缀泄漏；
> 3. **Grounded（事实锚定）** — 每个 claim 数字都能在 Payload 中找到锚点（term-overlap / NLI entailment / 数字锚点回读）；
> 4. **数值透传一致** — 数字经 `_normalize_num` 归一化后在 Payload 中 substring 命中；过滤 0–10 等系统名误报；
> 5. **安全 / 大小** — 无 PII、长度上限。
>
> 校验失败一律**带错误信息重试**，而非直接报错返回；把「不可见的幻觉」转成「可见、可捕获的失败分支」。注意：**schema 合规 ≠ 数值正确**（`"balance": 14500` 类型对但可能错），所以 schema 校验必须与数值锚定叠加，不能只验结构。

---

## 九、排版与免责模板

### 9.1 PDF 排版要求

- 防孤行控制：章节标题不单独留在上一页，正文不被挤压到下一页，禁止页面大块空白。
- 统一项目符号，禁止乱码、重复圆点符号。

### 9.2 单主体报告免责声明（固定）

```
本报告由明鉴·财税票风控系统自动生成，分析基于企业财务及涉税数据计算得出。报告仅供内部风控参考，不构成审计、投资、授信、法律或信用评级意见。使用者结合业务实际审慎使用，系统不对分析结果的完整性、准确性作出保证。
```

### 9.3 聚合总览报告免责声明（固定）

```
本聚合报告基于匿名样本统计生成，仅供内部风控参考，不针对任何单一主体；不构成审计、投资、授信、法律或信用评级意见。报告各模块统计子集存在差异，请参考章节标注说明。使用者结合业务实际审慎使用。
```

> 【V1.1 补全】免责声明/固定模板文案属于「条件片段」，同样纳入文本契约，一处维护、keyed-by-type（单主体 vs 聚合），不得在渲染层多处分叉写死。

---

## 十、测试验收标准（必测场景）

- 用例 1：2 年完整财务数据，部分指标预警：输出完整报告，趋势分析正常，风险点与建议匹配。
- 用例 2：仅单年度数据，无上年对比：趋势段落提示无同比，不出现「持续」时序词汇。
- 用例 3：部分指标字段缺失：表格标注【无数据】，不编造数据。
- 用例 4：全部指标达标：不输出虚假风险话术，风险识别描述未见显著风险。
- 用例 5：筛选条件下所选章节无可用数据：预览阶段拦截，隐藏确认生成按钮，提供调整选项。
- 用例 6：用户仅选择财务章节，原始存在经营真实性维度：裁剪雷达图多余维度，或自动补充对应章节。

> 【V1.1 补全】追加**机制红线验收用例**（面向 Cursor 改造）：
> - 用例 7（阈值唯一源）：新增一个阈值，必须收敛进指标字典 + 单测锁定；grep 报告输出与渲染层，不得出现字典之外的硬编码百分比。
> - 用例 8（无硬编码文本）：渲染层任意一段文本，要么是 keyed-by-claim 的条件片段，要么来自措辞表；新增「写死文案」即失败。
> - 用例 9（数值透传）：把 Payload 中一个数字改小，报告对应位置必须同步变，证明该数字是透传而非 LLM 生成。
> - 用例 10（全中文）：`FORBIDDEN_MARKERS` 全量断言输出不含英文/技术符号。

---

## 十一、变更管理规则

1. 业务口径、输出规则发生变更，**优先更新本规范书与指标字典，再修改代码与 LLM 提示词**。
2. 不允许只修改提示词绕过业务规范。
3. 产品、开发、测试统一以本规范书作为验收依据。
4. 【V1.1 补全】**每条规则变更必须带一个锁定该规则的测试**（边做边固化）；变更后跑 `AUTH_REQUIRED=false` 全量 pytest + 前端 build，禁止只改提示词不补测试。

---

## 十二、禁止总览检查清单（开发自测）

- 禁止风险等级、阈值硬编码写死在模板、提示词内
- 禁止聚合报告使用单主体「风险等级、评级展望」字段
- 禁止图表展示风险维度，正文无对应解析
- 禁止指标达标依然输出风险建议
- 禁止单年度数据使用「持续、逐年」
- 禁止推测内容直接作为既定事实输出
- 禁止生成完成后才告知用户无可用数据
- 禁止 LLM 承担指标计算、风险判定业务逻辑
- 禁止报告内出现代码、内部字段名称
- 禁止摘要、表格、正文数字互相矛盾
- 【V1.1 补全】禁止在渲染层/模板新增硬编码文案绕过 claim 条件（打补丁信号 1）
- 【V1.1 补全】禁止新增散落阈值数字而非收敛到指标字典（打补丁信号 2）
- 【V1.1 补全】禁止用「提示词加一句指令」去堵 LLM 幻觉，而不加代码层约束/校验（打补丁信号 3）
- 【V1.1 补全】禁止数字由 LLM 生成而非代码透传（数字只能 passthrough）
- 【V1.1 补全】禁止报告正文/图表出现英文/技术符号（`FORBIDDEN_MARKERS`）

---

## 附：机制层方向（供 Cursor 破局参考）

本规范书所有约束的底层架构，是把报告生成重构为**强制三段式管道**（源自 Reiter & Dale NLG 管道 + dbt Semantic Layer + 约束解码）：

```
L1 指标语义层（单一事实源：指标 + 阈值 + 标签）
   → 文档规划器（规则引擎，确定性产出 claim/message 列表 + 顺序 + 结构）
   → 表达实现（条件片段模板；LLM 仅润色 commentary，受约束解码）
   → 校验器（渲染前钩子 + 后置校验 + CI 门禁）
   → L4 版式
```

机制上「不可能再冒问题」的关键只有一句：**渲染层唯一输入是 Document Plan（claim 列表），文本只能是 keyed-by-claim 的条件片段；没有 claim 就没有文本**。这与 NLG 的 message/document-plan 正统实现、以及专利 6651219「decisional statements → logic path → 文本片段」是同一件事。

参考：
- Reiter & Dale，《Building Natural Language Generation Systems》（[Cambridge](https://www.cambridge.org/pn/universitypress/subjects/languages-linguistics/computational-linguistics/building-natural-language-generation-systems?format=HB&isbn=9780521620369)）
- Puduppully & Lapata，Data-to-text Generation with Macro Planning（[arXiv:2102.02723](http://arxiv.org/pdf/2102.02723v1)）——「先规划再生成」
- Osuji et al.，Are Multi-Agents the new Pipeline Architecture for Data-to-Text Systems?（[INLG 2025](https://aclanthology.org/2025.inlg-main.33/)）
- dbt MetricFlow（[GitHub](https://github.com/dbt-labs/metricflow) / [Semantic Layer](https://docs.getdbt.com/docs/use-dbt-semantic-layer/dbt-sl)）
- Constrained Decoding（[Awesome-LLM-Constrained-Decoding](https://github.com/Saibo-creator/Awesome-LLM-Constrained-Decoding)）
- LLM Guardrails / Data Fidelity（[AgentPatterns](https://agentpatterns.ai/verification/data-fidelity-guardrails/)、[SD Times](https://sdtimes.com/ai/when-the-model-is-confident-and-wrong-a-practitioner-guide-to-llm-output-reliability/)）
