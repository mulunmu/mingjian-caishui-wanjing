# 税务风险研判系统 · 结构文档

> 版本：v1.0 ｜ 日期：2026-08-25
> 定位：功能展示型税务风险研判系统——向用户展示「评分 / 真实性 / 反欺诈 / 基准 / 趋势」5 个能力，AI 问答为入口，组合报告为出口。

---

## 一、系统定位

**一句话**：一个「能对话的税务风险研判引擎」，200 家匿名企业是它的样机数据，AI 问答是入口，研判结论和组合报告是产出。

**核心原则（不可违背）**：

1. **不查企业、不查名字、不探索匿名个体** —— 匿名企业没有探索欲望，画像/搜索/网络全部砍掉。
2. **功能展示优先，数据探索退后** —— 用户看的是「系统能做什么」，不是「数据有多少」。
3. **研判可溯源、可追问、有证据链** —— 结论不是一句话，是「结论 + 证据链 + 溯源 + 追问」。
4. **报告 = 维度 × 功能的组合产物** —— 不是单一固定报告，按行业/地区/时间/信号切片组合。

---

## 二、硬约束（数据现实）

| 约束 | 事实 |
|---|---|
| 企业名字 | 永久不可得（MD5 哈希，无明文、无外部补充） |
| 数据范围 | 仅一套数据：200 家小微，180 万条，3.5 年 |
| 主体类型 | 100% 非上市（有限责任公司/个体） |
| 内部连通 | 200 家之间 0 交易，无产业链链条（网络视图砍掉） |
| 脱敏字段 | 企业名、税号、法人、地址、**违法类型** 均已脱敏 |

**已验证的数据覆盖率（落地依据）**：

- 信用等级 100%、增值税 99.5%、发票 96%、财报 95%、缴税 91%
- 现金流 61%、社保 50%（部分可算，需降级）
- 三口径交叉验证：183/193 家（94.8%）可算
- 行业 107 个（过度分散，需聚合大类）
- 银税互动 488 条贷款、75% 企业有贷款

---

## 三、总体架构

```
数据层        引擎层              交互层          输出层
─────────    ──────────────────  ──────────────  ──────────
数据.sql  →   ① 五维评分引擎    →   研判中心       →  组合报告
  │           ② 经营真实性(三口径)    (唯一入口)        (维度×功能)
  │           ③ 反欺诈(进销错配)      ├─ AI研判对话      证据链完整
  │           ④ 行业基准库          ├─ 卡片引导        溯源
  │           ⑤ 趋势研判            └─ 动态追问
  ▼
PostgreSQL   core_metrics / industry_benchmark / conclusion_store
```

---

## 四、数据层（ETL + 数据模型）

**输入**：`数据.sql`（1.95GB，`REPLACE INTO` 格式，13 张核心表）

**目标表**：

| 表 | 内容 |
|---|---|
| `core_metrics` | 一企业一行：`enterprise_id`=taxpayer_id(哈希)、`display_label`(匿名标签)、五维指标字段 |
| `industry_benchmark` | 分行业大类基准值 |
| `conclusion_store` | 研判结论缓存（结论+证据链+溯源） |
| 原始明细表 | 保留（`invoice_details` 等，供溯源下钻） |

**关键处理**：

1. `enterprise_id` = `taxpayer_id`（哈希主键，全表对齐）
2. `display_label` = 地区 + 行业大类 + 规模（**不含股东姓名**，避免泄露自然人身份）
3. 行业 107 → 6 大类：批发零售 / 制造 / 建筑 / IT软件 / 服务 / 其他
4. 五维指标从税务表直接算，**零名字依赖**

---

## 五、引擎层（5 个功能）

| 功能 | 计算逻辑 | 数据来源 | 结论类型 |
|---|---|---|---|
| ① 五维评分 | 税务健康/真实性/行业/法律/财务加权 | credit_level + payment + arrears + illega + 发票 + 财报 | 风险分布、风险等级、预警信号 |
| ② 经营真实性 | 增值税↔企税↔财报↔发票↔社保 五源偏差 | value_added + corporate_income + finance + invoice + social | 偏差率、疑似造假清单 |
| ③ 反欺诈 | 进销商品错配(scbm) + 红字发票 + 集中度 | invoice + details(商品明文+scbm) | 虚开嫌疑、错配样例 |
| ④ 行业基准 | 分大类 税负率/逾期率/信用分布/发票密度 均值·分位 | 全表按大类聚合 | 基准值表、偏离度 |
| ⑤ 趋势研判 | 信用3年、销售额月度、社保趋势、缴税节奏 | credit_level + value_added + social + payment | 趋势曲线、异常点 |

**补充维度**：银税互动（贷款）并入 ① 作为「融资健康」子指标。

---

## 六、研判与问答层（AI，核心）

### 研判原子结构（后端标准对象）

```json
{
  "conclusion": "建材批发行业逾期率最高",
  "evidence_chain": ["15,328笔中3,757笔逾期(26.6%)", "建材36家中23家有逾期", "23/36=64%"],
  "trace": "syx_tax_payment 表 → 明细",
  "followups": ["为什么逾期高?", "对比软件开发行业"]
}
```

**呈现规则**：对话里只渲染 `conclusion + followups`（证据链**隐藏**）；报告里才完整写 `evidence_chain + trace`。

### 意图解析

问句 → 判断「功能 × 维度切片」（维度：行业/地区/时间/信号/整体）。

### 上下文记忆与追问

- 复用 `session_store`，记录「关注维度 + 已覆盖功能 + 上轮结论」
- 每个结论自带追问方向，对话树状生长，不是直线问答

### 报告自动生成

当「某维度 × 已覆盖功能」≥ 阈值 → AI 自动提示「是否生成 XX 组合报告」。

---

## 七、报告层

**报告 = 维度 × 功能 的组合**，不是单一固定文档：

```
维度（行业/地区/时间/信号/整体） × 功能（评分/真实性/反欺诈/基准/趋势）
例：行业×趋势 → 《各行业趋势研判》；地区×评分 → 《广东地区风险评分》
```

**报告结构**（每章三段式：功能是什么 → 结论 → 证据链）：

```
摘要：研判总览 + 关键发现
第一章 风险评分引擎   第二章 经营真实性验证   第三章 反欺诈能力
第四章 行业基准库     第五章 趋势研判         附录：数据说明 + 方法说明
```

复用 `report_generator.py`（Jinja2 + WeasyPrint），重写模板。

---

## 八、前端交互层（研判中心，唯一入口）

**没有独立的报告中心**，一切在一个「研判中心」内发生。

### 卡片系统（引导 + 快捷输入）

| 卡片类型 | 内容 | 点击后输入框 |
|---|---|---|
| 维度卡（选范围） | 整体/按行业/按地区/按时间/按信号 | "分析各行业的…" |
| 功能卡（选分析） | 评分/真实性/反欺诈/基准/趋势 | "…的风险评分情况" |
| 组合卡（AI 动态） | 建材批发×趋势、广东×评分 | "分析建材批发行业的趋势走向" |

**卡片美观四原则**：紧凑 chip 不占地方；分组清晰（维度中性色/功能品牌色/组合强调色）；动态增删不铺满；选中态明确。

### 完整用户流程

```
1. 进入研判中心 → AI 打招呼 + 维度卡/功能卡
2. 点 [按行业]+[趋势] → 输入框填入"分析各行业的趋势走向"
3. 发送 → AI 给结论（结论+追问，证据链隐藏）
4. 追问 → 上下文累积
5. 覆盖度够 → AI 提示"生成《行业趋势研判报告》？"
6. 确认 → 组合报告（证据链完整写入）
```

---

## 九、与现有系统的关系（复用框架 + 重构业务层）

**判断：不重新搭建，做「换血式」改造。**

| 层级 | 判断 | 内容 |
|---|---|---|
| **复用** | ✅ | `main.py`/`deps.py`/`session.py`/`responses.py`/`auth_service`/`rate_limiter`/`session_store`/`email_service`/`report_generator`(改模板)/`llm_reply`(改 prompt)/测试+CI；前端 `App.tsx`/路由/`apiClient`/`dataSource`/`ChatPanel`(改)/`Dashboard`(改)/主题背景 |
| **重写** | ❌ | `CoreMetrics` 模型、`assessment.py`(数据源换税务)、`intent_engine.py`(名字解析→研判意图)、ETL(全新) |
| **新增** | ➕ | 研判原子结构、证据链管理、`industry_benchmark`、`conclusion_store`、报告切片、行业聚合 |
| **砍掉** | ✂️ | `EnterpriseSearch`、`EnterpriseDetail`、`NetworkGraph`、独立报告中心、`mock_data`、`legal_service`(企查查口径) |

---

## 十、构建清单（分阶段）

### P0 · 数据底座（ETL）
- [ ] ETL 脚本：解析 `数据.sql` → 结构化
- [ ] `core_metrics` 宽表 + `display_label` + 行业聚合(107→6)
- [ ] 五维指标字段计算 + 原始明细表保留

### P1 · 评分 + 基准
- [ ] 五维评分引擎 + 风险等级 + 预警信号
- [ ] 行业基准库（分大类均值·分位，**可重算**）

### P2 · 真实性 + 反欺诈
- [ ] 三口径交叉验证（183 家）
- [ ] 进销商品错配（scbm）+ 红字发票 + 集中度

### P3 · 研判原子 + AI 问答
- [ ] 研判原子结构 + 意图解析 + 上下文记忆 + 证据链规则

### P4 · 报告
- [ ] 组合报告生成（维度×功能）+ 模板 + 自动补报告

### P5 · 前端
- [ ] 研判中心页面 + 卡片系统 + AI 对话（证据链隐藏）+ 卡片美观

---

## 十一、数据部分需采取的措施

### 1. 环境准备
- [ ] PostgreSQL 实例（现有 docker-compose 可用）
- [ ] 1.95GB `数据.sql` 导入方式确认

### 2. 合规与授权（⚠️ 最重要）
- [ ] 明确使用授权范围、能否对外演示
- [ ] 注意：股东姓名/身份证/电话/经营范围/税局分局仍明文，可能反推企业，需确认是否二次脱敏

### 3. 规则拍板
- [ ] 行业 107→6 大类映射规则确认
- [ ] `display_label` 字段组合（**建议不含股东姓名**）
- [ ] 负数发票/红冲口径：虚开信号 vs 正常退货

### 4. 数据质量决策
- [ ] 5 张空表确认放弃
- [ ] 低覆盖表（现金流61%/社保50%）缺失标注策略
- [ ] 违法类型脱敏 → 法律维度只算量不算型

### 5. 溯源数据保留
- [ ] 是否保留原始明细表（56万商品明细等），决定溯源深度

### 6. 未来更新
- [ ] 静态 dump 还是增量？标准库/基准是否随新数据重算

---

## 附录：一句话总结

> **框架留下来，器官换一遍。** 现有 FastAPI+React+LLM+PDF 框架复用；业务层从「查企业」换成「功能展示 + AI 研判 + 组合报告」；数据以 taxpayer_id 为主键、零名字依赖；报告是维度×功能的组合产物。
