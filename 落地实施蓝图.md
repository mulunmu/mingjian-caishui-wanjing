# 明鉴 · 落地实施蓝图（前端 + 后端）v2

> 本文档 = 当前阶段的**回滚点 + 落地指导**。执行顺序已按「打磨优先、验证在后」重排。
> 版本：2026-09-13 v2 · 与 `系统诊断与改进方向.md` 配套（那份是「病根诊断」，这份是「怎么落地、按什么顺序落地」）。

---

## 〇、定位与决策（回滚点）

### 定位（定稿）
- 项目 = **工程作品**，不是理论研究。
- **竞争性 = 稀缺涉税数据 + 统一口径引擎 + 可信交付**（不靠「学术理论创新」）。
- 已作废方向：纳税信用评级复现（规则公开，反推无意义）、「补数据/扩样本」（数据封闭）。

### 产品北极星（一切打磨的准绳）
- **小白可读、可用、一眼看出核心**：外行（信贷员/评级员/稽查员）一看就懂、一来就会用。
- 报告：不失严谨地让小白读懂「结论 + 为什么」；UI：首屏即核心功能要点；功能：零门槛上手。
- **这一层靠工程打磨，不靠新算法**——引擎算法已齐全，缺的是「把引擎结果翻译成小白话」的最后一公里。

### 数据约束（不可动摇）
- 193 家**匿名**企业、封闭集、无法补充；外部 API（企查查等）只在**落地期**用于「查别的企业」，且要收费。
- 系统本质 = **固定数据 + 统一口径指标引擎**；数据是死的，功能是活的——打磨点是「功能」。

### 三个炸点（核心卖点，回答「独特性不凸显」）
1. **维度对比**：企查查「身份维度」vs 明鉴「经营维度」——「注册资本能吹，三套账对不上没法吹」。
2. **可信溯源**：随机抽一家（虚拟名 + 真实脱敏数据）→ 结论 + 为什么 + 每个数字点回原始字段。
3. **可复用实证**：接公开数据源证明引擎可插拔；「三口径真实性」别人复现不了 = 最硬护城河。

### 本会话关键修正
- 炸点不是「同名对比」是「**维度对比**」（数据匿名，无法同名）。
- **匿名不是短板，是合规加分项**（脱敏 = 隐私合规 = 可信背书）。
- 可复用是**部分**的：财务/经营指标可复用；三口径真实性只有涉税数据能做，公开数据源补不了。
- **执行顺序（本次重排）**：先打磨已有功能（含账号记忆性）→ 打磨完善后才开始数据验证（炸点）。

---

## 一、执行顺序（一句话）

**打磨优先：先让「已有功能」不丢、不乱、不突兀、像顾问；数据验证（三个炸点）排在打磨之后。**
账号记忆性是打磨的第一件事（数据都记不住，谈不上打磨）。

---

## 二、阶段一：打磨（先做）

### 2.0 账号记忆性（新增，最高优先）★

**目标**：登录账号后，之前的**对话历史 + 报告列表**都能找回，不因刷新/重登/30 分钟过期而丢失。

**现状（根因）**：
- 报告：已按 `owner` 落盘 + `can_access_report` 过滤（`AUTH_REQUIRED=true` 时生效），**基本没丢**。
- 对话：**丢了**。① `ChatSessionRecord` 无用户字段，只按 `session_id` 存；② `session_store.py` 有 `TTL_SECONDS=30*60` 过期；③ 前端 `chatStore.ts` 的 `messages`/`sessionId` 只存内存，刷新即清；④ 后端无「历史会话列表/恢复」接口（`api/v1/chat.py` 只有 `POST /chat`）。

**后端改动**：
1. `models/engine_store.py`：`ChatSessionRecord` 加 `owner: Mapped[str | None] = mapped_column(String(255), index=True, nullable=True)`（需增量迁移/ALTER，不重建表）。
2. `services/session_store.py`：
   - `ensure_session_id(sid, owner=None)` 建会话时记录 owner。
   - **拆 TTL 语义**：活跃上下文（`last_intent` 等）仍 30min 新鲜度；`history_json` 历史归档保留 N 天（默认 7 天），不再被 30min 清掉。
   - 新增 `list_sessions(owner)`（按 owner 查最近 N 条，返回 sid + 首条摘要 + updated_at）、`load_history(owner, sid)`（校验归属后返回 history）。
3. `api/v1/chat.py` 新增：
   - `GET /chat/sessions`（`get_current_user` → 当前用户会话列表）。
   - `GET /chat/sessions/{session_id}`（归属校验 → 返回历史消息）。
   - `DELETE /chat/sessions/{session_id}`（可选，删历史）。
   - 现有 `POST /chat` 继续传 `session_id` 续期。

**前端改动**：
1. `stores/chatStore.ts`：
   - `sessionId` 写入 localStorage（key 按用户隔离：`chat_session_<email>`）。
   - 新增 `restoreHistory()`：登录/挂载后 `GET /chat/sessions` → 取最近会话 → `GET /chat/sessions/{id}` → 回填 `messages` + `sessionId`。
   - `clearChat`：清当前会话并开新 `session_id`（不误删历史）。
2. 新增「历史会话」下拉/侧栏组件（列会话、点击切换继续）。
3. 登出时清 localStorage 的 chat 缓存（`authStore.logout` 同步）。

**验证**：生成报告 + 聊几句 → 登出 → 重新登录 → 报告列表仍在 + 对话历史可恢复。

---

### 2.1 统一设计系统（ivory / navy / gold）★

- **前端**：`src/styles/globals.css` 的 `--primary` 从 indigo 改为品牌深蓝；`tailwind.config.ts` shadcn 变量对齐。语义色前后端统一（绿好/黄警/红险）。
  - 令牌：纸底 `#F7F4EE` / 藏青 `#152446`（深 `#0B1C3E`）/ 古铜金 `#A18A5F`。
- **后端**：`backend/app/templates/_brand_tokens.css` 对齐同一套（现在报告是深蓝 `#003366` + 金 `#C9A24B`，统一到上述令牌）。
- 说明：这是其余所有打磨的底色，**第二个做**（炸点页/报告/对话都要用它）。

### 2.2 导航与信息架构收敛（7 → 3）

- **前端**：`src/components/layout/Header.tsx` 收为 3 项主线：**出报告 / 风险研判 / 数据接入**；反欺诈·真实性降为报告章节/对话能力；发送记录·账号进用户菜单。
- `src/App.tsx` 默认落地页从 `ResearchCenter` 改为**报告向导**（呼应「报告是卖点却不在首屏」）。

### 2.3 风险评估/对话「死板」→ 自然 + 结果小白可读 ★

- **后端** `backend/app/services/llm_reply.py`：
  - 去掉 `[规则模板生成]` 前缀。
  - 输出结构从「5 句结论列表」改为「**一句话大白话结论 → 1-2 个关键数字（可点溯源）→ 一条能照做的建议**」。
  - 加**术语翻译层**：内部指标名（进销错配/序列缺口/六维雷达/税负）→ 大白话别名（进货销货对不上/开票断断续续/该交的税明显偏低）。
  - 适配四场景：放贷答「能不能贷」/ 评级答「等级」/ 预警答「哪里异常」/ 稽查答「哪里可疑要查」。
- **修「死板」根因（引擎演示心智）**：风险评估对话不再「始终在 193 家里翻」，改成「演示引擎能算什么、能看出什么」——默认抛一个已跑好的匿名样例结论，用户顺着继续问，而不是对着满屏维度/功能卡片选。
- **前端** `ResearchCenter` + `stores/chatStore.ts`：
  - 开场从「选维度/功能卡片」改为「引擎演示」：先给一条示例结论 + 大白话 followups（"这家能贷吗？/ 哪里不对劲？/ 信用怎么样？"）。
  - 结果渲染：结论置顶、数字高亮可溯源、原因大白话。
- **结果小白可读 = 硬指标**：对话输出的每一轮，外行读第一句就知道「好/坏/风险在哪」，不依赖任何财税术语。

### 2.4 报告封面（回纹 + 细网格，极简：名字 + 时间）

- 已生成 `cover_frame_loan/rating/warn/audit.svg`（四场景强调色：放贷蓝/评级金/预警橙/稽查红），保留回纹框 + 系统名作门面。
- **封面只放：报告名（title）+ 报告日期 + 编号**，其余一律不上封面。
- **去掉**（这些属正文/执行摘要，不是封面内容）：场景标签（放贷研判）、主体（全库样本）、一句话结论（群体风险判断「中高风险」，样本193家）、等级徽章（群体风险判断·中高风险·偏弱）。
- **后端**：`enterprise_report.html` / `slice_report.html` 封面改为「标题 + 日期 + 编号」，`cover_meta` 的 `scenario_label/subject/one_liner/risk_level/business_level` 不再渲染到封面（结论后置到执行摘要）。

### 2.5 报告「结论 + 原因」（小白可读）

- **后端**：`report_html.py` 章节头铺 `business_level` 好坏标签；每个关键数字跟「偏强/正常/预警」；结论句升级为「结论 + 为什么」（原因用大白话）；建议改「能照做」的动作句。

### 2.6 场景对齐（portrait/alert → 四业务场景）

- **后端**：`backend/app/services/report_templates.py` 场景 `portrait`(画像)/`alert`(预警) → **放贷/评级/预警/稽查**。
- **前端**：`src/constants/reportScenarios.ts` 同步四场景。

### 2.7 风险态势「突兀」收敛（193 家 dump 问题）

- **前端**：`src/pages/` 风险态势/overview 页不再一进来就摊开 193 家；改为「**先给结论层，再下钻**」——首屏展示行业/全局态势与「异常企业 TopN」，点开再看单家（渐进式披露）。
- **后端**：`overview` 接口已能出聚合，确认返回「全局结论 + TopN 异常」，不整表下发。

### 2.8 UI 减负（拥挤 → 留白 / 渐进披露）★

- **前端** 全局密度收一档：
  - 卡片/列表加大内边距与间距，**一行只承载一个主结论**，副信息折叠进「更多」。
  - 首页/风险评估页默认只给 2-3 个核心动作，其余收进次级入口（渐进披露）。
  - 数据表/列表默认只显示 **TopN + 「查看全部」折叠**，不整表铺开。
- **具体页**：
  - `ResearchCenter`：砍掉满屏「维度/功能卡片」堆叠，改为「结论区 + 一个提问框 + 少量大白话 followups」。
  - 风险态势页：首屏只留「全局结论 + 异常 TopN」（与 2.7 合并）。
  - 报告中心：卡片降密，一行 2-3 张，删除/发送等操作收进「···」菜单。

---

## 三、阶段二：数据验证 / 独特性炸点（打磨完善后做）

> 打磨（阶段一）做完，功能「不丢、不乱、不突兀、像顾问」了，再谈「把独特性讲出来、用数据验证」。

### 3.1 维度对比屏（企查查身份 vs 明鉴经营）
- **后端**：新增 `api/v1/contrast.py` `GET /contrast/{enterprise_id}`，返回 `public_profile`（注册资本/行业/成立时间，司法失信标注「未接入」）+ `operation_profile`（三口径 + 税负 + 进销 + 红冲 + 信用等级）。数据已在宽表，只拼装。
- **前端**：新增 `ContrastPage.tsx` 两列对比 + 压轴句「注册资本能吹，三套账对不上没法吹」。

### 3.2 可信溯源（随机抽企业 → 结论 + 为什么 + 溯源）
- **后端**：`metric_registry.py` 每个指标补 `source`（源表/源字段/口径/记录数）；新增 `GET /enterprises/random`（虚拟名，明文企业名不落接口）+ `GET /enterprises/{id}/trace`。
- **前端**：`TraceDrawer.tsx` 数字可点 → 溯源抽屉。

### 3.3 可复用实证（接公开数据源，证明引擎可插拔）★ 数据验证
- **后端**：抽象 `etl/adapters/`（`syx_adapter` + `listed_company_adapter` 上市公司财报 → `enterprise_financials`）；在上市公司真实舞弊标签上跑财务指标，输出 AUC。
- **诚实边界**：三口径真实性无法在公开数据复现（申报/发票不公开），这恰是护城河。
- **前端**：`ReusabilityPage.tsx` 展示「同一引擎 · 两个数据源 · 都有区分度」。

---

## 四、里程碑（执行顺序）

| 里程碑 | 内容 | 顺序 | 说明 |
|---|---|---|---|
| M0 账号记忆 | 2.0 对话历史持久化+恢复、报告归属确认 | **① 最先** | 数据都记不住，谈不上打磨 |
| M1 设计系统 | 2.1 统一配色 | ② | 其余打磨的底色 |
| M2 导航+落地页 | 2.2 7→3、默认报告向导 | ③ | 主线清晰 |
| M3 对话自然化 | 2.3 去模板 + 死板根因 + 结果小白可读 | ④ | 顾问感 + 小白可读 |
| M4 报告打磨 | 2.4 封面 + 2.5 结论原因 + 2.6 场景对齐 | ⑤ | 核心交付物 |
| M5 交互收敛与减负 | 2.7 风险态势渐进披露 + 2.8 UI 减负 | ⑥ | 去掉「突兀」「拥挤」 |
| M6 演示炸点 | 3.1 维度对比 + 3.2 可信溯源 | ⑦ 打磨后 | 独特性被看见 |
| M7 数据验证 | 3.3 可复用实证 | ⑧ 最后 | 堵死「193 专用」质疑 |

**一句话顺序**：M0 账号记忆 → M1 设计系统 → M2~M5 打磨 → M6~M7 验证。

---

## 五、文件级改动清单（速查）

**后端新增**：`api/v1/contrast.py`、`api/v1/enterprise.py`（random+trace）、`etl/adapters/{__init__,syx,listed_company}.py`、验证脚本。
**后端修改**：`models/engine_store.py`(ChatSessionRecord 加 owner)、`services/session_store.py`(owner+TTL 拆分+list/load)、`api/v1/chat.py`(历史会话接口)、`services/llm_reply.py`(去模板+术语翻译层+小白可读结论)、`services/report_html.py`(结论+原因)、`services/report_templates.py`(四场景)、`services/metric_registry.py`(补 source)、`templates/enterprise_report.html`/`slice_report.html`/`_brand_tokens.css`(封面+令牌)。
**前端新增**：`pages/ContrastPage.tsx`、`pages/ReusabilityPage.tsx`、`components/ui/TraceDrawer.tsx`、历史会话侧栏组件。
**前端修改**：`stores/chatStore.ts`(sessionId/messages 持久化+恢复)、`stores/authStore.ts`(登出清 chat 缓存)、`App.tsx`(路由+落地页)、`layout/Header.tsx`(导航3项)、`styles/globals.css`+`tailwind.config.ts`(令牌)、`constants/reportScenarios.ts`(四场景)、`ResearchCenter`(引擎演示开场+结果小白可读)、风险态势页(渐进披露)、报告中心+全局(密度降档/留白)。

---

*配套文档：`系统诊断与改进方向.md`（病根诊断）→ 本文档（落地 + 顺序）。记忆文件已同步关键决策。*
