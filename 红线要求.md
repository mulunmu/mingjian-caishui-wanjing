# 明鉴系统红线 v2

> 版本：2.0  
> 生效日期：2026-09-16  
> 替代：`红线要求-v1-历史.md`  
> 适用范围：对话、RAG、指标、阈值、报告、模型编排、后续语音扩展。

## 一、目标与边界

系统目标是实现财税风控领域内的受控灵活对话、可组合报告和后续多端语音扩展。

系统不追求跨领域通用聊天，不替代专业金融分析师，不允许模型自由创造事实。域外问题必须正确弃权并引导回可处理范围。

## 二、十条不可破坏的红线

1. **事实唯一来源**：数字、企业名单、阈值命中、评级、风险结论只能由确定性代码和已实现工具产生。
2. **计划先于执行**：模型只能输出类型化 `SemanticFrame`、`ToolPlan` 或 `ReportBlueprint`，不能直接执行任意动作。
3. **禁止整篇报告生成**：报告必须先编译为章节和工具 DAG，逐章执行，逐章叙述。
4. **禁止隐藏阈值**：阈值、适用范围、行业/地区差异、来源和版本必须登记，不能在 prompt 或业务代码中散落硬编码。
5. **检索必须版本化**：只有 `validated` 且 `enabled=true` 的工具、指标和阈值可以进入候选集；`planned`、`draft` 对模型不可见。
6. **回答必须可溯源**：每个数字必须回到 `Claim`、工具调用、数据源、规则版本和报告章节。
7. **不确定就澄清，无数据就弃权**：禁止猜答、补数、伪造企业或把缺失数据补成结论。
8. **模型职责分离**：路由、规划、金融复核、叙述、校验可以替换，但不能越权。
9. **框架不是事实来源**：LangGraph、LangChain、pgvector 等可以用作编排、状态、检索和适配基础设施，但不得拥有事实和数值。
10. **未过评测不得切换**：旧路径、影子路径、新路径必须经过分阶段门禁，不能凭主观感受全量替换。

## 三、允许与禁止的技术框架

允许：

- LangGraph：状态图、持久化、中断恢复、人在环、流式执行。
- LangChain：文档、Embedding、Retriever、Reranker、Tool 适配。
- PostgreSQL、pgvector：结构化事实、版本、关系、向量和混合检索。
- LiteLLM、Instructor、Pydantic：模型适配和结构化输出。

禁止：

- 让框架自动决定业务事实、阈值或结论。
- 双模型网关同时管理同一模型路由导致行为不可追踪。
- LLM 生成数字后再由代码“事后找锚点”来掩盖越权。
- 在未验证工具存在时让规划器选择该工具。

## 四、Semantic Kernel

系统只有一个共享语义内核，对话和报告不得各自维护指标、章节和阈值。

核心对象：

- Concept Registry：金融概念、别名、口语表达。
- Metric Registry：指标、公式、单位、粒度、数据源。
- Threshold Registry：阈值、作用范围、严重度、动作、版本。
- Tool Registry：输入输出、依赖、成本、权限、状态。
- Scenario Registry：贷款、评级、预警、稽查、尽调等。
- Chapter Registry：报告章节和所需工具。
- Block Registry：KPI、图表、表格、叙述等报告组件。

状态机：

```text
planned -> draft -> validated -> deprecated
```

规则：

- 新指标从 `planned` 开始。
- 只有公式、数据源、别名、粒度和测试齐全后才能进入 `validated`。
- 阈值变化必须新增版本，不得静默覆盖历史版本。
- 同义别名可以增加，但不得改变指标口径。

## 五、对话红线

每轮对话必须落入：

- `answer`：可以回答。
- `clarify`：需要补充范围、主体或业务域。
- `abstain`：超出领域或没有数据。
- `refuse`：用户要求编造、伪造或绕过事实边界。

规则：

- 候选工具召回和工具执行权限必须分离。
- 实体未解析时仍可召回候选，但不能执行。
- 寒暄、能力问答、反馈、脏话、天气、多语言、未知企业必须走独立策略。
- 口语、指代、多意图和话题回溯不能靠新增正则修补主路径。

## 六、记忆与话题回溯

记忆单位是话题线程，不是单一 `focus_history`。

每个话题线程必须记录：

- 会话和轮次
- 父话题
- 摘要
- 实体和过滤条件
- 场景和意图
- 工具计划摘要
- Claim 引用
- 活跃/完成状态

必须支持：

- “上一个问题” = N-1。
- “上上个问题” = N-2。
- 跨越多个无关话题后，按语义引用旧话题。
- 引用旧话题继续追问，不自动改变当前活动话题。
- 明确回滚时才切换活动话题。

## 七、模型分工

推荐职责：

- 路由模型：语义帧、业务域、工具计划，建议使用低延迟非推理模型。
- 金融模型：金融术语归一化、因果复核，可选，不进入每轮必经路径。
- 叙述模型：把确定性 Claims 说成人话。
- 验证层：结构化校验、数字锚定和事实一致性。
- 硬闸门：代码实现，永不跳过。

金融模型未配置时，系统必须继续工作，不得因可选模型缺失阻断主流程。

## 八、报告红线

报告流程固定为：

```text
用户目标
-> ReportBlueprint
-> 章节工具 DAG
-> 确定性工具执行
-> ClaimGraph
-> Block 渲染
-> 报告装配
```

规则：

- LLM 不能直接生成整篇报告。
- 每个章节只使用本节声明的工具。
- 每个章节必须输出可追溯的段落块（指标结论、综合研判），不得只输出一段整章总述。
- 用户可见 PDF/HTML 不得暴露 `core_metrics` 等内部表名或技术溯源字段；溯源只保留在结构化 API。
- Claims 不得跨章节泄漏。
- 同一 Blueprint 重复执行必须产生相同 Claims，表达可以不同。
- 自定义报告保存 Blueprint，不保存一段不可复现的自由生成文本。

## 九、发布门禁

发布顺序：

```text
legacy
-> shadow
-> gate passed
-> canary
-> semantic primary
-> legacy retirement
```

门槛：

- 工具选择准确率不低于 90%。
- 检索 Recall@5 不低于 90%。
- 澄清和弃权正确率不低于 95%。
- 未锚定数字为 0。
- 报告 Blueprint 可复现率为 100%。
- 多轮回溯任务成功率不低于 85%。
- 影子评估达到样本、路由、覆盖率和延迟门槛。
- 删除旧入口或软降级正则前，退役审计必须输出 `safe_to_retire=true`。
- 活动 `app/` 不得导入 `legacy` 包；语义主路径内部失败必须受控返回 503，不得静默回旧路由。
- `backend/legacy` 不得重新存在于仓库或生产镜像；旧入口、旧正则和旧兼容模块必须保持删除状态。
- `CHAPTER_REGISTRY` 是章节唯一真源；不得重新引入章节兼容别名。
- 客户端提交非法报告场景必须返回 4xx，不得把输入错误转成 500。

`SHADOW_SEMANTIC_ENABLED` 在生产默认必须为 `false`，只有预发布评估时才开启。

## 十、每次改动必须满足

- 新功能先写失败测试，再实现。
- 新增指标必须有公式、源表、别名、粒度和覆盖率测试。
- 新增阈值必须有边界测试和版本。
- 新增工具必须明确输入、输出、依赖和适用场景。
- 所有变更后必须运行全量回归。
- 全量未通过时不得宣称完成，不得隐藏既有失败。
- Sentry、日志或生产监控未配置时，不得声称已接入。

## 十一、禁止事项

- 用正则修补模型主路径。
- 让 LLM 决定图表类型。
- 让 LLM 生成数值、名单或最终风险结论。
- 让未验证工具进入 RAG。
- 让影子路径写业务结论或覆盖旧回答。
- 在门禁未通过时删除旧路径。
- 以“临时验证”为名跳过测试或数字锚定。

## 十二、一句话准则

> 表达可以自由，事实永远封闭。  
> 模型可以规划，代码负责执行。  
> RAG 负责找候选，Registry 决定什么是真的。  
> 只有经过门禁的路径，才配接管生产。

## 十三、Stage 14 闭环补充

本节在 2026-09-17 Stage 14 完成后生效，优先级高于前文的旧实现描述。

### 编排边界

- 生产默认使用 `AsyncDagRuntime` 执行确定性金融 DAG。
- LangGraph 作为生产外层状态机，负责多 Agent 协作、checkpoint、interrupt/resume、循环和流式。
- `LANGGRAPH_OUTER_ENABLED=false` 时，主路径必须能够回到不依赖 LangGraph 的语义主路径。
- LangGraph、LangChain 或任何外层框架不得成为数字、阈值、名单、评级或报告结论的事实来源。

### 对话组装

- 每个 semantic-primary 回合必须经过已验证的 intent -> policy -> content -> synthesis 图。
- 多意图请求可以拆成多个子回合执行，但只能持久化为一个合并回合和一个正式回答。
- 跨话题回溯必须读取持久话题记忆，不得靠临时 prompt 或 Redis 热缓存伪装长期记忆。
- 数字只能来自子回合的确定性 Claims，合并回答不得重新生成或改写数字。

### 报告组装

- 自定义报告必须逐章节、逐 block 组装，禁止 LLM 一次生成整篇报告。
- 正式 block 类型为 `metric_paragraph`、`comparison_paragraph`、
  `trend_paragraph`、`synthesis_paragraph`。
- 章节与 block 必须通过兼容矩阵；不可组合时返回受控澄清或弃权。
- 报告快照、API 详情、前端展示和 PDF 必须使用同一 `blocks` 树。
- 报告 block 回滚只能通过 `REPORT_BLOCK_TREE_ENABLED=false` 或回滚镜像，不得删除历史快照。

### 发布与回滚

- 依赖版本必须冻结；LangGraph 依赖必须保持独立安装组。
- 回滚优先回退镜像/标签，禁止通过关闭主路径并静默恢复已删除的 legacy 入口。
- 数据库结构新增字段采用幂等迁移；回滚不得删除已验证字段或历史数据。
- Sentry 未实际配置时，任何报告、文档和对话不得宣称已接入 Sentry。

## 十四、Stage 16 生产质量与 LLM 延迟闭环补充

本节在 2026-09-17 Stage 16 完成后生效，优先级高于前文的旧实现描述。

### 核心回答生成

- 面向用户的专业分析回答必须由在线 LLM 生成，确定性 Claims 只提供事实和数字。
- 固定模板、预写回答、原始 Claim 直接拼接都不得作为核心对话的生产兜底。
- LLM 不可用、结构校验失败或事实闸门清空全部句子时，必须受控失败返回 503，不得伪装成成功回答。
- `reply_source=llm` 只能在模型输出实际通过结构校验和事实闸门后返回。

### 延迟与模型升级

- 分类使用一次非阻塞 `instructor` 调用，保留 Pydantic schema 约束，`max_retries=0`。
- 常规回答先调用一次快速模型（`LLM_DIALOGUE_MODEL`）的异步 JSON 路径。
- 快速模型失败后最多升级一次主模型（`LLM_MODEL`），升级调用必须继续受 schema 约束。
- 主模型升级路径只允许一次显式 schema 修复重试；空结论、缺字段和结构违约不得被当作成功。
- 禁止无界重试、禁止同一轮叠加 Instructor、JSON、模板和多模型多层兜底。
- 网络返回的 fenced JSON 或夹带说明文字时，先本地提取 JSON，不得为了解析多打一次模型。

### 生命周期与审计

- trace ID 必须贯穿请求、Agent、模型和回答元数据。
- 报告审批中断必须有 TTL；过期审批不得劫持新的无关问题。
- LangGraph checkpoint 必须支持保留期清理，默认先 dry-run，再显式 `--apply`。
- Sentry 只有 SDK、DSN、事件上报均实际验证后才能称为已接入；未配置 DSN 时必须明确说明未上报。

## 十五、Stage 17 指标与阈值闭环补充

本节在 2026-09-17 Stage 17 完成后生效。

### 指标终态

- 指标终态只能是 `validated` 或 `unsupported`；不允许长期停留在 `planned`。
- `validated` 必须同时具备真实源字段、确定性执行器、别名、溯源表和测试。
- `unsupported` 必须写明缺失源字段或缺失时间序列，且必须保持 `retrieval_enabled=false`、工具 `enabled=false`。
- 与已有已验证指标同源同口径的候选指标不得重复注册，应标记为 unsupported（重复口径）。
- 新增指标必须同时更新语义白名单、分类提示、RAG 工具快照和严格审计。

### 阈值闭环

- 阈值只能在其指标源字段和执行器已验证后启用。
- `cross_avg_deviation` 和 `cross_max_deviation` 是已注册指标，不得再作为无工具的孤立阈值存在。
- 阈值边界测试必须覆盖等于、低于和高于阈值。

### 真实性问题

- 缺注册资本、员工人数、逐月序列、欠税金额、进销项税额等源字段时，禁止用代理字段冒充原指标。
- 场景综合指标只有在全部必需输入存在且权重口径可审计后才能 validated。
- 未知企业、虚构企业、测试公司和明确不存在的企业必须走 `unknown_entity`，不得因上游误判为 analysis 而继续执行工具。
- Instructor 生成必须使用 JSON 模式承载大段结构化结果，禁止依赖可能产生嵌套 `arguments` 的 tool-call 文本。

## 十六、Stage 18 混合 RAG 补充

本节在 2026-09-17 Stage 18 完成后生效。

### 向量与索引

- 只有 `validated`、`enabled=true` 的工具可以生成 embedding。
- embedding 必须记录模型名、维度、文档哈希和工具 ID；同一工具/模型只能有一行。
- pgvector 表和 HNSW 索引是候选召回基础设施，不是事实来源。
- 模型、维度和缓存路径必须版本化；更换 embedding 模型必须重建全量索引并重新评测。

### 混合召回

- 生产候选集必须先经过 validated/executable 过滤，再做稠密和稀疏排序融合。
- 稠密检索使用 pgvector cosine，稀疏检索使用 BM25；标题、别名、场景和精确匹配可以加权，但不能绕过工具状态门禁。
- 融合后仍须由确定性执行器和事实闸门负责数字、阈值和结论。
- pgvector 或 embedding 不可用时只允许回退到确定性词法检索，不得回退到未验证工具。

### 评测门禁

- Golden set 的 Recall@5 不得低于 90%。
- unsupported 工具进入候选集必须为 0。
- embedding 数量必须覆盖全部 validated 工具。
- 每次更换 embedding 模型、索引参数或融合权重后，必须重新运行全量检索评测和 105 条对话矩阵。
