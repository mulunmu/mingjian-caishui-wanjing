# 明鉴 · 红线要求全篇审计报告

> 依据：`红线要求.md` v1.0（2026-09-14）  
> 审计范围：当前工作区代码（对话主路径 + 报告路径 + 能力地图 + 测试）  
> 审计日期：2026-09-15  
> 判定标准：`PASS` / `PARTIAL` / `FAIL`

---

## 总评

**整体判定：未达标（PARTIAL，关键铁律与架构项为 FAIL）**

脚手架已出现（`PERSONA`、`can_answer`、`CHAPTER_REGISTRY`、`SHAPE_TO_CHART`、`focus_history`、`MetricTool`/`DialogAct.tools`），但**主路径仍是「封闭 ActName 枚举 + 正则/关键词」**，不是红线要求的「工具调用计划 + 三层模型 + 硬闸门」。

| 章节 | 判定 |
|------|------|
| §1 数字铁律 / 事实封闭 / 可视化 | **FAIL / PARTIAL** |
| §2 表达自由 / 禁补丁 | **FAIL** |
| §3 弃权三态 | **PARTIAL** |
| §4 统一人格 | **PARTIAL** |
| §5 焦点栈记忆 | **PARTIAL（验收 FAIL）** |
| §6 三层模型分工 | **FAIL** |
| §7 三张能力地图 | **PARTIAL** |
| §8 实施红线 | **PARTIAL（多项 FAIL）** |
| §9 十条验收标准 | **多数 FAIL / 无自动化** |

一句话：事实侧有护栏雏形，表达与路由侧仍在用补丁体系；对话路径**没有**红线意义上的最终 `hallucination_guard`。

---

## §1 铁律

### 1.1 数字铁律 — **FAIL**

| 检查项 | 判定 | 证据 |
|--------|------|------|
| 数字由确定性引擎以 Claim 产出 | **PARTIAL** | `judgment_service` 确为 Claim 工厂；但 `inventory_scope` / `followup_items` / `slice_report` / `chat_router` 也直接造 Claim |
| LLM 不产出数字/名单/结论性事实 | **PARTIAL** | 多处 prompt 禁止编造；但 `plain_language` / `llm_reply` 仍要求「带关键数字」；`persona.warn` 要求「结论必须可量化」 |
| Claims 注入 LLM 作为唯一事实源 | **PASS** | `generate_claim_reply` 明确「给定结论（唯一事实来源）」 |
| `hallucination_guard` 硬闸门、逐字锚定 | **FAIL（对话）** | 报告路径有 `enforce_chapter_integrity`；**`chat_router` 零调用** `hallucination_guard`。对话仅 `_sanitize_conclusions` 过滤 conclusions；`report_hint` 未过闸；通义解读拼接后**无最终闸门** |
| 匹配为逐字一致 | **PARTIAL** | `hallucination_guard._normalize_num` 做归一化（`85.2`≈`85.20`）；中文数词不可见；违规句是丢弃而非打回重写 |

关键缺口（`chat_router.py` ~2052–2061）：

```
DeepSeek 叙述 →（注释写「硬闸门之前」）→ 通义【金融解读】拼接 → 直接返回
# 实际：拼接后没有 hallucination_guard
```

`llm_reply.generate_claim_reply` 中 `report_hint` 未经 `_sanitize_conclusions` 即拼进正文。

`generate_reply` 无 Claim 分支仍返回未过闸的 LLM 文本（即便当前主路由未必调用，仍是存活面）。

### 1.2 事实封闭 — **FAIL**

- 能力地图未成为 LLM 可调用工具的真源：`metric_registry.to_tool_schema()` **无调用方**；`DialogAct.tools` **路由从不消费**。
- 主路径仍是 7 类 `ActName` + `intent_engine` 关键词，而非「从地图选工具 + 填参数」。
- 引擎未算的指标，无系统化「弃权」落地到工具层（见 §3）。

### 1.3 可视化不由 LLM 决定 — **PARTIAL**

| 检查项 | 判定 | 证据 |
|--------|------|------|
| LLM 不选图 | **PASS** | 未发现 LLM 决定 chart type |
| `SHAPE_TO_CHART` / `infer_chart` | **PASS（API）** | `chart_payloads.py` 已定义 |
| 主路径只靠 shape 推断 | **FAIL** | `judgment_service` 仍硬编码约 16 处 `"type": "bar"|…`；`infer_chart` 仅 `followup_items` 使用 |

---

## §2 表达自由与禁补丁 — **FAIL**

### 2.1「表达自由、事实封闭」— **PARTIAL**

- 事实侧：Claim 锚定方向正确。
- 表达侧：`NarrationPlan.sentences` `max_length=5`；`SummaryPlan` `max_length=6`；prompt 写死「1–2 句」「恰好 3 句」——仍限制句数/句式，违反「只约束数字来源」。

### 2.2 对话要像人 — **PARTIAL**

- 有多意图拆分 `split_multi_intent`、指代/库存焦点合并雏形。
- 主理解路径仍是封闭枚举 + 正则归一，口语/省略/回溯未形成工具计划能力。
- 「回到刚才那批」类回溯未接线（见 §5）。

### 2.3 禁止补丁式修复 — **FAIL**

| 禁止项 | 现状 |
|--------|------|
| `dialog_act` 正则补丁 | **仍大量存在**：文件内 `re.*` 约 46 处；LLM 成功后仍跑 `_normalize_act`；无 LLM 时 `_soft_fallback` 正则主路径 |
| `_FUNC_PATTERNS` / `_DIM_PATTERNS` 仅作兜底 | **FAIL**：`intent_engine.recognize` 仍以关键词为结构主路径 |
| 封闭 `ActName` 7 类 → 工具计划 | **FAIL**：仍为 7 类 Literal；`tools` 字段空转 |
| 散落 `{"type":"bar"}` → `SHAPE_TO_CHART` | **PARTIAL**：表在，硬编码未清 |

`dialog_act.py` 文件头仍写「LLM instructor + pydantic **封闭枚举**」——与红线「必须替换为工具调用计划」直接冲突。

---

## §3 弃权机制 — **PARTIAL**

| 检查项 | 判定 | 证据 |
|--------|------|------|
| Schema 强制 `can_answer` + `clarify_question` | **PASS** | `DialogAct` 字段 + instructor |
| 路由三态分支 | **PASS（骨架）** | `chat_router` ~1129：有反问 / 弃权文案分支 |
| 编造请求必须拒绝 | **FAIL** | 无「编造」专用检测；软路径倾向 clarify，非「拒绝并说明数字只来自数据」 |
| 超纲弃权 | **PARTIAL** | Prompt 要求天气类 `clarify_question=null`；无 LLM 时软路径几乎总是 clarify，难达 abstain |
| 每轮必落三态 | **PARTIAL** | 依赖 LLM 自觉填 `can_answer`；低置信默认补 clarify_question，abstain 难触发 |

缺验收用例自动化：「帮我编一个营收」「今天天气怎么样」等无对应 pytest 闭环。

---

## §4 统一人格 — **PARTIAL**

| 检查项 | 判定 | 证据 |
|--------|------|------|
| `PERSONA` 定义符合文档 | **PASS** | `persona.py`：明鉴财税风控顾问 + 四原则 + fallback_style |
| 四场景语气叠加 | **PASS** | `TONE_VARIANTS` |
| **所有** LLM 调用注入同一 `PERSONA` | **FAIL** | 仅 `generate_claim_reply`（且需传入 persona）真正注入 |

未注入 `PERSONA` 的调用面（不完全列表）：

- `dialog_act._build_system`（理解层）
- `generate_financial_interpretation`（金融解读层）
- `generate_narration` / `generate_executive_summary`（报告解读）
- `llm_semantic_parser`、`classify_intent_llm`
- 定制报告轮次 LLM、`generate_reply` 兜底

另：`report_templates.TONE_PROFILES` 与 `persona.TONE_VARIANTS` 双轨，违反「叠加在同一 PERSONA 之上」。

---

## §5 记忆（焦点栈）— **PARTIAL（验收 FAIL）**

| 检查项 | 判定 | 证据 |
|--------|------|------|
| `focus_history` 结构（kind + ts） | **PASS** | `scope_state.merge_analysis_focus` 入栈 industry/province |
| 替代单值焦点为真源 | **PARTIAL** | 栈与 `industry_l1`/`province`/`enterprise_id` 并存，单值仍主用 |
| 「回到刚才那批」回溯 | **FAIL** | `resolve_focus_from_history` **定义但无任何调用方**；无出栈/回填 UX |
| 不引入 Mem0/Letta | **PASS** | 依赖中无此类框架 |
| PG + 内存可持久化 | **PARTIAL** | `session_store` 有持久化意图；需确认 DB 列是否已落地 |
| 入栈/出栈可测试可回放 | **FAIL** | 无 `focus_history` / `resolve_focus_from_history` 测试 |

---

## §6 三层模型分工 — **FAIL**

### 红线规定顺序

```
理解(DeepSeek) → 引擎 Claims → 通义点金解读 → DeepSeek 校验润色 → hallucination_guard → 回复
```

### 实际对话主路径

```
DeepSeek DialogAct(封闭枚举)
  → 引擎 Claims（+ 硬编码 chart type）
  → DeepSeek generate_claim_reply（叙述+sanitize conclusions）
  → [可选] 通义【金融解读】拼接
  → 返回（无最终硬闸门）
```

| 层 | 规定 | 实际 | 判定 |
|----|------|------|------|
| 理解层 | 意图 + **工具计划** + PERSONA + focus_history | 封闭枚举分类；无工具消费；无 PERSONA/焦点栈注入 | **FAIL** |
| 数字中枢 | judgment Claims + shape | 有 Claims；shape 并存但 type 仍手写；非唯一 Claim 源 | **PARTIAL** |
| 金融解读 | 通义点金，禁止新数字 | 可选；无 instructor schema；无 PERSONA；拼在叙述**之后** | **PARTIAL** |
| 校验层 | DeepSeek 铁律校验 + 大白话 | 与叙述合并为一次调用，且在通义**之前** | **FAIL** |
| 硬闸门 | 永不跳过 | 对话终态未跑；报告路径较完整 | **FAIL（对话）** |

模型栈本身（DeepSeek + litellm/instructor，无 LangChain）方向正确，但**流水线顺序与职责切开未完成**。

---

## §7 三张能力地图 — **PARTIAL**

| 表 | 判定 | 说明 |
|----|------|------|
| Metric Tools | **PARTIAL** | 指标带 `shape`；`to_tool_schema` 已写但**未接线到路由** |
| Chapter Tools | **PASS** | `CHAPTER_REGISTRY` 收拢章节并派生别名，报告/定制路径在用 |
| Shape → Chart | **PARTIAL** | `SHAPE_TO_CHART`/`infer_chart` 存在；主分析图仍手写 `type` |

红线「加指标不改路由」目前不成立：路由不读 MetricTool 表。

---

## §8 实施红线

### 8.1 禁止事项

| 禁止 | 判定 |
|------|------|
| 在 `dialog_act` 新增/依赖正则补丁 | **FAIL**（存量大，且 LLM 后仍归一） |
| LLM 自行决定图表类型 | **PASS** |
| 跳过 `hallucination_guard` | **FAIL**（对话终态） |
| 引入 LangChain / LangGraph / Mem0 等 | **PASS** |
| Prompt 写死回复模板 | **FAIL**（「恰好 3 句」等；`plain_language` 硬模板兜底） |
| 硬编码行业/地区/指标列表 | **FAIL**（`intent_engine` 内 `_INDUSTRY_KW` / `_PROVINCE_KW` 等） |

### 8.2 必须事项

| 必须 | 判定 |
|------|------|
| instructor + Pydantic | **PARTIAL**（主分类/ClaimBundle 有；通义解读无） |
| 铁律回归测试 | **PARTIAL**（报告侧强；对话+通义拼接弱） |
| 能力地图变更同步三表 | **PARTIAL**（Chapter/Shape 真；Metric 工具未进路由） |
| 新指标带 `shape` | **PASS**（注册表层面） |
| 弃权三态 schema 强制 | **PASS**（字段有；行为覆盖不全） |

---

## §9 验收标准对照

| # | 红线验收 | 判定 | 说明 |
|---|----------|------|------|
| 1 | 「编一个营收」拒绝 | **FAIL** | 无专用拒绝路径 / 无自动化 |
| 2 | 口语「咋样/瞅瞅」 | **PARTIAL** | 部分自然语言测偏模板，非工具路由 |
| 3 | 含糊则澄清 | **PARTIAL** | 有 clarify 骨架；目标话术无专用测 |
| 4 | 话题回溯焦点栈 | **FAIL** | `resolve_focus_from_history` 未接线 |
| 5 | 多意图两工具一并答 | **PARTIAL** | 有拆句；非双工具计划执行 |
| 6 | 天气超纲弃权 | **PARTIAL** | 依赖 LLM；无 pytest；软路径偏 clarify |
| 7 | 10 轮人格一致 | **FAIL** | 无测；PERSONA 未全层注入 |
| 8 | 新指标 `shape` 自动出图 | **FAIL** | 主路径仍写死 `type` |
| 9 | 20 条口语 ≥90% 且无正则 | **FAIL** | 正则仍深度参与 |
| 10 | 全量 hallucination_guard 无漏检 | **PARTIAL** | 报告较好；对话终态漏 |

---

## 风险优先级（建议修复序）

### P0 — 铁律物理载体

1. **对话终态强制 `hallucination_guard`**：对 `reply + 【金融解读】 + report_hint` 统一过闸；违规打回重写或剥离，不得静默漏数字。
2. **纠正三层顺序**：引擎 Claims → 通义解读（只解释）→ DeepSeek 校验润色 → 硬闸门 → 返回。
3. **`report_hint` / followups 凡含数字必须过闸**。

### P1 — 架构根源（禁补丁）

4. 以 `metric_registry.to_tool_schema()` + `CHAPTER_REGISTRY` 为 LLM 工具面；路由消费 `act.tools`，逐步废止 `ActName` 主路径。
5. 停止在 `_normalize_act` / `_soft_fallback` 加规则；存量正则列入删除清单并设收敛指标。
6. `judgment_service` 去掉硬编码 `type`，只出 `shape`，渲染统一 `infer_chart`。

### P2 — 弃权 / 人格 / 记忆

7. Schema + 路由保证：编造请求 → abstain/拒绝文案；超纲 → abstain；低置信 → clarify。
8. 理解层 / 金融层 / 报告叙述层全部 `build_persona_prompt`；合并双轨语气表。
9. 接线 `resolve_focus_from_history`（「回到…」）；补 PG 列与 push/pop 单测。

### P3 — 验收自动化

10. 为 §9 十条各补至少 1 个 pytest（或固定 HTTP smoke），纳入 CI；尤其 #1/#4/#6/#9/#10。

---

## 已合规亮点（避免误判为「全盘皆无」）

- Claim 作为叙述唯一事实源的主设计正确。
- 报告路径 `hallucination_guard.enforce_chapter_integrity` 较完整。
- `CHAPTER_REGISTRY` 收拢成功；指标注册普遍带 `shape`。
- `PERSONA` 字典与四场景叠加定义符合文档原文。
- `can_answer` / `clarify_question` 已进 Pydantic schema。
- 未引入 LangChain / LangGraph / Mem0 / Letta。
- LLM 不负责选图。

---

## 结论

对照《红线要求》十条铁律与验收表，当前代码处于**「契约表与字段已铺设，主路径未切换」**阶段：

- **表达侧**仍被枚举、正则、句数模板锁死；
- **事实侧**对话终态硬闸门缺失，存在绕过面；
- **架构侧**三层顺序颠倒，工具计划空转，焦点回溯未落地。

在 P0 闸门与 P1 工具路由未完成前，任何「更灵活」的 prompt/正则增量都应视为红线风险，而不是进度。

---

*审计口径：以 `红线要求.md` 原文条款为准，对照当前仓库实现与测试覆盖，不依赖历史里程碑自评。*
