# 明鉴 · 红线要求全篇审计报告（第四次）

> 依据：`红线要求.md` v1.0  
> 审计日期：2026-09-16  
> 轮次：第 4 次（对照原文 + 本轮已合入改动核验）

---

## 总评

**整体：仍未全达标，但第 3 次点名的「立刻」项多数已落地。**  
护栏与流水线从「假达标」推进到「主路径基本对齐」；架构债（封闭枚举、`_normalize_act`、句锁、硬编码行业）仍在。

| 章节 | 第3次 | 第4次 | 说明 |
|------|-------|-------|------|
| §1 数字铁律 | PARTIAL↑ | **PARTIAL↑** | 统一闸门 + 空 allowed；仍非逐字打回 |
| §2 禁补丁 | FAIL↓ | **FAIL↑** | 编造/超纲去正则主路径；LLM 后仍 `_normalize_act` |
| §3 弃权 | PARTIAL↑ | **PARTIAL↑** | `refusal_kind` schema 化 |
| §4 人格 | PARTIAL | **PARTIAL↑** | 通义带 scenario；报告层未全覆盖 |
| §5 焦点栈 | PARTIAL | **PARTIAL↑** | `skip_current` 取上一层 |
| §6 三层模型 | FAIL | **PARTIAL↑** | 通义→DeepSeek→闸门；理解层非纯工具计划 |
| §7 能力地图 | PARTIAL | **PARTIAL↑** | `act.tools` 路由已消费；仅首工具 |
| §8 实施红线 | PARTIAL | **PARTIAL↑** | judgment 去 type；normalize_meta_charts |
| §9 十条验收 | FAIL↑ | **PARTIAL↑** | #1/#4/#6/#8/#10 有测进展 |

---

## 相对第 3 次的关键落地

1. **流水线顺序**：Claims → 通义解读 → DeepSeek 校验润色 → `apply_chat_hallucination_guard`（不再后拼【金融解读】）。  
2. **空 allowed**：无数字 Claim 时剥离一切含数字句，闸门永不因空集跳过。  
3. **`refusal_kind`**：删除 `_FABRICATE_RE` / `_OUT_OF_DOMAIN_RE`；LLM schema + 无 LLM 关键词兜底。  
4. **工具路由**：`resolve_analyze_tools` / `chapter_tool_catalog` / `to_tool_schema` 进 prompt 与路由。  
5. **图表**：judgment 只出 `shape`；路由 `normalize_meta_charts`；`_bar_chart` 改 shape 推断。  
6. **焦点**：`resolve_focus_from_history(skip_current=True)`。

---

## 仍 FAIL / 高优先级债

| 项 | 现状 |
|----|------|
| 封闭 `ActName` 7 类 | 未替换为纯工具计划主路径 |
| LLM 后 `_normalize_act` | 仍调用 |
| `NarrationPlan` / 「恰好 3 句」 | 句数句式锁仍在 |
| PERSONA 全覆盖 | 报告叙述/摘要/定制等未统一 |
| `_INDUSTRY_KW` / `_PROVINCE_KW` | 仍硬编码 |
| 多意图并行工具 | 仅消费 `tools[0]` |
| §9 #2/#5/#7/#9 | 自动化仍不足 |

---

## 建议下一轮顺序

1. 削弱/旁路 LLM 成功路径上的 `_normalize_act`（先度量再删）。  
2. `NarrationPlan` / chat prompt 放开句数，只保留数字锚定。  
3. 报告层统一注入 `PERSONA`。  
4. 行业/地区选项改为数据源动态读取。  

---

*口径：以 `红线要求.md` 为准，结合当前代码与已有 pytest。*
