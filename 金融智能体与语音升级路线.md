# 明鉴 3.0 升级路线：金融智能体理解层 + 组件化报告 + 语音智能体

> 调研日期：2026-09-14 · 依据：项目现状实读（对话链路 + 报告链路 + 定制报告）+ 网络调研（论文 / GitHub / 论坛）
> 配套文档：`项目全方位调研.md`（技术现状锚点）、`落地实施蓝图.md`（打磨+验证顺序）、`交互与报告重构方案.md`（交互重构方法）、`PRINCIPLES.md`（铁律）
> 定位约束：工程作品（非理论研究）；竞争性 = 稀缺涉税数据 + 统一口径引擎 + 可信交付；铁律 = **数字只由算法产生，LLM 只分类 / 叙述 / 引导**

---

## 〇、先修正一个关键认知（决定方案形状）

你描述的架构是：「两套对话（LLM + 规则回答）中间加一个金融智能体当理解层」。

**代码现状不是这样。** 系统当前已经是一条完整的「理解 → 执行 → 叙述」链路，理解层已有雏形：

| 层 | 现有模块 | 作用 |
|----|----------|------|
| 意图/行为理解 | `dialog_act.py` | DialogAct 7 类行为分类（LLM instructor 结构化 + 正则降级） |
| 语义理解 | `llm_semantic_parser.py` | NL → SemanticQuery 语义 IR（借鉴 SuperSonic SchemaMapper→Parser→Corrector→Translator） |
| 执行（确定性引擎） | `judgment_service.py` | 数字/Claim 全由算法产出 |
| 表达/叙述 | `llm_reply.py` | LLM 只组织语言，`plain_language.py` 15 条术语映射 |

"LLM 回答"和"规则回答"是**同一条链路的两个分支**（LLM 可用走 LLM、不可用降级模板），不是两套独立对话系统。

所以你要做的**不是"中间加一层金融智能体"，而是把现有理解层和叙述层从"通用 LLM + 15 条硬编码术语"升级成"金融领域化的大脑"**。缺的是三样东西，正好对应你说的三个痛点：

| 痛点 | 根因（已实读代码确认） | 解法 |
|------|------------------------|------|
| 语义理解太弱、不灵活 | LLM 是通用模型，prompt 只有「行业/指标白名单」，**没有金融领域知识** | 注入**金融指标语义层 + 金融知识库** |
| 指标解读/语义转换死板 | `plain_language.py` 只有 **15 条**硬编码术语替换，无法做因果解读 | 用**金融大模型 API 做「指标 → 白话 → 因果」生成**（守铁律） |
| 无法引导用户提问金融问题 | 追问是固定模板（`build_fraud_followups` 等），非上下文动态生成 | 加**主动引导（proactive 追问规划）** |

---

## 一、可行性判断总表（先给结论）

| 方向 | 可行性 | 成本 | 主要风险 | 竞赛/毕设价值 |
|------|--------|------|----------|---------------|
| **短期1 金融智能体理解层** | ★★★★★ 高 | 中（纯软件，复用现有链路） | 铁律被破（LLM 编数）、过度工程（自训大模型） | 最高：直接回应"AI 交互"这个展示点 |
| **短期2 报告可视化+组件化** | ★★★★★ 最高 | 中低（**代码已有 80% 骨架**） | 组件过多导致报告失控、图表响应式没做好 | 高：核心交付物升级，小白可读 |
| **长期 语音智能体** | ★★★★ 中高 | 中高（新增 ASR/TTS/实时编排） | 800ms 延迟、打断、多端适配、真硬件过度工程 | 中高：软硬结合是加分项，但要控制范围 |

**一句话可行性结论**：三个方向技术上都成熟、都落地得了；**性价比排序 = 短期2（组件化报告）＞ 短期1（理解层）＞ 长期（语音）**。短期2 的代码骨架已经现成，是"收尾 + 暴露参数"的活，最容易见效。

---

## 二、短期1：金融智能体理解层（对话升级）

### 2.1 定位：不是新对话系统，是"给骨架装金融大脑"

沿用 FinGPT 的 **data-centric** 路线：**不重训模型，用「领域数据 + 结构化知识 + 指令」让通用大模型获得金融能力**。这是本方案最核心的选型决策：

> ❌ **不要自训练/自部署金融大模型**（FinGPT / 轩辕 / BloombergGPT 都需 GPU，竞赛/毕设训不起也没必要）。
> ✅ 用 **LiteLLM 换更强的通用模型或金融大模型 API**（现状已支持，`LLM_MODEL` 一键切换），把金融能力通过**知识注入**补上。

### 2.2 三层升级设计（对应三个痛点）

```
用户话术
   │
   ▼
[理解层] DialogAct + SemanticQuery        ← 已有，注入金融指标语义层做约束
   │
   ▼
[知识层] 金融指标语义库 + 金融知识库       ← ★ 新增：指标别名/好坏阈值/因果模板/金融含义
   │
   ▼
[执行层] judgment_service（Claim/数字）    ← 已有，不动（铁律）
   │
   ▼
[解读层] 金融大模型「指标→白话→因果」      ← ★ 升级 llm_reply + plain_language
   │
   ▼
[引导层] proactive 追问规划                ← ★ 升级 followup_items
```

#### ① 知识层：金融指标语义层（把 15 条术语升级成结构化语义）

现有 `plain_language.py` 是平铺的 `TERM_ALIASES` 列表，改成**指标语义层**——在 `metric_registry.py` 给每个指标挂字段（这呼应 SuperSonic 的 MDL 语义层、调研里金融风控"知识图谱语义层"的思路）：

```python
# metric_registry 每个指标扩展（示意）
{
  "key": "tax_burden_ratio",
  "canonical": "税负率",
  "alias": "实际交税占营收的比例",        # 大白话别名（小白读）
  "good_when": "处于行业合理区间",         # 好坏判据
  "causal_hint": "税负明显偏低常意味着收入申报不足或成本虚列",  # 因果解读模板（金融知识）
  "finance_meaning": "衡量企业税负是否与经营规模匹配，是稽查重点关注项",  # 金融含义
  "source": {"table": "core_metrics", "field": "..."}  # 溯源（复用已有 source 计划）
}
```

这同时服务对话解读 + 报告可读性 + 溯源三个需求，一举三得。**金融知识库**先做成结构化 JSON（指标口径 + 四场景决策逻辑 + 金融概念），不急着上向量库——竞赛/毕设阶段结构化检索足够，且**可审计、零幻觉**（比 RAG 向量库更符合"数字不编"铁律）。真要上 RAG，参考调研里的 Bayesian RAG（不确定性感知）压低幻觉。

#### ② 解读层：金融大模型做「指标 → 白话 → 因果」

升级 `llm_reply.generate_claim_reply` 的 system prompt：**不再让通用 LLM 凭空改写，而是把"指标语义层"的 alias/causal_hint/finance_meaning 一起喂进去**，让它产出"结论 → 关键数字 → 为什么（因果）→ 能照做的建议"。铁律照旧：数字只能来自给定 claims，`hallucination_guard` 的 `_sanitize_conclusions` 照旧过滤。

```python
# llm_reply 升级点（示意，不动铁律）
system = (
  "你是明鉴金融风控顾问。只能改写已给定结论，禁止新增数字/企业名/事实。"
  "用指标语义层把内部指标翻成大白话，并给出因果解释（为什么这个数字意味着风险）。"
  f"指标语义层：{json.dumps(metric_semantics)}"   # ★ 注入金融知识
)
```

#### ③ 引导层：proactive 追问规划

现有 `followup_items` 是固定模板，改成**LLM 基于「当前结论 + 金融知识 + 对话历史」动态生成下一步追问**，但仍输出为**结构化芯片**（`drilldown/action/navigate/dialog_act`），不是自由文本——守住"追问必须指向可执行操作"的契约。这是"能像正常大模型一样聊天、同时引导用户问金融问题"的关键：**LLM 负责"该引导问什么"，引擎负责"这个追问能不能执行"**。

### 2.3 落地步骤（文件级）

| 步骤 | 文件 | 改动 |
|------|------|------|
| 1. 指标语义层 | `metric_registry.py` + `plain_language.py` | 每个指标补 `alias/causal_hint/finance_meaning`；`translate_terms` 改为读语义层而非硬编码 15 条 |
| 2. 金融知识库 | 新增 `finance_knowledge.py`（结构化 JSON/py） | 四场景决策逻辑 + 指标口径 + 金融概念解释 |
| 3. 解读升级 | `llm_reply.py` | `generate_claim_reply`/`generate_narration` 注入指标语义层上下文 |
| 4. 引导升级 | `followup_items.py` + `dialog_act.py` | followup 生成加 LLM 动态规划分支，输出仍为结构化芯片 |
| 5. 模型切换 | `.env` / `llm_reply._llm_completion_params` | LiteLLM 已支持，可选接入金融大模型 API 或更强通用模型 |

### 2.4 验收标准
- 问"税负率 0.5% 说明什么"→ 回答"实际交税占营收 0.5%，明显偏低，通常意味着收入申报不足或成本虚列，建议核查 X"（有因果、有可照做建议、无编造数字）。
- 用户随口问一句，系统能主动引导到放贷/评级/预警/稽查四场景之一。
- 铁律套件照过：`hallucination_guard` 不放过任何编造数字。

---

## 三、短期2：报告可视化 + 自定义报告组件化

### 3.1 现状（已实读确认，含好消息）

`slice_report.py` 的 `_build_context_from_spec`（:1552）**已经是事实上的"章节组件渲染引擎"**——它接受任意 `spec["chapters"]` 列表逐章装配；`CUSTOM_CHAPTERS` 的 8 个 function 已经是"组件注册表"雏形。**所以"自由组合"的引擎底座已存在，缺的是"暴露参数 + 前端 UI + 可视化升级"。**

两个明确缺口：
1. **可视化**：图表由 matplotlib 预渲染 PNG，`report_charts.py` 的 `figsize/dpi` 全硬编码，HTML 靠 `.chart-img{max-height:90mm}` 缩放，**非响应式**；图表类型由 `judgment` 的 `meta["charts"]` 写死，用户不能选。
2. **组合自由度**：现在是"选章节子集"（1~8 章任意组合），但**章内结构零自由度**（title/purpose/dimension 写死、图表不可选、KPI 不可选）；前端**没有任何勾选 UI，全靠对话**；`tone` 字段存在但未接入。

### 3.2 可视化升级（报告可读性）

| 问题 | 改法 | 位置 |
|------|------|------|
| 图表尺寸硬编码 | `figsize/dpi` 改为**由组件 spec 参数驱动**（每个图表组件声明期望尺寸/横竖/类型） | `report_charts.py` 各渲染函数 |
| 非响应式 | `.chart-img` 改响应式（`max-width` + 按 A4 页面栅格）；关键结论图可放更大、次要图收小 | `slice_report.html` / `enterprise_report.html` CSS |
| 图文顺序颠倒（头重脚轻） | 复用已有 `decision_memo` 密段：先文字结论 → 后限高图（蓝图 §2.5 已定） | `slice_report.html` 章节循环 |
| 小白看不懂数字好坏 | 每个关键数字后铺 `business_level`/`metric_level` 好坏标签（蓝图已定，补齐到全指标） | `report_html.py` / 模板 |
| 图表类型可选 | `chart_payloads.py` 已有 radar/heatmap/pie/funnel/**scatter（scatter 还没渲染器，是现成扩展点）** | `_render_chapter_charts` 加分支 |

### 3.3 自定义报告组件化（自由组合，细到"组件"）

把"章节"降级为"容器"，**章内可自由组合子组件**：

```
报告 = [ 章节1, 章节2, ... ]              ← 已有（8 function 任意子集）
章节 = 标题 + 结论段 + 子组件列表          ← 新增：子组件可配
子组件 = 图表组件(类型可选: 雷达/柱/折线/漏斗/热力图)
       + 数字表组件(字段可选)
       + KPI 组件(可选指标)
       + 白话结论组件(因果解读)
```

**落地方案（改 `custom_report.py:spec_to_report_spec` + `slice_report.py`）**：
- `CustomReportSpec` 增加 `chapter_blocks`：每章可声明 `chart_type`（可选）、`kpis`（可选指标子集）、`include_table`（布尔）。
- `spec_to_report_spec` 把 `dimension/title/purpose` 从写死改为**可被 spec 覆盖**。
- `_chapter_claims` 生成的章节 dict 已是自包含的"组件 props"（`{title, claims, charts, numeric_rows, meta}`），把 `charts`/`numeric_rows` 按 spec 的 `chapter_blocks` 过滤/选型即可。
- `tone` 字段真正接入 `get_scenario_tone`（现在被丢弃）。

**前端加可视化组合 UI（主要工作量在这）**：
- `ReportWizard` 从"三步向导"扩展第 4 步"**组装章节**"：左侧章节库（8 积木卡片）→ 拖入右侧画布 → 每章点开可勾选"要哪张图/要不要数字表/重点看哪几个 KPI"。
- 复用现有 `validate_custom_report`/`_chapter_available` 做**实时数据可用性提示**（"这个组件在当前范围没数据"灰显）——这是组件化的体验关键，避免用户组合出空报告。
- 保留对话定制作为等价入口（两者共用同一 spec，避免双入口漂移）。

### 3.4 落地步骤（文件级）

| 步骤 | 文件 | 改动 |
|------|------|------|
| 1. 图表参数化 | `report_charts.py` | figsize/dpi 改为参数驱动，补 scatter 渲染器 |
| 2. 响应式 CSS | `slice_report.html`/`enterprise_report.html` | 图表区块响应式 + 先文后图 |
| 3. spec 扩展 | `schemas/custom_report.py` | 加 `chapter_blocks` 字段 |
| 4. 组件装配 | `custom_report.py:spec_to_report_spec` | dimension/title/purpose 可覆盖、chart/kpi/table 按 spec 过滤、tone 接入 |
| 5. 前端组合 UI | `ReportWizard.tsx` + 新增 `ChapterComposer.tsx` | 章节拖拽 + 章内组件勾选 + 实时可用性提示 |

---

## 四、长期：语音智能体（软硬件结合）

### 4.1 技术选型（调研结论）

语音 Agent 的难点**不在模型，而在 800ms 延迟预算内的流式编排 + 打断 + 多端**。成熟开源栈（全中文友好）：

| 环节 | 选型 | 说明 |
|------|------|------|
| ASR | **SenseVoice**（阿里，多语言+情感）或 FunASR/Paraformer 流式 | 中文强，MIT/Apache |
| TTS | **CosyVoice**（阿里）或 Kokoro-82M（轻量流式） | 中文强，流式 |
| 编排框架 | **Pipecat**（传输无关，500-800ms）或 **LiveKit Agents**（WebRTC 原生） | 二选一 |
| 端侧（移动/嵌入式） | **sherpa-onnx**（ASR/TTS/VAD 全离线，Android/iOS/HarmonyOS/WebAssembly） | 真"软硬结合"的落点 |
| 打断 | Silero VAD + 语义 turn detection | 体验关键 |

### 4.2 分阶段（控制范围，避免过度工程）

| 阶段 | 目标 | 技术 |
|------|------|------|
| **P1 Web 端语音 Demo**（毕设可交） | 电脑浏览器里跟明鉴语音对话：问风险、要报告、说"发到我邮箱" | Pipecat/LiveKit + SenseVoice + CosyVoice + 现有 LLM 理解层 |
| **P2 移动端**（加分项） | 手机浏览器 H5/PWA 直接语音交互 | 复用 P1 后端，前端 WebRTC + 麦克风 |
| **P3 端侧/硬件**（演示/展望） | 离线语音（无网也能 ASR/TTS），或接嵌入式开发板 | sherpa-onnx 端侧 ASR/TTS + 轻量 LLM |

### 4.3 关键：语音 Agent 复用现有理解层，不是新造

语音只是**输入/输出模态**，中间的"理解层"完全复用短期1 的金融智能体——ASR 转文字 → 走现有 DialogAct/SemanticQuery/engine → TTS 播报。这保证语音不产生第二套逻辑，也守住铁律。

**语音要支持的"操作"，现有后端已经具备**：
- "定制报告" → `generate_custom_report`（已有）
- "报告发邮件" → 邮件功能全链路已做（`email` 模块 + outlook.py）
- "解读" → 短期1 的解读层

所以语音落地的核心工作量是**三件**：① 实时音频管线（ASR/LLM/TTS 流式编排）② 打断与端点检测 ③ 语音操作意图映射到现有 API（"发邮件"→ 触发邮件 tool call）。

---

## 五、风险与铁律（改动时勿破）

1. **数字铁律**：金融智能体的"解读/引导"再灵活，**数字永远由 judgment_service 产出**，LLM 只做"指标→白话→因果"的翻译，`hallucination_guard` 照旧。这是本方案"活而不乱"的底线。
2. **脱敏**：语音场景涉及更口语化的企业指代，转写文本仍走现有脱敏/匿名约束，不泄真实税号企业名。
3. **下钻边界**：`DRILLDOWN_OPS` 不因"灵活"而无限扩容；proactive 引导的追问必须是可执行的结构化芯片。
4. **组件化不过度**：章内子组件控制在"图/表/KPI/结论"四类，不无限细分（避免组合爆炸 + 报告失控）。
5. **不碰 text-to-SQL**：语义层让 LLM 映射到**受控指标对象**，绝不生成 SQL 查库（会编数，违反铁律）。
6. **双入口统一**：组件化 UI 与对话定制共用同一 `CustomReportSpec`，避免"向导一份、对话一份"漂移。

---

## 六、参考来源

**金融大模型 / 金融 Agent（论文 + GitHub）**
- [AI4Finance-Foundation/FinGPT](https://github.com/AI4Finance-Foundation/FinGPT) — 开源金融 LLM，data-centric 路线（本方案"知识注入"思想的来源）
- [Golden Touchstone（EMNLP 2025 Findings）](https://aclanthology.org/2025.findings-emnlp.1227/) — 中英双语金融 LLM 基准
- [agentic-fintech/Agentic_FinTech_Survey](https://github.com/agentic-fintech/Agentic_FinTech_Survey) — 金融 AI Agent 综述（tool use over 金融数据）
- [LLM Agents in Finance: A Survey（EMNLP 2025 Findings）](https://aclanthology.org/2025.findings-emnlp.972/) — 金融 Agent 功能分类（数据分析/投研/风控）
- [FinLLM/FinAgent 生命周期评测套件](https://ar5iv.labs.arxiv.org/html/2602.19073) — 金融 LLM 七类任务 + 治理
- [度小满"轩辕"金融大模型](https://developer.baidu.com/article/detail.html?id=5315277) — 国产金融大模型，成本优化 + 场景适配（可校准风险概率）
- [2025年中国金融智能体发展研究报告](https://m.jiemian.com/article/13761479.html)、[北京金融科技产业联盟 AI+Agent 金融应用报告](https://www.sohu.com/a/970732584_121752970)

**指标解读 / 语义层 / RAG / 报告生成**
- [智慧金融——2025 AI+风控新范式](https://blog.csdn.net/cdfunlove/article/details/160743088) — 知识图谱 + GraphRAG + 智能体 + 报告自动生成
- [Bayesian RAG（不确定性感知检索，降幻觉）](https://www.frontiersin.org/journals/artificial-intelligence/articles/10.3389/frai.2025.1668172/full)
- [MMRepAgent：多模态可解释报告生成框架](https://www.ebiotrade.com/NEWSF/2025-12/20251227000427159.htm) — 多工具组合出可解释报告（本方案"组件化"的参照）

**语音智能体**
- [FunAudioLLM：SenseVoice + CosyVoice + FunASR](https://github.com/QwenAudio) — 阿里语音栈（中文强）
- [pipecat-ai/pipecat](https://github.com/pipecat-ai/pipecat) — 实时语音 Agent 编排框架（500-800ms）
- [livekit/agents](https://github.com/livekit/agents) — WebRTC 原生语音 Agent 框架
- [k2-fsa/sherpa-onnx](https://github.com/k2-fsa/sherpa-onnx) — 端侧离线 ASR/TTS/VAD（Android/iOS/HarmonyOS/WebAssembly），"软硬结合"落点
- [yzfly/awesome-voice-agents](https://github.com/yzfly/awesome-voice-agents) — 语音 Agent 全景清单

---

## 七、落地答疑（成本 / 多轮上下文 / 路由架构 / 语音 App）

### 7.1 API 成本账（钱不是问题，时间是）
- 一个用户 turn ≈ 3~4 次 LLM 调用（dialog_act.classify → semantic_parser → llm_reply），约 3000~4000 token 输入 + 600~1000 token 输出。
- **单轮成本**：DeepSeek V4 Pro ≈ ¥0.03~0.1；GLM-5 / Qwen-Flash ≈ ¥0.01~0.03；Claude Opus / GPT-5.5 ≈ ¥0.3~1（没必要）。
- **毕设全程（含 5000 轮调试）**：DeepSeek ¥150~300；Flash ¥50~150。答辩演示可忽略。
- **两个坑**：① 理解层别用推理模型（DeepSeek V4 Pro 是 thinking 模型，首 token 600~900ms 会卡），换非推理模型（DeepSeek V3.2 / GLM-Flash / Qwen-Flash）；② 真正成本是调 prompt 的时间，不是 token。

### 7.2 灵活回答 / 中断续接 / 话题回溯（三个问题三种答案）
- **寒暄**（"早上好"→"早上好我是明鉴系统"）：是**表达层**问题，`dialog_act._GREETING_RE` 已能识别寒暄，改 `llm_reply` 文案即可，不是理解层问题。
- **中断续接**：是**会话持久化**问题，不是 LLM 问题。根因在 `ChatSessionRecord` 无 owner + `session_store` 30min TTL + 前端只存内存，按 `落地实施蓝图.md §2.0`（M0 账号记忆）做即可。
- **话题回溯**（按区域→评分→问企业→回到按区域）：唯一需要新设计的。把 `analysis_focus` 从"单一变量"升级为**焦点栈 `focus_history: list`**（每步压栈 `{scope, industry_l1, province, query_type, scenario, conclusion_id, 时间}`）；LLM 只负责"识别回到哪个话题"，代码负责取栈恢复状态。业界叫 data threads / 分层记忆。

### 7.3 意图识别：不是"全交 LLM"也不是"路由映射"，是"LLM 动态路由到受控工具集"
- 四层分工：**理解**（LLM function calling 选工具）→ **语义**（SemanticQuery 填槽）→ **执行**（引擎算数，铁律）→ **呈现**（查询类型→图表契约，非 LLM 决定）。
- 把 `metric_registry` 指标 + `DRILLDOWN_OPS` + 四场景打成 LLM 的 tool schema；路由从"正则硬编码"升级为"LLM 动态选受控工具"，不匹配问题消失。
- 业界依据："模型越自由错误越多，框住越稳"；动态函数路由实测错误率 22%→1.3%。
- **系统高度不取决于 LLM 有多自由，取决于工具集全不全 + 上下文记得住不住。**

### 7.4 语音识别如何操作 + 手机端打包
- **识别链路**：麦克风 → VAD → ASR → 走现有理解层 → TTS。ASR 只做"语音→文字"，不改变任何逻辑。
- **操作系统** = 把现有后端能力（定制报告 `generate_custom_report`、邮件全链路）包装成 tool，LLM 通过 function calling 依次触发。**这些后端能力已全部做完，语音只补"操作意图→API 调用"映射。**
- **手机端 App**：用 **Capacitor** 把现有 React 代码包成 iOS/Android（WebView，复用 100% 代码）+ `@capgo/capacitor-speech-recognition` 插件。**不要用 uni-app/React Native 重写。**
- **iOS 是语音痛点**（Safari 阻止 TTS 播报、麦克风受限）→ 必须原生 App；Android 浏览器够用。成本：Apple 开发者账号 $99/年。ASR 可选云端（SenseVoice/FunASR）或端侧（sherpa-onnx，离线隐私好）。

---

## 八、参考来源（补）

**成本 / 模型选型**
- [2026 国内大模型 API 终极对照表（20+ 厂商）](https://zhuanlan.zhihu.com/p/2081782887695890378)、[11 家主流大模型 API 价格优缺点](https://zhuanlan.zhihu.com/p/2081685506409210163)、[LLM API 成本计算器](https://openreplay.com/zh/tools/llm-cost-calculator/)

**路由架构（LLM 动态路由 / function calling）**
- [ReAct vs Tool Calling: Why Your LLM Should Decide — But Never Execute](https://dev.to/parth_sarthisharma_105e7/react-vs-tool-calling-why-your-llm-should-decide-but-never-execute-cp3)
- [Best Practices for Building an AI Agent Router（Arize）](https://arize.com/blog/best-practices-for-building-an-ai-agent-router/)
- [动态函数路由：22%→1.3% 稳定落地](https://blog.csdn.net/2301_76168381/article/details/155242064)、[Why Dynamic Function Routing Is the Key to Stable LLM Agents](https://www.besthub.dev/articles/why-dynamic-function-routing-is-the-key-to-stable-llm-agents-b0e2bca252f2)

**多轮上下文 / 记忆**
- [Agent 记忆模块最佳实践](https://cloud.tencent.cn/developer/article/2596153)、[LangGraph 记忆系统实战](https://cloud.tencent.cn/developer/article/2588385)、[ENGRAM: Lightweight Memory Orchestration](https://huggingface.co/papers/2511.12960)

**语音 App / 打包**
- [Capacitor：把 Web 应用打包成移动 App](https://peaklab.fr/glossaire/capacitor)、[@capgo/capacitor-speech-recognition（PTT/端侧识别）](https://cdn.jsdelivr.net/npm/@capgo/capacitor-speech-recognition@8.1.3/README.md)、[Exploring the Capacitor Speech Recognition API](https://capawesome.io/blog/exploring-the-capacitor-speech-recognition-api/)
- [k2-fsa/sherpa-onnx（端侧 ASR/TTS/VAD）](https://github.com/k2-fsa/sherpa-onnx)

---

*本文为路线规划，不替代实施。落地时按「短期2 → 短期1 → 长期」顺序推进，每步附验收样例 + 铁律套件回归。*
