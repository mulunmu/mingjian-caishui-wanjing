# Optional outer orchestration runtime. Keep LangGraph out of the core
# financial execution image until the canary and rollback gates pass.
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
psycopg[binary]==3.3.5
