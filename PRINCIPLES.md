# 准则（Principles）

明鉴（Míngjiàn）——复用型税务风险研判系统。本文件是产品与研发的**最高准则**，任何方向性决策与实现都以它为准。

## 铁律

1. **联网搜索铁律**：任何方向/实现，先联网搜索——看论文、看 GitHub，借鉴成熟经验并转化为系统养分。禁止闭门造车。
2. **计算定结论、模型定表达、溯源定可信**：数字只由算法产出，LLM 只组织语言、不改数字，每个结论可溯源。
3. **隐私红线**：公共源数据的企业名/税号不可逆脱敏（MD5），UI 不暴露真实身份；用户自有数据可选命名。
4. **报告是卖点、口径是地基**：没有统一口径，指标就不可信，报告就立不住。

## 产品定位

- 复用型数据分析系统：核心资产是数据库（数据 = 指标），最大卖点是报告系统。
- 不做：单一企业的简单查询。
- 做：两级分析——全部（聚合切片）+ 个体（深度研判报告）。
- 命名：用户可选提供企业名或匿名。

## 三大支柱

1. **指标语义层（口径）**：每个指标有规范定义（定义 / 公式 / 粒度 / 维度 / 默认过滤 / 边缘情况），口径可执行、一致。
2. **两级分析**：全部（行业 / 地区 / 时间 / 信号聚合）+ 个体（指标判断 + 报告）。
3. **LLM 三职**：识别（意图 / 字段）+ 总结（摘要 / 解读，锚定 computed 结论）+ 引导（追问 / 接入引导）。规则优先，LLM 兜底。

## 借鉴来源

- 指标语义层：dbt MetricFlow、LookML、Cube、Holistics
- LLM 数据智能体：OpenChatBI、kejishucloud/chatbi、天穹 ChatBI
- 字段映射：Granit.DataExchange 四层映射、Osmos AutoMap、Posit querychat、Couchbase NL2SQL++
