# 明鉴·财税票·万景（明鉴01）——项目全方位介绍

> **文档性质**：对当前工程真实能力与边界的全景说明（产品 / 技术 / 数据 / 成熟度）。  
> **写作原则**：以仓库代码与已验收行为为准，不夸大商用就绪度。  
> **对应工程根目录**：`2.0/`（Docker Compose 项目名 `20`）  
> **文档日期**：2026-08-31

---

## 1. 一句话定位

**明鉴是一套财税票风控「报告」产品**：用确定性算法对脱敏企业样本做群体切片与个体深挖，再以自然语言对话为入口、以可下载 PDF 为出口交付研判结论。

核心理念：**不卖系统，卖报告**。系统是生成工具；可解释、可溯源、可校验的风控报告才是产品。

---

## 2. 产品是什么 / 不是什么

### 2.1 是什么

| 维度 | 说明 |
|------|------|
| 品类 | B 端财税 / 经营风险**研判**（非通用 BI、非纯 ChatBI） |
| 交付物 | 场景化 PDF 报告 + 对话内结论（Claim + 图表） |
| 分析层级 | **两级**：① 聚合切片（行业 / 地区 / 信号 / 场景）；② 个体深度研判（匿名样本） |
| 交互 | 研判中心对话为主入口；报告中心向导 + 定制报告为交付；仪表盘 / 专项页为辅助 |
| 信任模型 | **计算定结论、模型定表达、溯源定可信** |

### 2.2 明确不做 / 未做满

- **不做**具名企业公开检索、个体关系图谱（隐私红线）。
- **法律维**当前仅覆盖**税务违法 / 稽查类信号**（`tax_illegal_only`），不含完整失信被执行、民商事诉讼全景。
- **不是**银行信贷正式审批系统：可做初筛 / 尽调辅助演示与内测，未具备合同级 SLA、人工复核岗与完整合规交付包。
- **数据规模**为演示级 live 样机（约 **193** 家匿名主体），不能等同全国行业覆盖。

---

## 3. 目标用户与使用场景

| 用户 | 典型用法 | 当前适配度 |
|------|----------|------------|
| 尽调 / 风控分析人员 | 综合尽调报告、行业切片、个体深度 PDF | 演示与内测可用 |
| 税务 / 合规相关岗位 | 税务合规体检、风险信号分布 | 口径可用，覆盖面受数据限制 |
| 发票风险排查 | 进销错配、红字、集中度、序列缺口 | 引擎已落地，信号需结合业务复核 |
| 产品演示 / 创新创业赛 | Docker 一键栈 + 抗幻觉叙事 | **较适配** |
| 银行正式采购上线 | 私有化、授权链、责任归属、SLA | **尚未具备商用证据** |

场景报告模板（代码 `report_templates.SCENARIOS`）：

| 场景键 | 报告标题 | 侧重 |
|--------|----------|------|
| `financial` | 财务健康体检报告 | 四能力、勾稽、同业、趋势 |
| `tax` | 税务合规体检报告 | 准时率、税负、违法信号 |
| `fraud` | 发票舞弊排查报告 | 进销错配 / 红冲 / 集中度 / 序列缺口 |
| `due_diligence` | 综合尽调报告 | 经营表现 + 真实性 + 舞弊 + 对标 |
| `profile` | 企业画像报告 | 规模 / 地区 / 行业 / 信用结构 |
| `overview` | 综合总览报告 | 六维总览 + 五场景摘要索引 |
| `custom` | 定制风控报告 | 对话式自由组合章节 |

另支持：**指定企业**个体深度报告、报告向导预校验（无样本 / 无可用章时拦截）。

---

## 4. 功能全景

### 4.1 研判中心（对话入口）

- 自然语言提问：趋势、评分、对比、排名、分布、分段、FAQ、口径方法论等。
- **SemanticQuery IR**：问法先落成结构化语义查询，再分派到确定性 claim builder。
- 多轮追问：会话继承行业 / 省份 / 查询类型（规则优先，LLM 兜底解析）。
- 结论呈现：文字 + 内联图表；带来源标签（规则 / LLM 润色）、样本量提示。
- 可引导生成报告、邮件发送报告（SMTP 可配置）。

### 4.2 六维研判引擎（数字唯一来源）

权重定义见 `assessment_weights.py`（合计 100%）：

| 维度 | 权重 | 含义（概要） |
|------|------|----------------|
| 税务健康 | 20% | 纳税准时、税负、欠税滞纳等 |
| 经营真实性 | 20% | 多口径营收交叉、Benford 等 |
| 发票健康 | 15% | 发票异常信号聚合 |
| 行业地位 | 15% | 同业对标与相对位置 |
| 法律合规 | 15%→**部分覆盖时降至 5%** | 目前主要为税务违法；降权避免「恒高分」虚抬总分 |
| 财务健康 | 15% | 盈利 / 偿债 / 营运 / 现金流等比率 |

引擎能力包括：综合评分与等级、趋势、真实性交叉、舞弊信号、行业基准、预警清单、切片归因（拖累因素按**主体去重**）。

### 4.3 报告中心（最大卖点）

- 场景模板组章 + 结论前置骨架（评级展望 / 优势风险二栏 / 财务维重排）。
- 排版：**WeasyPrint（主）**；失败路径有 FPDF，但**后置校验不可跳过**。
- 图表：matplotlib 嵌入（KPI、归因、雷达等）；雷达维度必须 ⊆ 正文已输出章节。
- 抗幻觉：Claim 锚定 + 预校验硬门禁 + HTML/上下文后置校验 + `*.validation.json` 留痕。
- 历史列表、中文下载名、向导确认步拦截。

### 4.4 个体画像

- 匿名企业详情：`display_label`（地区·行业·规模），不以真实企业名对外。
- 个体深度 PDF：权益为负等特殊会计走「计算失效 / 资不抵债」，禁止伪造成绩进优势栏。

### 4.5 数据接入

- Excel 上传与字段分层映射（精确 / 模糊 / LLM 语义辅助）。
- 指标语义层（`metric_registry`）统一口径；ETL：MySQL 源表 → PostgreSQL `core_metrics` + `engine_features` 预计算。

### 4.6 安全与运维（工程能力）

- Docker Compose：`db`（PostgreSQL）+ `redis` + `backend` + `frontend`；MySQL 仅 `etl` profile。
- 可选 / 默认开启方向：JWT 鉴权、CORS 白名单、Redis 限流与 LLM 日配额；配额耗尽可软降级规则路径。
- Health：DB / Redis / 编码与 engine 计数等自检。
- 测试：后端 pytest 覆盖对话、引擎、报告闸门、语义 IR、鉴权等（`backend/tests/` 下 60+ 测试文件）；另有 Playwright E2E、规范书审计脚本。

---

## 5. 核心技术概述（重点）

本章对应大赛与对外技术叙事的主干，描述的是**已实现机制**，而非愿景清单。

### 5.1 总体架构：五层 + 双轨

```
L0 数据层     MySQL 财税票明细（ETL）→ PostgreSQL（core_metrics / engine_features / 会话与报告元数据）
L1 指标语义层 metric_registry + financial_benchmarks + assessment_weights（单一事实源）
L2 语气/规则  业务语言铁律；指标波动 → 业务行为 → 涉税/经营风险（禁卡方/p值等统计词上用户面）
L3 结构层     场景模板 + 文档规划（claim / message 列表）
L4 版式层     HTML/PDF 只消费 plan，不发明结论

交互双轨：
  对话：Intent / SemanticQuery → claim builders →（可选）LLM 润色 → 前端图表
  报告：场景 chapters → 组装 context → 预校验 → 渲染 → 后置校验
```

**铁律**：数字只来自 L0/L1 确定性计算；大模型不得改数、不得独自下「一定虚开」类定性结论（使用「需核查」等方向词）。

### 5.2 抗幻觉管道（Claim 唯一化）

借鉴 NLG 经典管道（Reiter & Dale：内容决定 → 结构化 → 词汇化 → 表面实现）的工程落地：

1. **Content determination**：`judgment_service` 等规则引擎产出带条件的 Claim（含 value / trace / confidence）。
2. **Document plan**：场景章节顺序 + claim 列表；封面 story / 章 purpose 由 claim 条件拼装。
3. **Surface realization**：模板片段 + LLM 仅润色；`NarrationPlan` / `SummaryPlan` 等应用层 JSON 约束；发明数字或方向矛盾句由 sanitize 剥离。
4. **Guardrail**：`hallucination_guard`（章内完整性、跨面数字、禁词、雷达子集、时序词等）+ `report_preflight`（渲染前硬门禁）+ 后置 HTML/上下文校验。

**置信度策略（概念）**：

- `computed`：进正文；
- `inferred`：聚合报告禁止「据推断」类用户面表述；
- LLM 自由发挥数字：丢弃或剥离。

**诚实边界**：章节「数字是否全部锚定」等部分检查目前仍为 **soft_flags**（记入 validation、暂不一律拒 PDF），正逐步收紧。因此对外宜表述为「机制化抗幻觉 + 硬门禁覆盖关键一致性」，不宜宣称「幻觉已彻底消除」。

### 5.3 四层报告校验（生产闸门）

依据 `企划书/业务规则手册-报告校验.md`：

| 层 | 模块 | 职责 |
|----|------|------|
| L1 真相源 | 财务比率字典、`scope_contract`、手册 | 阈值与统计口径中心化 |
| L2 预校验 | `report_preflight.run_preflight` | 范围对齐、家数≤样本 N、雷达⊆章节、权益规则、小样本提示、空正文等 → **失败拒出 PDF** |
| L3 LLM | `llm_reply` | 只润色，不算数 |
| L4 后置 | `run_postflight_html` / `run_postflight_context` | 禁词与时序等兜底；**禁止 FPDF 跳过** |

典型已修机制（说明系统「为何可信」而非功能列表）：

- 切片筛选时禁止会话全集缓存污染子集 N；
- 拖累因素跨维同主体去重，家数不得超过样本；
- 负权益 ROE「计算失效」、负负债「账务异常」；
- 单位启发式修复（避免把比率 0.88 打成 88%）；
- N&lt;30 小样本强制「仅供参考」。

### 5.4 指标语义层

- `metric_registry`：指标定义、公式、粒度、维度；与 FAQ / 方法论问答同源。
- `financial_benchmarks`：财务比率阈值与特殊会计（权益失效等）唯一源。
- 借鉴思路：dbt MetricFlow / LookML「define once, use everywhere」。
- 营收偏差与多源交叉偏差分口径收编（如 `REVENUE_DEVIATION_WARN` vs `CROSS_*`），禁止模板硬编码判定阈值。

### 5.5 SemanticQuery 语义 IR

问法 → 结构化 IR（`QueryType`）：lookup / aggregation / comparison / trend / ranking / distribution / segmentation / correlation / faq / methodology。

- 规则优先校正与槽位继承；LLM 可选解析自然语言到 IR。
- 执行层按类型分派到各 `build_*_claims`，保证**对话数字与报告同源**。

### 5.6 确定性算法引擎

**真实性（`authenticity_engine`）**

- Benford 定律（χ² + MAD，参考开源实现思路）；
- 多口径交叉：增值税 ↔ 企业所得税 / 财报 ↔ 发票 ↔（可选）社保趋势，输出偏差与告警。

**反欺诈（`fraud_engine`）**

- 进销商品错配（以销项类目在进项中缺失占比为主信号，非单纯 Jaccard）；
- 红字发票异常；
- 开票集中度（Top 占比 / HHI）；
- 发票序列局部缺口（避免跨票本全局跳号误报）；
- 可选 pyod 异常分；结果写入 `engine_features` 预计算，运行期优先读预聚合。

**评分与归因（`assessment`）**

- 六维加权；法律部分覆盖时动态降权；
- 切片归因、预警、同业分位等。

### 5.7 隐私与脱敏

- 企业主键：`enterprise_id = MD5(taxpayer_id)`；
- 展示：`display_label`（地区 + 行业大类 + 规模），不含真实名称；
- API 层不返回 PII 列；源库仍需运维侧脱敏策略配合（工程审计中对「源库明文」有诚实记录）。

### 5.8 前端与部署技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React 18 + TypeScript + Vite + Zustand + Tailwind + ECharts（源码在仓库根 `src/`） |
| 后端 | FastAPI + SQLAlchemy 2.x async + PostgreSQL 16 |
| 缓存 / 限流 | Redis 7（可选；无则内存回退） |
| LLM | LiteLLM + DeepSeek 等（可选）；无 Key 时规则路径仍可研判与出模板报告 |
| 报告渲染 | WeasyPrint 主路径 + FPDF 降级（均过校验） |
| 部署 | Docker Compose；前端 `:3000`，API `:8000` |

主要前端页面：`ResearchCenter`（研判）、`ReportCenter`（报告）、`EnterprisePage`、`DataIngestPage`、`Overview` / `Authenticity` / `Fraud` 等专项、登录注册。

主要 API 域：`/api/v1/chat`、`risk`、`report`、`ingest`、`metrics`、`auth`、`subscription`。

---

## 6. 数据事实

| 项 | 现状 |
|----|------|
| 活库主体 | 约 **193** 家真实脱敏企业（ETL 入 PG） |
| 源数据 | 财税票融合库（发票、申报、财报、违法、社保等 `syx_*` 表） |
| 行业归并 | 多级行业映射为批发零售 / 制造 / 建筑 / IT软件 / 服务 / 其他等大类 |
| 关联键 | 源侧 `taxpayer_id`；应用侧哈希后的 `enterprise_id` |
| 运行期读库 | 应用以 PostgreSQL 为准；MySQL 用于 ETL / 部分溯源明细 |

---

## 7. 与「纯 LLM 报告」的差异（技术卖点压缩表述）

1. **架构隔离**：算分与结论在规则引擎；LLM 在表达层。  
2. **口径产品化**：指标字典 + 财务阈值字典，场景共用同一套数。  
3. **可机器验收**：预校验 / 后置校验 / validation 日志 / CI 契约测试，减少「靠人眼通读 PDF」的脆弱性。  
4. **两级分析同一底座**：群体切片与个体深挖共用指标与引擎，而非两套脚本。  
5. **业务语言约束**：用户面禁统计黑话；空数据弃权优先于编造。

---

## 8. 成熟度与诚实边界（务必写进对外材料）

### 8.1 已达到的水平

- 可 Docker 部署的完整前后端闭环；live 样机可出多场景 PDF。  
- 报告抗幻觉与一致性已从「调文案」升级为「管道 + 硬门禁」；多轮人工复审暴露的问题多数已机制化修复。  
- 适合：**创新创业赛演示、实验室 / 课程项目、机构内测、技术方案评审**。

### 8.2 尚未达到的水平（商用缺口摘要）

| 缺口 | 说明 |
|------|------|
| 付费验证 | 无稳定试点合同 / LOI / 付费使用证据 |
| 责任链 | 免责声明有；缺完整授权协议、人工复核岗、事故处置与签章流程 |
| 合同级质量承诺 | 部分数字锚定仍为 soft；不能写进「零幻觉」硬 SLA |
| 数据覆盖 | 193 家 ≠ 可售的全国 / 全行业基准产品 |
| 法律全景 | 税务违法为主，非完整司法尽调 |
| 交付体系 | 私有化实施、多租户、运维 SLA、培训支持尚未产品化 |

**结论**：产品原型与工程可信度偏强；**商业证据与交付责任偏弱**。对内测可称「可用」；对外收费上线仍属「未就绪」。

### 8.3 与旧文档的校正

- 评分维度以代码为准为 **六维**（部分旧介绍仍写五维）。  
- 前端默认端口以 Compose 为准为 **3000**（非 5173）。  
- 历史 `AUDIT_REPORT` 轮次分值反映当时审计切片；本文不以某一轮分数代替当前商用判断。

---

## 9. 快速体验

```bash
cd 2.0
docker compose up -d --build
```

| 入口 | 地址 |
|------|------|
| 前端 | http://localhost:3000 |
| API 文档 | http://localhost:8000/docs |
| 健康检查 | http://localhost:8000/api/v1/health |

配置 `LLM_API_KEY` 后可体验语义解析与润色；未配置时规则研判与模板报告仍可用。  
**注意**：修改后端代码后须 `docker compose up -d --build backend`（镜像默认不挂载源码）。

生产向环境变量（摘要）：`AUTH_REQUIRED`、强随机 `JWT_SECRET`、收紧 `CORS_ORIGINS`、替换默认库口令与演示账号口令。

---

## 10. 关键文档索引

| 文档 | 用途 |
|------|------|
| `PRINCIPLES.md` | 产品最高准则 |
| `HANDOFF-CURSOR.md` | 报告机制北星与接手约束（最贴近工程现状） |
| `企划书/业务规则手册-报告校验.md` | 报告校验唯一业务依据 |
| `企划书/报告设计规范v1.md` | 版式与用语规范 |
| `项目3.0企划书.md` | 早期分阶段蓝图（部分步骤已完成，以代码为准） |
| `AUDIT_REPORT.md` | 多轮工程审计与 remediation 轨迹 |
| `README.md` | 启动与栈说明 |
| `jiegou.md` | 结构说明 |

---

## 11. 总结

明鉴 2.0 已经把「财税票数据 → 统一指标 → 确定性六维引擎 → Claim 规划 → 受约束表达 → 可机器拦截的 PDF 报告」打通，并在约 193 家 live 脱敏样本上形成可演示、可回归的闭环。

它的核心竞争力不在「又接了一个大模型」，而在于：**把大模型关在表达层，把信任建立在语义口径、确定性计算与校验闸门上**。

同时必须诚实承认：当前更接近**高质量原型 / 内测级报告工厂**，距离银行级商用交付，差在付费验证、合规责任链、合同级质量承诺与数据覆盖，而不是再多改两版 PDF 文案。

---

*本文档由工程现状整理生成，若与代码冲突，以代码与 `HANDOFF-CURSOR.md` / 业务规则手册为准。*
