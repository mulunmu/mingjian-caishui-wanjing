# 明鉴 · 红线要求全篇审计报告（第二次）

> 依据：`红线要求.md` v1.0（2026-09-14）  
> 审计范围：当前工作区代码（对话主路径 + 报告路径 + 能力地图 + 测试）  
> 审计日期：2026-09-15  
> 轮次：第 2 次（对照《红线要求》原文重新核验，不以第一次报告为真源）  
> 判定标准：`PASS` / `PARTIAL` / `FAIL`

---

## 总评

**整体判定：仍未达标（PARTIAL，关键架构与铁律项仍为 FAIL）**

相较上一轮，**P0 对话终态闸门、理解层 PERSONA、焦点回溯接线**已落地，属真实进度。但红线核心——**工具调用计划替代封闭枚举、三层模型正确顺序、禁正则补丁、§9 十条自动化验收**——仍未完成。

| 章节 | 第一次 | 第二次 | 变化 |
|------|--------|--------|------|
| §1 数字铁律 | FAIL | **PARTIAL** | 对话终态闸门已加；仍有绕过面 |
| §2 表达自由 / 禁补丁 | FAIL | **FAIL** | 无实质收敛 |
| §3 弃权三态 | PARTIAL | **PARTIAL** | 骨架同；编造拒绝仍缺 |
| §4 统一人格 | PARTIAL | **PARTIAL↑** | 理解层 + 金融层已注入 |
| §5 焦点栈 | PARTIAL | **PARTIAL↑** | 已接线；语义/持久化仍错 |
| §6 三层模型 | FAIL | **FAIL** | 顺序仍错（DS→通义→闸门） |
| §7 能力地图 | PARTIAL | **PARTIAL** | 表在，工具未接线 |
| §8 实施红线 | PARTIAL | **PARTIAL** | 闸门改善；模板/硬编码仍在 |
| §9 十条验收 | 多数 FAIL | **FAIL** | 仍几乎无自动化 |

一句话：**护栏加厚了，主干路由没换。**

---

## 本轮已确认的改进（相对代码现状）

1. **对话终态 `hallucination_guard`**（`chat_router.py` ~2074–2098）：金融解读拼接后对 `reply` 做句级过滤；对含数字的 `followups` 做过滤。
2. **理解层注入 PERSONA**（`dialog_act._build_system`）；金融解读层也调用 `build_persona_prompt`。
3. **焦点回溯调用点**（`chat_router.py` ~767–776）：匹配「回到/刚才那批/之前的…」时调用 `resolve_focus_from_history`。

以上三项是可观测的代码变更，不是文档自评。

---

## §1 铁律

### 1.1 数字铁律 — **PARTIAL**（↑ 自 FAIL）

| 检查项 | 判定 | 证据 |
|--------|------|------|
| 对话终态硬闸门 | **PARTIAL** | 主分析路径有 P0 闸门；仅在 `if claims` 且 `if _allowed` 时改写；**drilldown 早退路径仍无闸门** |
| 金融解读纳入终态闸门 | **PASS** | 先拼接 `【金融解读】`，再对整段 `reply` 过滤 |
| `report_hint` 过闸 | **FAIL** | 对 hint 做了 `filter_unanchored_sentences` 但**只打日志，不回写/不剥离**；`generate_claim_reply` 内 hint 仍未 `_sanitize` |
| followups 过闸 | **PARTIAL** | 过滤后 ~2103–2105 若 bundle 有 followups 会**再次覆盖**，可能抵消数字过滤 |
| Claim 仅来自 `judgment_service` | **FAIL** | `inventory_scope` / `followup_items` / `faq_kb` / `slice_report` / `chat_router` 仍直接造 Claim |
| 逐字锚定 | **FAIL** | `_normalize_num` 归一化（非逐字）；中文数词不可见；违规句丢弃非打回重写 |
| 无 Claim 的 `generate_reply` | **FAIL** | 自由文本返回，无 guard |

### 1.2 事实封闭 — **FAIL**（不变）

- `to_tool_schema()` 仍无调用方；`DialogAct.tools` 路由仍不消费。
- 能力仍由 `ActName` 7 类 + `intent_engine` 关键词驱动。

### 1.3 可视化 — **PARTIAL**（不变）

- LLM 不选图：**PASS**
- `SHAPE_TO_CHART` / `infer_chart`：API **PASS**；`judgment_service` 仍约 **12× `"type": "bar"`**（合计 ~16 处 type 硬编码）：**FAIL**

---

## §2 表达自由与禁补丁 — **FAIL**（不变）

| 检查项 | 判定 | 现状 |
|--------|------|------|
| `_normalize_act` 正则 | **FAIL** | 仍约 **12** 处 `re.search`；**LLM 成功后仍调用** |
| `_soft_fallback` 正则 | **FAIL** | 仍约 **30** 处 |
| 关键词仅兜底 | **FAIL** | `_FUNC_PATTERNS` / `_DIM_PATTERNS` 仍是结构主路径 |
| 封闭 `ActName` → 工具计划 | **FAIL** | 仍 7 类 Literal；`tools` 空转 |
| `NarrationPlan` 不限句数句式 | **FAIL** | `max_length=5/6`；prompt「1–2 句」「恰好 3 句」仍在 |
| 散落 chart type → shape 推断 | **PARTIAL** | 同 §1.3 |

---

## §3 弃权机制 — **PARTIAL**（不变）

| 检查项 | 判定 | 说明 |
|--------|------|------|
| Schema 强制 `can_answer` / `clarify_question` | **PASS** | 字段 + instructor |
| 路由三态分支 | **PASS** | clarify / abstain 早退存在 |
| 「编一个营收」拒绝 | **FAIL** | 无编造检测；软路径→泛化 clarify，非「数字只来自数据」拒绝 |
| 超纲弃权（天气） | **PARTIAL** | 依赖 LLM 填 `clarify_question=null`；软路径几乎总是 clarify |
| 每轮必落三态 | **PARTIAL** | 低置信强制 clarify，abstain 难触发 |

---

## §4 统一人格 — **PARTIAL↑**

| 检查项 | 判定 | 说明 |
|--------|------|------|
| `PERSONA` 定义 | **PASS** | 与红线一致 |
| 四场景叠加 | **PASS** | `TONE_VARIANTS` |
| 理解层注入 | **PASS↑** | `dialog_act._build_system` 已注入 |
| 对话叙述层注入 | **PASS** | `generate_claim_reply`（需传入 persona；主路由有传） |
| 金融解读层注入 | **PARTIAL↑** | 已注入，但固定 `build_persona_prompt(None)`，场景语气丢失 |
| 报告叙述 / 摘要 / 语义解析 / 定制报告 / `generate_reply` | **FAIL** | 仍未统一注入；报告侧仍用 `TONE_PROFILES` 双轨 |

红线「所有 LLM 调用注入同一份 PERSONA」：**未整体达标**，主对话理解+叙述已覆盖。

---

## §5 记忆（焦点栈）— **PARTIAL↑**（验收仍 FAIL）

| 检查项 | 判定 | 说明 |
|--------|------|------|
| 结构 kind + ts | **PASS** | `merge_analysis_focus` 入栈 |
| 回溯已接线 | **PASS↑** | `chat_router` 检测「回到…」并调用 resolver |
| 回溯语义正确 | **FAIL** | `resolve_focus_from_history` 返回**同 kind 最近一条**（通常即当前焦点），未跳过当前/弹出上一层；enterprise/scenario 从不入栈 |
| 用正则触发回溯 | **PARTIAL** | 触发本身又是正则补丁，与 §2.3 精神冲突；应用理解层工具/意图表达 |
| PG 往返 | **FAIL** | 模型有 `focus_history_json`；读回未可靠灌入 `dialogue_state`；`建表.sql` 未见列 |
| 可测试可回放 | **FAIL** | 仍无 focus 栈专项测试 |

---

## §6 三层模型分工 — **FAIL**（不变）

### 红线顺序

```
理解(DeepSeek+工具计划) → 引擎 Claims → 通义解读 → DeepSeek 校验润色 → hallucination_guard → 回复
```

### 当前主路径

```
DeepSeek DialogAct(封闭枚举 + _normalize_act)
  → 引擎 Claims
  → DeepSeek generate_claim_reply（叙述）
  → [可选] 通义【金融解读】追加
  → P0 filter_unanchored_sentences
  → 返回
```

| 层 | 判定 |
|----|------|
| 理解 = 工具计划 | **FAIL** |
| 数字中枢 | **PARTIAL** |
| 通义在叙述之前、且禁止新数字 | **FAIL**（仍在叙述之后；自由文本无 instructor） |
| DeepSeek 校验在通义之后 | **FAIL**（无独立校验层） |
| 硬闸门永不跳过 | **PARTIAL**（主路径有；drilldown/无 Claim/空 allowed 可跳） |

---

## §7 三张能力地图 — **PARTIAL**（不变）

| 表 | 判定 |
|----|------|
| Metric Tools（注册 + shape + 路由消费） | **PARTIAL**（shape 有；`to_tool_schema` 未接线） |
| Chapter Tools / `CHAPTER_REGISTRY` | **PASS**（结构与消费）；工具化 **未做** |
| Shape → Chart 为唯一出图路径 | **PARTIAL**（API 有；judgment 仍写 type） |

---

## §8 实施红线

### 8.1 禁止

| 禁止 | 判定 |
|------|------|
| `dialog_act` 正则补丁 | **FAIL** |
| LLM 决定图表类型 | **PASS** |
| 跳过 `hallucination_guard` | **PARTIAL↑**（主路径改善；非全覆盖） |
| LangChain / Mem0 等 | **PASS** |
| Prompt 写死回复模板 | **FAIL**（「恰好 3 句」等） |
| 硬编码行业/地区列表 | **FAIL**（`intent_engine._INDUSTRY_KW` / `_PROVINCE_KW`） |

### 8.2 必须

| 必须 | 判定 |
|------|------|
| instructor + Pydantic | **PARTIAL**（通义仍自由文本） |
| 铁律回归（含对话） | **FAIL**（对话 P0 无专项 pytest） |
| 三表同步且进路由 | **PARTIAL** |
| 指标带 shape | **PASS** |
| 弃权三态强制输出 | **PASS**（schema）；行为覆盖 **PARTIAL** |

---

## §9 验收标准对照（第二次）

| # | 验收用例 | 判定 | 说明 |
|---|----------|------|------|
| 1 | 「编一个营收」拒绝 | **FAIL** | 无拒绝路径 / 无测 |
| 2 | 口语理解 | **FAIL** | 无路由级自动化 |
| 3 | 含糊澄清 | **FAIL** | 无目标话术测 |
| 4 | 话题回溯 | **FAIL** | 接线了但语义错 + 无测 |
| 5 | 多意图两工具 | **FAIL** | 拆句后常澄清二选一；`tools` 未执行 |
| 6 | 天气弃权 | **FAIL** | 软路径偏 clarify；边界脚本甚至期望 `no_abstain` |
| 7 | 10 轮人格一致 | **FAIL** | 无测；报告层未统一 PERSONA |
| 8 | 新指标 shape 自动出图 | **FAIL** | judgment 仍手写 type；无验收测 |
| 9 | 口语 20 条 ≥90% 且无正则 | **FAIL** | 正则仍深度参与 |
| 10 | 全量 hallucination_guard | **PARTIAL** | 报告测强；**对话 P0 无回归测** |

**§9 合计：0/10 达到「实现正确 + 自动化通过」；1/10 代码侧有进展（#10 主路径闸门）。**

---

## 风险优先级（第二次修订）

### P0 — 仍阻塞「铁律」

1. **纠正流水线顺序**：Claims → 通义（instructor 约束、禁数字）→ DeepSeek 校验润色 → 终态闸门。
2. **闸门无死角**：drilldown / 无 Claim / `report_hint` 回写 / bundle followups 覆盖后的二次过闸；空 `_allowed` 时的策略写清。
3. **补对话路径 hallucination_guard 回归测试**（至少：植入非法数字 → 被剥离）。

### P1 — 架构根源（否则永远 PARTIAL）

4. 路由消费 `to_tool_schema()` / `act.tools`；逐步废止 `ActName` 主路径。
5. 停止 LLM 后 `_normalize_act`；设正则删除里程碑。
6. `judgment_service` 只出 `shape`，统一 `infer_chart`。

### P2 — 弃权 / 人格 / 记忆

7. 编造请求专用 abstain/拒绝；超纲软路径真正 abstain。
8. 报告叙述/摘要/定制/语义解析统一 PERSONA；金融层带 scenario。
9. 焦点栈：跳过当前取上一层；enterprise 入栈；PG 往返修好；去正则触发（改理解层）。

### P3 — §9 十条自动化进 CI

10. 每条至少 1 个 pytest 或固定 HTTP smoke。

---

## 结论

第二次审计结论：**有局部止血，无架构达标。**

- 可记为进度：对话终态闸门、理解层 PERSONA、焦点回溯调用点。
- 不可记为达标：工具计划、三层顺序、禁补丁、编造拒绝、焦点语义、§9 验收。

在 P1（工具路由替代枚举+正则）完成前，系统仍处于红线文档所禁止的「补丁叠加」形态；P0 闸门只能降低漏数字风险，不能替代事实封闭与表达自由的正确实现。

---

*审计口径：以 `红线要求.md` 原文为准，对当前仓库实现与测试覆盖独立核验。*
