# 邮箱验证码与重置密码 — 交付文档

2026-09-13

## 一、交付内容

1. 注册页增加邮箱验证码校验
2. 发验证码前增加防机器校验
3. 登录页增加「忘记密码」入口，新增重置密码页

## 二、改动清单

### 新增文件

| 文件 | 说明 |
|---|---|
| `backend/app/models/verification.py` | 两张表：`email_verification_codes`、`password_reset_tickets` |
| `backend/app/services/verification_service.py` | 验证码签发/校验、表单令牌、重置票据 |
| `backend/tests/test_auth_email_code.py` | 28 个单测 |
| `src/pages/ForgotPasswordPage.tsx` | 重置密码页 |

### 修改文件

| 文件 | 改动 |
|---|---|
| `backend/app/api/v1/auth.py` | 3 个新端点；注册接口增加验证码字段 |
| `backend/app/api/deps.py` | 鉴权时校验 token 的 pwd_ver |
| `backend/app/services/auth_service.py` | 新增 `update_password` / `get_user_pwd_ver` / `verify_token_checked`；token 带 pwd_ver |
| `backend/app/services/email_service.py` | 新增 `send_verification_code` |
| `backend/app/models/engine_store.py` | `AppUser` 增加 `pwd_ver` 列 |
| `backend/app/models/__init__.py` | 注册两个新模型 |
| `backend/app/main.py` | 启动时补 `pwd_ver` 列 |
| `docker-compose.yml` | 注入 `EMAIL_*`、`REQUIRE_EMAIL_VERIFICATION`、`VERIFY_*` |
| `.env.example`、`backend/.env.example` | 同上 |
| `src/pages/RegisterPage.tsx` | 验证码输入行 + 倒计时 |
| `src/pages/LoginPage.tsx` | 「忘记了密码？重置密码」入口 |
| `src/App.tsx` | 新增 `/forgot-password` 路由 |
| `src/types/auth.ts`、`src/api/auth.ts`、`src/stores/authStore.ts` | 类型与接口封装 |

数据库变更由启动时 `create_all` + `ADD COLUMN IF NOT EXISTS` 自动完成，**无需手动执行迁移**。

## 三、接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/auth/form-token` | 取表单令牌，打开表单时调用 |
| POST | `/api/v1/auth/send-code` | 发验证码，`purpose` 为 `register` / `login` / `reset` |
| POST | `/api/v1/auth/register` | 注册，新增可选 `code` 字段 |
| POST | `/api/v1/auth/verify-reset-code` | 校验重置验证码，换取 `reset_token` |
| POST | `/api/v1/auth/reset-password` | 凭 `reset_token` 重置密码 |

## 四、流程

### 注册

```
打开注册页 → 取 form-token（静默）
填邮箱 → 点「获取验证码」→ 服务端校验令牌 + 冷却 + 配额 → 发信
回填验证码 + 密码 → 提交 → 服务端验码 → 建账号
```

### 重置密码

```
打开重置页 → 取 form-token
填邮箱 → 点「获取验证码」→ 收信
回填验证码 + 新密码 + 确认密码 → 提交
  → 校验验证码 → 换取一次性 reset_token
  → 凭 reset_token 改密码，账号 pwd_ver +1
  → 跳转登录页
```

## 五、配置

`.env` 必填：

```
REQUIRE_EMAIL_VERIFICATION=true
EMAIL_SENDER=你的邮箱@qq.com
EMAIL_PASSWORD=SMTP授权码
EMAIL_SMTP_HOST=smtp.qq.com
EMAIL_SMTP_PORT=465
```

`EMAIL_PASSWORD` 是**授权码**，不是邮箱登录密码。

可选参数（均有默认值）：

| 变量 | 默认 | 说明 |
|---|---|---|
| `VERIFY_CODE_TTL` | 300 | 验证码有效期（秒） |
| `VERIFY_RESEND_COOLDOWN` | 60 | 重发冷却（秒） |
| `VERIFY_MAX_ATTEMPTS` | 5 | 单码最大试错次数 |
| `VERIFY_DAILY_LIMIT_PER_EMAIL` | 10 | 每邮箱每日上限 |
| `VERIFY_DAILY_LIMIT_PER_IP` | 30 | 每 IP 每日上限 |
| `FORM_TOKEN_MIN_AGE_SECONDS` | 2 | 表单停留最少秒数，设 0 关闭 |
| `RESET_TICKET_TTL_SECONDS` | 600 | 重置票据有效期（秒） |

## 六、注意事项

1. **开关与邮件配置必须成对**。`REQUIRE_EMAIL_VERIFICATION=true` 但未配 `EMAIL_SENDER`/`EMAIL_PASSWORD` 时，发码返回 503，注册不可用。

2. **改后端代码必须重建镜像**。容器默认不挂载源码：
   ```
   docker compose up -d --build backend frontend
   ```

3. **前端密码校验是 6 位，后端要求 8 位**。这是改动前就存在的不一致，本次未处理。7 位密码会被后端拒绝。

4. **`admin@example.com` 每次后端启动都会重建**。只要 `.env` 里 `DEMO_USER_EMAIL` 有值且 `DEMO_USER_PASSWORD` ≥ 8 位，`ensure_demo_user()` 就会创建它。不需要演示账号时改这两项。

5. **重置密码会使该账号所有已登录会话失效**。这是 `pwd_ver` 机制的作用。

## 七、验证记录

| 项目 | 结果 |
|---|---|
| 后端 `pytest` | 8 失败 / 478 通过 / 4 跳过 |
| 前端 `vitest` | 6 通过 |
| 前端 `tsc -b` | 仅 `ReportCenter.tsx(148,9)` 一条错误 |

**上述 8 个失败、1 条 TS 错误均为改动前既有问题**（切片报告默认场景值变更未同步测试），与本次功能无关。

真机验证通过项：

- 注册：发码 200 / 冷却 429 / 无码 400 / 错误码 400 / 正确码 200 / 重放 400
- 防机器：无令牌 403 / 伪造令牌 403 / 立即发码 403 / 等待 3 秒 200
- 重置：无票据 400 / 伪造票据 400 / 登录 token 冒充 400 / 票据二次使用 400 / 旧密码失效 401 / 新密码可登录 200 / 改密后旧 token 立即 401
- 验证码与令牌均不写入日志

## 八、未实现

- 验证码登录（后端 `purpose=login` 分支已就绪，无对应登录端点）
- 重置密码成功后的页面提示（项目无 toast 组件，当前直接跳转登录页）
