# 明鉴 · 财税票 · 万景 —— 项目全方位调研

> 调研日期：2026-09-14  
> 范围：仓库 `risk-assessment/2.0` 全栈（前端 / 后端 / 数据 / 部署 / 报告 / 对话）  
> 目的：摸清技术栈、系统结构、结构性问题与改进优先级，供后续决策与落地参照  
> 相关文档：`PRINCIPLES.md`、`系统诊断与改进方向.md`、`报告结构改进方案.md`、`落地实施蓝图.md`

---

## 〇、一句话总览

**明鉴是「财税票数据 → 确定性风控引擎 → 场景化 PDF 报告」的产品**；对话是入口与导航，报告是卖点。  
技术上已具备 **Claim 溯源 + 抗幻觉 + DialogAct 对话契约 + 决策备忘录雏形**；结构上仍被 **三大上帝模块、报告叙事复读、Docker/迁移/仓库卫生** 拖累。

| 维度 | 现状快照 |
|------|----------|
| 产品形态 | 聊天研判 + 报告中心为主；态势 / 个体 / 舞弊 / 真实性为辅 |
| 样本 | 演示库约 **193** 家脱敏主体（ETL 自 MySQL → PostgreSQL） |
| 铁律 | 数字只由算法；LLM 只分类 / 叙述 / 引导 |
| 后端体量 | `services/` ≈ 52 模块；`slice_report` 3465 行 / `judgment` 2153 / `chat_router` 2017 |
| 前端 | React 18 + Vite + Zustand + Tailwind + ECharts |
| 测试 | `backend/tests` ≈ 78 个 `test_*.py`；前端测试偏少 |
| 部署 | Docker Compose：Postgres + Redis + backend + frontend（MySQL 仅 ETL profile） |

---

## 一、产品与定位

| 项 | 内容 |
|----|------|
| 品类 | 财税票 **风控报告产品**（非通用 BI / 非纯 ChatBI） |
| 数据 | 税务 + 发票 + 财务三域（税局口径，脱敏） |
| 加工 | 原始数据 → 口径指标 → 引擎评级 / 信号 → 报告与对话 |
| 场景 | 放贷 / 评级 / 预警 / 稽查（四模板）+ 定制 + 个体深度报告 |
| 两级分析 | **聚合切片**（全库 / 行业 / 地区）+ **个体** |
| 准则 | 见 `PRINCIPLES.md`：计算定结论、模型定表达、溯源定可信 |

核心矛盾（仍成立）：

1. **可信 vs 易懂** —— 抗幻觉做对了；表达曾被锁死，近期在「决策备忘录 / 白话」上补，但报告结构仍松散复读。  
2. **功能全 vs 主线清** —— 对话与报告双主线已收敛，但遗留页与上帝模块仍分散注意力。  
3. **引擎硬 vs 叙事软** —— 数字硬；章节骨架（论断 / 所以呢 / 大图）偏软、偏碎。

---

## 二、技术栈全景

### 2.1 前端

| 层 | 技术 | 位置 |
|----|------|------|
| 框架 | React 18.3 + TypeScript 5.5 | `package.json`, `src/` |
| 构建 | Vite 5.4，开发端口 3000 | `vite.config.ts` |
| 路由 | react-router-dom v6 | `src/App.tsx` |
| 状态 | Zustand 4 | `src/stores/*` |
| HTTP | Axios，`baseURL: /api/v1`，代理到 `:8000` | `src/api/client.ts` |
| 图表 | ECharts 5 + echarts-for-react | `src/components/charts/` |
| 样式 | Tailwind 3.4 + CSS 变量（藏青 / 古铜 / 暖象牙） | `tailwind.config.ts`, `globals.css` |
| 动效 / 图标 | Framer Motion、Lucide | 各页面 |
| 测试 | Vitest + jsdom（覆盖面窄） | — |

**未深度使用：** `react-markdown` 在依赖中但几乎无引用；无 React Query。

### 2.2 后端

| 层 | 技术 | 位置 |
|----|------|------|
| API | FastAPI + Uvicorn | `backend/app/main.py` |
| ORM | SQLAlchemy 2.x（async + sync） | `app/db/` |
| 主库 | PostgreSQL 16（asyncpg / psycopg2） | compose `db` |
| 源库 ETL | MySQL 8（pymysql，profile=`etl`） | `app/etl/` |
| 缓存 / 限流 | Redis 7（可降级内存） | `cache_service`, `rate_limiter` |
| LLM | LiteLLM + instructor + DeepSeek 默认 | `llm_reply`, `dialog_act`, … |
| PDF | 主路径 WeasyPrint + Jinja2；降级 fpdf2 | `report_html`, `slice_report` |
| 认证 | JWT（jose）+ bcrypt | `auth_service`, `api/deps` |
| 分析 | numpy / scipy / matplotlib / pyod | 舞弊等引擎 |
| 依赖管理 | 仅 `requirements.txt`（无 pyproject） | `backend/requirements.txt` |
| 运行镜像 | `python:3.12-slim` | `backend/Dockerfile` |

### 2.3 基础设施

```
docker-compose.yml
  mysql (profile etl) → 一次性灌数
  db (Postgres) + redis
  backend :8000  （代码 COPY 进镜像，默认无源码 volume）
  frontend :3000
```

- DB / Redis 端口默认绑 `127.0.0.1`。  
- 后端改代码需 **`docker compose up -d --build backend`**，本地热更不便。  
- 报告 PDF 落在容器内 `backend/reports/`（无挂载则宿主机不可见）。  
- 迁移：Alembic 名存实亡；启动时 `create_all` + 零散 `ALTER TABLE`。

---

## 三、仓库与模块结构

### 3.1 顶层

```
2.0/
├── src/                      # 前端
├── backend/                  # 后端
├── docker-compose.yml
├── package.json / Dockerfile*
├── 企划书/、*.md             # 产品与规范文档较多
├── 报告结构改进方案.md       # 报告叙事结构（待落地）
└── （大量临时 PDF / 预览 / _tmp_* 脚本，见卫生问题）
```

### 3.2 后端 `app/`

| 目录 | 职责 |
|------|------|
| `api/v1/` | auth / chat / risk / report / email / ingest / metrics / subscription |
| `services/` | 引擎、对话、报告、LLM、会话（核心业务） |
| `models/` | SQLAlchemy：core_metrics、profiles、financials、engine_store… |
| `schemas/` | claim、semantic_query、custom_report |
| `db/` | PG 会话、URL、MySQL、编码自检 |
| `etl/` | MySQL → PG 管道 |
| `templates/` | `slice_report.html` / `enterprise_report.html` / 封面 SVG / tokens |
| `tests/` | 契约、护栏、对话、报告结构等 |
| `scripts/` | smoke、边界套件、PDF 抽取、合同轮换 |

**关键服务角色：**

| 模块 | 角色 | 体量风险 |
|------|------|----------|
| `chat_router.py` | 对话总控 | ~2017 行，上帝模块 |
| `judgment_service.py` | Claim 建造与语义执行 | ~2153 行 |
| `slice_report.py` | 切片 / 定制 / 个体 PDF 主链路 | ~3465 行，最大上帝模块 |
| `assessment.py` | 六维评分 / 仪表盘 | 核心计算 |
| `dialog_act.py` | DialogAct 分类 | 对话控制面 |
| `intent_engine.py` + `semantic_query.py` | 意图 / 语义 IR（双轨并存） | 入口重叠 |
| `decision_memo.py` | 决策摘要 / Action Title 共享层 | 报告叙事 |
| `fraud_engine` / `authenticity_engine` / `insight_engine` | 专项引擎 | — |
| `hallucination_guard.py` | 报告数字锚定 | 抗幻觉 |
| `llm_reply.py` | 仅组织语言 | 铁律落点 |
| `scope_state` / `followup_items` / `inventory_scope` | 范围与追问契约 | 近期强化 |

### 3.3 前端 `src/`

| 目录 | 职责 |
|------|------|
| `pages/` | ResearchCenter（对话）、ReportCenter、Overview、Enterprise、Login… |
| `components/chat/` | ChatPanel、FollowUpButtons、EvidenceDrawer… |
| `components/report/` | ReportWizard、邮件弹窗 |
| `components/charts/` | ECharts 封装 + `chartTheme` |
| `stores/` | auth / chat / overview / report |
| `api/` | 薄适配层（归一化后端形状） |
| `types/` | chat / risk / report / overview… |

**主路由：** `/` `/research`（研判）· `/report`（出报告）· 用户菜单进态势 / 个体 / 账户等。

### 3.4 数据流（心智模型）

```
MySQL(ETL) → PostgreSQL(core_metrics…)
                ↑
Browser → Vite:/api → FastAPI
            ├─ chat_router → DialogAct / SemanticQuery → judgment Claims → llm_reply
            ├─ risk/* → assessment / engines
            └─ report/* → slice_report → decision_memo → Jinja → WeasyPrint/FPDF
```

---

## 四、主链路调研

### 4.1 对话

1. `POST /api/v1/chat` → `route_chat`  
2. 会话与 `dialogue_state`（unbound / individual / cohort）  
3. 结构化追问优先（bootstrap / drilldown / dialog_act / action…）  
4. `dialog_act.classify` → 协商范围 / 绑定主体 / 分析 / 下钻 / FAQ / 寒暄 / 定制报告  
5. 分析路径：意图 + SemanticQuery → `judgment_service` 出 Claim  
6. `llm_reply` 只改写表达；`followup_items` 出结构化芯片  
7. 定制报告走对话状态机，最终仍进 `slice_report.generate_custom_report`

**近期已修（契约向）：** unbound 聚合默认 cohort、假 drill 禁止、多意图拆分、寒暄 meta、`analysis_focus`、行业趋势 vs 地区对比纠偏、100+100 轮换套件曾过 200/200。

### 4.2 风险 API

- `/risk/summary`、企业列表 / 档案、舞弊与真实性概览  
- 与对话共用引擎与 Claim，避免两套算分

### 4.3 报告

| 类型 | 入口 | 渲染 |
|------|------|------|
| 切片四场景 | `/report/generate`、`/report/slice` | `slice_report.html` |
| 定制 | 对话为主 + validate | 同切片管线（文件名 `slice_*_custom_*`） |
| 个体 | `/report/enterprise` | `enterprise_report.html` |

管线：范围取数 → Claim / 章节 → **decision_memo** →（可选）受限 LLM 叙述 → 图 → HTML → PDF → `.meta.json` / `.context.json`。

---

## 五、架构优点（应保留）

1. **计算与语言分离** —— Claim 分级（computed / inferred / asserted）+ 过滤 asserted；LLM 模块明确「不产新数」。  
2. **报告抗幻觉纵深** —— `hallucination_guard`、词表 / 雷达校验、脏 PDF 拒出。  
3. **DialogAct 控制面** —— 范围协商与分析分流；库存数字不由 LLM 发明。  
4. **SemanticQuery IR** —— NL → 结构化查询的中间层，利于校正与测试。  
5. **降级路径完整** —— 无 LLM / 无 Redis / WeasyPrint 失败均可回退。  
6. **鉴权默认偏严** —— Compose 侧 `AUTH_REQUIRED`、JWT；生产 lifespan 校验。  
7. **决策备忘录共享层** —— 切片 / 定制 / 个体共用 governing question 与 BLUF 思路（方向对，结构待收紧）。  
8. **测试面较宽** —— 护栏、对话契约、报告结构、语义查询等有专项用例。  
9. **前端适配层清晰** —— API 模块归一化后端差异；主航「研判 + 出报告」产品脊梁清楚。

---

## 六、结构性问题清单

### 6.1 代码结构（技术债）

| ID | 问题 | 表现 | 影响 |
|----|------|------|------|
| C1 | **三大上帝模块** | `slice_report` / `judgment_service` / `chat_router` 各 2k～3.4k 行 | 难测、难改、回归成本高 |
| C2 | **对话 ↔ 报告紧耦合** | chat 直接调 `generate_custom_report` / validate | 边界模糊，API 与对话双入口难统一 |
| C3 | **意图双轨** | `intent_engine` + DialogAct + SemanticQuery + LLM parser | 行为难推理；偶发 FAQ 抢答 / 趋势误判（已部分修） |
| C4 | **迁移缺失** | 无可用 Alembic 版本；启动 ALTER | 环境漂移、上线不可复现 |
| C5 | **配置散落** | 无集中 Settings；`.env.example` 不全 | 部署踩坑、文档与 compose 不一致 |
| C6 | **双 PDF 栈** | WeasyPrint 主路径 + 大段 FPDF 遗留 | 维护双份版式逻辑 |
| C7 | **Docker 无源码挂载** | backend `COPY` only | 每次改代码重建镜像；PDF 难落宿主机 |
| C8 | **仓库卫生差** | `_tmp_*`、`_rpt_preview`、根目录散落 PDF、Windows 路径双记 | 噪音大、误提交风险 |
| C9 | **嵌套垃圾目录** | 如 `backend/backend/` 迹象 | 易混淆入口 |
| C10 | **前端追问双形态** | `followups: string[]` + `followupItems` | 易与后端契约漂移 |
| C11 | **设计令牌双轨** | CSS brand 变量 vs Tailwind warm/shadcn 残留 | 视觉不一致 |
| C12 | **遗留页面** | fraud/authenticity 等与聊天主线重叠 | 维护面大 |

### 6.2 产品 / 交互结构

| ID | 问题 | 说明 |
|----|------|------|
| P1 | 报告是卖点，首屏仍偏聊天 | 主航已是双入口，但「一键出报告」心智仍弱 |
| P2 | 可信锁死表达 | 铁律正确，但叙述曾过度模板化；密段化尚未落地 |
| P3 | 对话上下文偶发断裂 | 焦点行业 / 继续分析；近期用 `analysis_focus` 等修补，需持续契约套件 |
| P4 | 定制报告命名泄漏 | 文件仍 `slice_*_custom_*` | 列表与清理语义不清 |

### 6.3 报告叙事结构（当前最尖锐）

> 详见 `报告结构改进方案.md`。定性：**都是结构问题**。

| ID | 问题 | 样例表现 |
|----|------|----------|
| R1 | **同一结论多层复读** | KPI → 可照做/支撑要点 → 章节长标题 →「论断」→「所以呢」 |
| R2 | **松散小标题** | 「论断 / 所以呢 / 可照做」像填空题，不专业 |
| R3 | **图文顺序颠倒** | 雷达过大在前，处置文字过小在后 → 头重脚轻 |
| R4 | **空表壳** | 关键数字表数值空、只剩说明摘要长句 |
| R5 | **口水处置** | 「建议对照关键数字核对『结论：…』」无执行信息 |
| R6 | **（原主题：…）** | 占结构位、无决策价值 |

**目标骨架（已定口径、待改代码）：**

- 摘要：**判断 → 依据（正文可见）→ 优先处置**  
- 章节：**短论断标题 + 一段密段（判断+依据+处置）+ 限高图**  
- 取消独立「论断 / 所以呢」；个体 / 切片 / 定制 / 全库同一骨架  

### 6.4 测试与质量结构

| ID | 问题 |
|----|------|
| T1 | 对话 / 护栏 / 报告结构单测强；**真 LLM + HTTP 全链路 E2E** 弱 |
| T2 | ETL / Redis 多进程限流 / 迁移几乎无测 |
| T3 | 上帝模块内部路径多靠重型集成，单测粒度粗 |
| T4 | 前端几乎无页面 / 契约测试 |
| T5 | 契约轮换套件曾因「池子去重死循环」卡住 —— 套件本身也需结构健壮 |

### 6.5 文档结构

- 根目录与 `企划书/` 下 **MD 很多**（介绍、蓝图、审计、规范、诊断…），价值高但**入口分散、有交叉过时**。  
- README 仍写「五维」等旧表述，与现行「六维 / 四场景报告」不完全一致。  
- 需要一份「权威索引」；本文可充当 **2026-09 技术现状锚点**。

---

## 七、技术栈匹配度评估

| 选型 | 评价 |
|------|------|
| FastAPI + SQLAlchemy async | 适合 API + 异步 IO；与 Claim / 报告同步算力混用时需 `sync_runner` 线程池，合理 |
| Postgres 主存 + MySQL ETL | 职责清晰；ETL profile 分离正确 |
| Redis 可选 | 本地友好；生产应强制，避免多实例限流分裂 |
| LiteLLM | 换模型成本低；需守住「不产数」 |
| WeasyPrint | HTML 报告可控；依赖系统库，镜像需字体 / Pango |
| React + Zustand | 体量匹配；无需上 Redux |
| ECharts | 仪表盘足够；报告内嵌图走 matplotlib/静态图，两套管线需统一观感 |
| 无 Metric 语义服务独立进程 | 口径在 `metric_registry` 内聚，短期够用；长期可抽「指标语义层」服务以匹配 PRINCIPLES |

**不建议短期内**：换框架、上微服务拆库、为 ChatBI 重写引擎。应优先 **拆上帝模块 + 收紧报告结构 + 迁移 / Docker 卫生**。

---

## 八、改进优先级建议

### P0（结构正确性，建议立刻做）

1. **报告叙事结构改造**（按 `报告结构改进方案.md`）—— 去复读、密段、先文后图。  
2. **契约套件常驻 CI** —— `run_dialog_contract_rotate` 新 seed；防回归。  
3. **仓库卫生** —— `.gitignore` 收紧临时 PDF / `_tmp_*` / `_rpt_preview`；清理双路径噪音。

### P1（可维护性）

4. **拆分上帝模块**（先切开文件边界，行为不变）  
   - `slice_report` → context / render / store / enterprise / custom  
   - `chat_router` → scope / analyze / custom_report / followup  
   - `judgment_service` → builders / semantic_exec  
5. **Alembic 真正跑起来** —— 停用启动期随意 ALTER。  
6. **Settings 集中化** + 补全 `.env.example`。  
7. **前端 FollowUp 单一形态** —— 废弃纯 string 芯片主路径。

### P2（体验与工程）

8. Docker dev：**backend 源码或 reports volume**（或明确文档「必 rebuild」）。  
9. 报告图表紧凑档统一；长标签横条。  
10. 前端补关键路径 Vitest；后端补 chat→PDF 一条金样例。  
11. 文档索引页：标注哪些 MD 仍权威、哪些归档。

---

## 九、风险与约束（改动时勿破）

1. **数字铁律** —— 任何「好懂」改造不得让 LLM 发明家数 / 分位 / 比率。  
2. **脱敏** —— 公共源主体不可逆；报告 / UI 不泄真实税号企业名。  
3. **下钻边界** —— `DRILLDOWN_OPS` 不可为消音假 drill 而无限扩容。  
4. **订阅鉴权** —— 报告生成仍须服务端 plan 校验，前端门禁只是体验。  
5. **场景分轨** —— 贷 / 评 / 预 / 稽只换措辞，不复制四套松散模板结构。

---

## 十、调研结论

| 判断 | 说明 |
|------|------|
| 技术栈选型 | **整体合理**，与产品阶段匹配；短板在工程卫生与模块边界，不在「用错框架」 |
| 架构方向 | **Claim + DialogAct + 报告护栏** 是正确护城河，应加固而非推倒 |
| 最大结构债 | ① 报告叙事复读骨架 ② 三大上帝模块 ③ 迁移 / Docker / 仓库噪音 |
| 最近进展 | 对话契约根因修复 + 100+100 轮换通过；决策备忘录已接入但 **PDF 结构仍不合格** |
| 下一步 | 以 `报告结构改进方案.md` 为规格落地模板与 `decision_memo`，再并行拆分上帝模块 |

---

## 附录 A — 关键路径速查

| 用途 | 路径 |
|------|------|
| 应用入口 | `backend/app/main.py` |
| 对话路由 | `backend/app/services/chat_router.py` |
| DialogAct | `backend/app/services/dialog_act.py` |
| Claim 执行 | `backend/app/services/judgment_service.py` |
| 报告总成 | `backend/app/services/slice_report.py` |
| 决策备忘录 | `backend/app/services/decision_memo.py` |
| 切片模板 | `backend/app/templates/slice_report.html` |
| 个体模板 | `backend/app/templates/enterprise_report.html` |
| 品牌令牌 | `backend/app/templates/_brand_tokens.css` |
| 前端聊天态 | `src/stores/chatStore.ts` |
| 前端报告 | `src/pages/ReportCenter.tsx`, `src/components/report/ReportWizard.tsx` |
| Compose | `docker-compose.yml` |
| 铁律 | `PRINCIPLES.md` |

## 附录 B — 与既有文档关系

| 文档 | 关系 |
|------|------|
| `PRINCIPLES.md` | 最高准则；本调研服从铁律 |
| `系统诊断与改进方向.md` | 产品困境论述；本调研补充 2026-09 技术现状与体量数据 |
| `报告结构改进方案.md` | 报告叙事结构专项规格（待落地） |
| `落地实施蓝图.md` / `交互与报告重构方案.md` | 历史规划；以本调研的 P0/P1 为当前收敛 |
| `AUDIT_REPORT.md` / `INTEGRATION_REPORT.md` | 历史审计；细节可能过时，方法可参考 |

---

*本文为调研快照，不替代实施 PR。确认优先级后应按专项方案改代码并附样例 PDF / 契约套件结果验收。*
