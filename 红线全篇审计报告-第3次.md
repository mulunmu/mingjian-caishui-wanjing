# 明鉴 · 红线要求全篇审计报告（第三次）

> 依据：`红线要求.md` v1.0（2026-09-14）  
> 审计范围：当前工作区代码（对话主路径 + 报告路径 + 能力地图 + 测试）  
> 审计日期：2026-09-15  
> 轮次：第 3 次（对照《红线要求》原文重新核验）  
> 判定标准：`PASS` / `PARTIAL` / `FAIL`

---

## 总评

**整体判定：仍未达标。** 相对第二次，**闸门死角、编造/超纲行为、对话 guard 单测**有实质进展；**三层顺序、工具路由、禁补丁、焦点语义、§9 自动化**仍未过线。

| 章节 | 第1次 | 第2次 | 第3次 | 变化 |
|------|-------|-------|-------|------|
| §1 数字铁律 | FAIL | PARTIAL | **PARTIAL↑** | hint 回写、二次 followups、drilldown 闸门、单元测 |
| §2 禁补丁 | FAIL | FAIL | **FAIL↓** | 新增编造/超纲正则，补丁面扩大 |
| §3 弃权 | PARTIAL | PARTIAL | **PARTIAL↑** | 编造拒绝 + 天气弃权已落地（正则实现） |
| §4 人格 | PARTIAL | PARTIAL↑ | **PARTIAL** | 无新增覆盖面 |
| §5 焦点栈 | PARTIAL | PARTIAL↑ | **PARTIAL** | 语义/PG/测试未修 |
| §6 三层模型 | FAIL | FAIL | **FAIL** | 顺序仍为 DS→通义→闸门 |
| §7 能力地图 | PARTIAL | PARTIAL | **PARTIAL** | tools 仍空转 |
| §8 实施红线 | PARTIAL | PARTIAL | **PARTIAL** | 闸门↑；模板/硬编码/正则仍 FAIL |
| §9 十条验收 | FAIL | FAIL | **FAIL↑** | #10 有单元测；#1/#6 有实现无测 |

一句话：**行为护栏更厚，架构路径未换；§3 用更多正则换来行为达标，与 §2.3 直接冲突。**

---

## 本轮已确认改进（相对第二次）

1. **`report_hint` 过闸并回写**（`chat_router.py` ~2097–2103），不再只打日志。  
2. **bundle followups 覆盖后二次过滤**（~2118–2125）。  
3. **drilldown 路径同样跑 reply 闸门**（~999–1009）。  
4. **编造请求早拒**：`_FABRICATE_RE` → `can_answer=False` +「数字只来自系统数据…」（`dialog_act.py` ~205–212）。  
5. **超纲弃权**：`_OUT_OF_DOMAIN_RE`（含天气）→ `clarify_question=None` → abstain（~214–221）。  
6. **新增** `backend/tests/test_dialog_hallucination_guard.py`（对话数字剥离单元测）。

---

## §1 铁律 — **PARTIAL↑**

| 检查项 | 判定 | 证据 |
|--------|------|------|
| 主路径终态闸门 | **PASS↑** | 有 claims/allowed 时过滤 reply；hint 回写；followups 二次过滤 |
| drilldown 闸门 | **PARTIAL↑** | reply 过闸；followups 未滤；`if _da` 空 allowed 仍跳过 |
| 空 claims / 空 allowed | **FAIL** | `if _allowed:` 整段跳过，无数字 Claim 时 LLM 数字可漏网 |
| `generate_claim_reply` 内 hint sanitize | **FAIL** | hint 仍只 `translate_terms`，靠路由后置补救 |
| Claim 仅 `judgment_service` | **FAIL** | inventory/followup/faq/slice/chat_router 仍造 Claim |
| 逐字锚定 | **FAIL** | `_normalize_num` 归一化；丢句非打回重写 |
| `generate_reply` 无 Claim | **FAIL** | 自由文本无 guard |

---

## §2 表达自由与禁补丁 — **FAIL↓**

| 检查项 | 判定 | 现状 |
|--------|------|------|
| LLM 后 `_normalize_act` | **FAIL** | 仍调用；约 12 处 `re.search` |
| `_soft_fallback` | **FAIL** | 约 30 处 |
| 新增正则 | **FAIL↓** | `_FABRICATE_RE` / `_OUT_OF_DOMAIN_RE` 扩大补丁面 |
| 封闭 `ActName` | **FAIL** | 仍 7 类；`tools` 路由零消费 |
| `to_tool_schema()` | **FAIL** | 仍无调用方 |
| 句数/句式锁 | **FAIL** | `NarrationPlan max_length=5`；「恰好 3 句」「1–2 句」 |
| chart `type` 硬编码 | **FAIL** | `judgment_service` 仍约 12× `"type":"bar"` |

> 红线原文：「如果 LLM 分类错了……改地图和 prompt，**不写正则**。」本轮用正则实现 §3 验收，是**以违反 §2.3 换取 §3 行为**。

---

## §3 弃权机制 — **PARTIAL↑**

| 检查项 | 判定 | 说明 |
|--------|------|------|
| Schema 强制三态字段 | **PASS** | `can_answer` / `clarify_question` |
| 路由 clarify / abstain | **PASS** | 早退分支存在 |
| 「编一个营收」拒绝 | **PASS↑** | 早拒 + 说明数字来源（正则实现） |
| 「今天天气」弃权 | **PASS↑** | OOD → abstain（正则实现） |
| 含糊澄清 | **PARTIAL** | 低置信强制 clarify；目标话术无测 |
| 与 §2.3 一致性 | **冲突** | 行为对、手段错 |

---

## §4 统一人格 — **PARTIAL**（无实质进展）

| 层 | 判定 |
|----|------|
| `PERSONA` 定义 / 场景叠加 | **PASS** |
| 理解层 / 对话叙述层 | **PASS** |
| 金融解读 | **PARTIAL**（注入但 `PERSONA(None)`，丢场景） |
| 报告叙述/摘要、定制报告、`generate_reply`、语义解析、字段映射 | **FAIL**（未注入） |

---

## §5 焦点栈 — **PARTIAL**（验收仍 FAIL）

| 检查项 | 判定 | 说明 |
|--------|------|------|
| 结构 + 入栈 | **PASS** | industry/province + ts |
| 路由「回到…」接线 | **PASS** | 有调用 |
| 跳过当前取上一层 | **FAIL** | `resolve_focus_from_history` 返回同 kind **最近一条**（当前 tip） |
| enterprise/scenario 入栈 | **FAIL** | 从未 push |
| PG 往返 | **FAIL** | 读回未灌入 `dialogue_state`；建表未见列 |
| 专项测试 | **FAIL** | `test_scope_state` 仍无 focus 用例 |
| 触发方式 | **PARTIAL** | 仍用正则触发回溯 |

---

## §6 三层模型 — **FAIL**（不变）

### 红线

```
理解(工具计划) → 引擎 Claims → 通义解读 → DeepSeek 校验 → hallucination_guard → 回复
```

### 实际

```
DeepSeek DialogAct(+_normalize_act)
  → 引擎 Claims
  → DeepSeek generate_claim_reply
  → [可选] 通义【金融解读】追加
  → P0 filter
  → 返回
```

| 层 | 判定 |
|----|------|
| 理解 = 工具计划 | **FAIL** |
| 通义在校验之前、禁止新数字 | **FAIL**（在叙述后；自由文本；局部剥数字） |
| DeepSeek 独立校验层 | **FAIL**（无） |
| 硬闸门 | **PARTIAL**（主路径有；空 allowed / 无 Claim 路径可跳） |

---

## §7 能力地图 — **PARTIAL**（不变）

| 表 | 判定 |
|----|------|
| Metric Tools | **PARTIAL**（shape 有；schema 死代码） |
| Chapter Registry | **PASS**（表与别名）；工具化 **未做** |
| Shape → Chart | **PARTIAL**（`infer_chart` 仅 followups；judgment 仍写 type） |

---

## §8 实施红线

### 禁止

| 项 | 判定 |
|----|------|
| `dialog_act` 正则补丁 | **FAIL↓**（存量 + 新增编造/超纲） |
| LLM 选图 | **PASS** |
| 跳过 hallucination_guard | **PARTIAL↑** |
| LangChain / Mem0 | **PASS** |
| Prompt 写死模板 | **FAIL** |
| 硬编码行业/地区 | **FAIL** |

### 必须

| 项 | 判定 |
|----|------|
| instructor + Pydantic | **PARTIAL**（通义仍自由文本） |
| 铁律回归含对话 | **PARTIAL↑**（有单元测，无 router E2E） |
| 三表进路由 | **PARTIAL** |
| 指标带 shape | **PASS** |
| 弃权 schema 强制 | **PASS** |

---

## §9 验收标准对照（第三次）

| # | 验收 | 实现 | 自动化 | 判定 |
|---|------|------|--------|------|
| 1 | 编造拒绝 | **有**（正则） | 无 | **PARTIAL↑** |
| 2 | 口语理解 | 依赖分类 | 无目标测 | **FAIL** |
| 3 | 含糊澄清 | 低置信 clarify | 仅乱码 soft | **FAIL** |
| 4 | 话题回溯 | 接线错语义 | 无 | **FAIL** |
| 5 | 多意图两工具 | 拆句后常二选一；tools 不执行 | 仅 split 单测 | **FAIL** |
| 6 | 天气弃权 | **有**（正则） | 无 | **PARTIAL↑** |
| 7 | 10 轮人格 | 未全层注入 | 无 | **FAIL** |
| 8 | shape 自动出图 | 主路径仍手写 type | 无验收测 | **FAIL** |
| 9 | ≥90% 且无正则 | 正则更深 | 无 | **FAIL** |
| 10 | hallucination_guard | 主路径+单元测 | 单元有、E2E 无 | **PARTIAL↑** |

**严格「实现正确 + 自动化通过」：仍约 0/10；宽松「行为基本对」：#1/#6/#10 有代码进展。**

---

## 三轮趋势

```
闸门覆盖面  ████░░░░ → ██████░░ → ███████░   ↑ 止血有效
弃权行为    ██░░░░░░ → ███░░░░░ → ██████░░   ↑ 但靠正则
人格注入    ████░░░░ → █████░░░ → █████░░░   → 停滞
焦点栈      ███░░░░░ → ████░░░░ → ████░░░░   → 停滞
工具路由    █░░░░░░░ → █░░░░░░░ → █░░░░░░░   → 零进展
禁补丁      ██░░░░░░ → ██░░░░░░ → █░░░░░░░   ↓ 退步
三层顺序    █░░░░░░░ → █░░░░░░░ → █░░░░░░░   → 零进展
§9 自动化   ░░░░░░░░ → ░░░░░░░░ → ██░░░░░░   ↑ 仅 #10 单元
```

---

## 风险优先级（第三次修订）

### 立刻（否则永远「护栏假达标」）

1. **纠正流水线**：Claims → 通义（instructor、禁数字、带 scenario PERSONA）→ DeepSeek 校验润色 → 终态闸门。  
2. **空 `_allowed` 策略**：无数字 Claim 时禁止 LLM 输出数字，或强制走模板；不得整段跳过闸门。  
3. **§3 去正则化**：把编造/超纲迁入 schema 枚举或工具策略（如 `refuse_fabrication` / `abstain_ood`），删除 `_FABRICATE_RE` / `_OUT_OF_DOMAIN_RE`。

### 架构（红线根因）

4. 路由消费 `to_tool_schema` / `act.tools`；废止 `ActName` 主路径与 LLM 后 `_normalize_act`。  
5. `judgment_service` 只出 `shape`，统一 `infer_chart`。  
6. 焦点：跳过 tip 取上一层；enterprise 入栈；PG 往返；专项测；去掉正则触发。

### 验收

7. #1/#4/#6/#10 至少各 1 个 router 级 pytest；#9 禁止再增正则。

---

## 结论

第三次审计结论：**止血继续有效，架构债与补丁债同步加深。**

- 可记进度：闸门回写/二次过滤/drilldown、编造拒绝、天气弃权、对话 guard 单测。  
- 不可记达标：三层顺序、工具计划、禁补丁、焦点语义、§9 自动化闭环。  
- 特别警示：用新增正则兑现 §3，是在**用红线禁止的手段完成红线要求的行为**——短期验收会好看，长期与 §2.3 / §9#9 直接对撞。

下一步若继续改，应优先「流水线顺序 + 空 allowed 策略 + 编造/超纲 schema 化」，而不是再叠正则。

---

*审计口径：以 `红线要求.md` 原文为准，对当前仓库实现与测试独立核验。*
