# 项目 3.0 企划书 —— 风控报告产品

> 本文档是给 Cursor 的**逐阶段搭建指令**。按阶段从上到下执行，每阶段有「目标 / 涉及文件 / 具体步骤 / 验收标准」。
> 蓝图依据：`jiegou.md`（技术架构）+ `EXECUTION_CHECKLIST.md`（改造清单）+ 报告产出规格（真实性/可追溯性）。
> 项目根目录：`D:\projects\risk-assessment`

---

## 一、产品定位（一句话）

**风控报告产品**：依托大模型，根据客户提供的企业财税数据，自动化生成不同场景下的风控报告。系统只是生成报告的工具，报告才是产品。

### 两大产品形态

| 形态 | 输入 | 输出 | 场景 |
|---|---|---|---|
| ① 通识型综合风控报告 | 既定财税/发票/工商数据 | 无需交互，直接出完整报告 | 银行信贷初筛、授信辅助 |
| ② 场景化定制风控报告 | 相同数据 + 指定场景 | 聚焦特定维度的专项报告 | 欺诈识别、行业风向、尽调、基本面 |

### 大赛核心技术亮点（必须体现）

1. **报告描述真实性（抗幻觉）**：结论由算法算、LLM 只组织语言，杜绝 LLM 编造结论。
2. **数据指标可追溯性**：报告里每个数字都能点回源表字段、再点回明细行。

---

## 二、数据事实（务必先对齐，避免返工）

**数据位置：** `D:\projects\risk-assessment\data\`

| 文件 | 大小 | 说明 |
|---|---|---|
| `建表.sql` | 134KB | 全部表结构（源 MySQL 5.7，库名 `bill_tax_fusion_dwd_standrad`） |
| `数据.sql` | **1.95GB** | `REPLACE INTO` 格式数据，13 组核心表（`syx_` 前缀） |
| `财税票数据字典-20250417.xlsx` | 827KB | 字段字典（财税票三域字段解释） |

**关键表结构（真实表名，从 `建表.sql` 提取）：**

| 表 | 用途 | 对应功能 |
|---|---|---|
| `syx_enterprise_info` | 企业基本信息 | 基础画像 |
| `syx_credit_level` | 纳税信用等级 | ① 评分 |
| `syx_tax_illega` | 税务违法记录 | ① 评分（法律维度） |
| `syx_invoice` / `syx_invoice_details` | 发票主表 + 商品明细 | ② 真实性 / ③ 反欺诈（进销错配） |
| `syx_red_invoices_info` | 红字发票 | ③ 反欺诈 |
| `syx_corporate_income_season` / `_year` | 企业所得税季/年报 | ② 三口径交叉 |
| `syx_tax_finance_balance_year` / `profit_year` | 资产负债表 / 利润表 | ② 真实性 / ④ 基准 |
| `syx_cash_flow` | 现金流 | ② 真实性 |
| `syx_social_declaration` | 社保申报 | ② 真实性 |
| `syx_tax_interaction` | 银税互动（贷款） | ① 融资健康子指标 |
| `syx_investor_info` | 投资人（股东） | ⚠️ 明文，需脱敏 |

**关联主键：`taxpayer_id`（纳税人识别号/企业税号）**，跨表对齐全靠它。

**脱敏红线：** `taxpayer_name`（纳税人名称）、`taxpayer_id` 明文出现在源数据里，**必须 MD5 哈希后再入 `core_metrics`**。股东姓名、身份证、电话、地址、税局分局仍是明文，UI 层禁止任何定位到单一个体的入口。

---

## 三、总体架构（改造后）

```
MySQL 原始数据(数据.sql)  →  ETL 聚合  →  PostgreSQL
                                          ├─ core_metrics（一企业一行，五维指标）
                                          ├─ industry_benchmark（行业基准）
                                          └─ conclusion_store（研判结论+证据链）
                                                  ↓
                        引擎层：评分 / 真实性 / 反欺诈 / 基准 / 趋势
                                                  ↓
                        研判中心（唯一前端入口）→ 组合报告（证据链完整）
```

---

## 四、分阶段构建路线（给 Cursor 的逐步指令）

---

### 阶段 0 · 环境与数据准备

**目标：** 数据能入库，开源库就位。

**步骤：**

1. **开源库落位**：把桌面 6 个 ZIP 解压到 `D:\projects\risk-assessment\opensource\`（与 `FinRobot-master` 同级），目录名去掉 `-main`/`-master` 后缀：
   - `pyod`（异常检测）
   - `instructor`（结构化输出）
   - `outlines`（约束解码）
   - `selfcheckgpt`（幻觉自检）
   - `search_benford_law_compatibility`（Benford 定律）
   - `acc_fraud_detections`（Benford + 发票序列号错配）

2. **Python 依赖**（国内镜像，pip 不在 PATH 用 `python -m pip`）：
   ```bash
   python -m pip install -i https://pypi.tuna.tsinghua.edu.cn/simple \
       pyod instructor numpy scipy scikit-learn pandas
   ```
   > ⚠️ Python 3.14 可能缺 `numba`/`outlines` 的 wheel，建议后端统一用 **Python 3.11/3.12**。

3. **数据库决策（关键）**：`数据.sql` 是原生 MySQL 5.7 语法。**推荐方案**：docker 起一个 MySQL 实例 `mysql < 数据.sql` 存原始明细（供溯源下钻），PostgreSQL 存聚合后的 `core_metrics`（供应用查询），中间用 Python ETL 聚合。**备选**：单库 PG，用脚本把 `REPLACE INTO` 转成 PG 语法再导入。

**验收：**
- `opensource/` 下 6 个库目录存在。
- `python -c "import pyod, instructor"` 不报错。
- MySQL 里 `syx_invoice_details` 有数据（原始明细可查）。

---

### 阶段 1 · 删除冗余 + 简化 UI

**目标：** 从「查企业系统」瘦身为「研判中心」。

**步骤（先删后改）：**

1. **后端删除**（对照 `EXECUTION_CHECKLIST.md` 任务四）：
   - 删 `backend/app/api/v1/enterprise.py`、`network.py`、`graph.py`
   - 删 `backend/app/services/graph_service.py`、`legal_service.py`
   - 删 `backend/app/data/invoice_edges.json`、`companies_registry.json`
   - `backend/app/main.py`：去掉对应 `include_router`

2. **前端删除**：
   - 删 `frontend/src/pages/EnterpriseSearch.tsx`、`EnterpriseDetail.tsx`、`NetworkGraph.tsx`、`Reports.tsx`
   - 删 `frontend/src/components/EnterpriseRadarChart.tsx`、`EnterpriseDimensionCard.tsx`、`EnterpriseSkeleton.tsx`、`SearchBox.tsx`、`WarningSignalBadge.tsx`、`legacy/LegacyRedirects.tsx`
   - 删 `frontend/src/lib/pinyin.ts`、`mockEnterprises.ts`、`canvasTheme.ts`
   - `frontend/src/App.tsx`、`lib/solarModules.ts`、`lib/routes.ts`、`lib/api.ts` 清掉对应引用

3. **简化 UI**：只留 `Login/Register` + `Dashboard`（简化为入口）+ `ChatPanel`（升级为「研判中心」唯一业务页）。

4. **全量回归**：`cd backend && python -m pytest` + `cd frontend && npm run build`，清理残留 import。

**验收：**
- 前后端 build/test 全绿，无引用已删文件的 import。
- 首屏无企业搜索框、无网络图、无企业列表。

---

### 阶段 2 · 数据 ETL + 重写模型

**目标：** `core_metrics` 宽表落库，`/health` 变 live。

**步骤：**

1. **重写模型** `backend/app/models/core_metrics.py`：新 `CoreMetrics` 字段对齐五维指标，**删掉**上市公司字段（`market_cap`/`pe_ratio`/`roe`/`z_score`/`debt_ratio`/`enterprise_name` 明文），改为：
   - `enterprise_id` = MD5(`taxpayer_id`)（主键）
   - `display_label` = 地区 + 行业大类 + 规模（**不含股东姓名**）
   - 五维字段：信用等级/准时率/欠税/违法/发票/财报偏差/社保/现金流等

2. **新建 ETL** `backend/app/etl/pipeline.py`：
   - 读 MySQL 原始表 → 聚合到 PG `core_metrics`
   - 行业 107 → 6 大类：批发零售/制造/建筑/IT软件/服务/其他
   - 保留 `syx_invoice_details` 明细供溯源

3. **新建基准表** `industry_benchmark`（分行业大类均值/分位，可重算）。

4. **改 `assessment.py`**：数据源从旧宽表换成新税务表字段，算分框架（`_calc_*`/`_percentile`/`_risk_level`）保留。

**验收：**
- `GET /api/v1/health` 返回 `data_mode: live`、`enterprise_count == 200`。
- 前端 MockDataBanner 不显示。

---

### 阶段 3 · 算法接入（反欺诈 + 真实性）

**目标：** 确定性算法产出结论，供报告引用。

**步骤：**

1. **新建 `backend/app/services/fraud_engine.py`**（反欺诈）：
   - 用 `pyod`（IsolationForest/LOF）对发票做异常打分
   - 参考 `opensource/acc_fraud_detections/sequence_number_mismatches.py` 做**发票序列号错配**检测
   - 实现三个信号：进销商品错配（scbm）、红字发票异常、开票集中度

2. **新建 `backend/app/services/authenticity_engine.py`**（真实性）：
   - 参考 `opensource/search_benford_law_compatibility` 做 **Benford 定律**检测财报数字分布
   - 实现三口径交叉：增值税 ↔ 企业所得税 ↔ 财报 ↔ 发票 ↔ 社保，算偏差率

3. **评分/基准/趋势**：复用 `assessment.py` + 新建 `industry_benchmark` 聚合逻辑。

**验收：**
- 对一条假发票样本能输出「进销错配」异常分。
- 对一组财报数字能输出 Benford 违例结论。

---

### 阶段 4 · 研判原子 + AI 问答（真实性/可追溯性落地）

**目标：** 这是大赛技术亮点，**报告 claim 结构化 + 三级溯源**。

**步骤：**

1. **定义报告 claim schema**（每个结论句的强制结构）：
   ```json
   {
     "claim": "建材批发行业逾期率最高，达 26.6%",
     "value": { "metric": "逾期率", "number": 26.6, "unit": "%" },
     "trace": { "table": "core_metrics", "field": "tax_on_time_rate",
                "detail_table": "syx_invoice", "query_id": "Q_industry_overdue" },
     "confidence": "computed"   // computed | inferred | asserted
   }
   ```
   - `computed` → 进正文；`inferred` → 进正文但带「据推断」；`asserted`（LLM 自由发挥）→ **丢弃**。

2. **重写 `intent_engine.py`**：意图从「企业名解析」改为「功能 × 维度切片」（维度：整体/行业/地区/时间/信号）。沿用现有「规则优先 + LLM 兜底 + TEST_CASES」框架，但规则集全部重写。

3. **改造 `llm_reply.py`**：用 `instructor`（或 `outlines`）强制 LLM 输出上述 claim schema，**只组织语言、不产生新数字**。

4. **新建 `conclusion_store`**：存「结论 + 证据链 + 溯源 + 追问」，复用 `session_store` 做多轮上下文。

**验收：**
- 问「分析各行业的趋势走向」→ 返回结论 + 追问，证据链在对话里隐藏。
- 每条结论都能反查到 `trace` 指向的表和字段。

---

### 阶段 5 · 报告产出（组合报告 + 溯源）

**目标：** 报告 = 场景模板 × (维度×功能章节)，证据链完整写入。

**步骤：**

1. **报告框架**：报告 = 场景模板（通识 / 欺诈 / 尽调 / 基本面）× 章节引擎（维度×功能）。每个场景模板定「讲什么故事」，章节引擎定「每章怎么填充」。

2. **改 `report_generator.py` + 模板**：每章三段式「功能是什么 → 结论 → 证据链」；附录自动生成「数据说明 + 方法说明」。

3. **三级溯源渲染**：报告里每个数字带可点击溯源；前端渲染 claim 的 `trace` 为溯源气泡。

4. **抗幻觉校验**：报告生成后逐句反查「数字能否在溯源链路找到」，找不到回退模板句；离线用 `selfcheckgpt` 抽检。

**验收：**
- 点「生成《行业趋势研判报告》」→ 产出 PDF，含完整证据链 + 数据说明附录。
- 报告里无「无锚点」的 LLM 自由发挥数字。

---

### 阶段 6 · 收尾验收

- 前端 `npm run build` + 后端 `python -m pytest` 全绿。
- 端到端走通：登录 → 研判中心 → 点卡片问「行业趋势」→ 出结论 → 追问 → 生成 PDF 报告。
- 报告含真实性保障（结论可验证）+ 可追溯性（数字可溯源）。

---

## 五、关键决策点（需用户拍板，Cursor 先停）

1. **报告对象**：匿名群体/行业切片（数据名字不可得，只能如此）**还是**具体企业（需真实姓名，与脱敏约束冲突）。→ 默认按「匿名群体/切片」做。
2. **数据库**：MySQL 存原始 + PG 存聚合（推荐）**还是**单库 PG 转换导入。→ 默认推荐方案。
3. **Python 版本**：3.14 缺部分 wheel，是否统一降到 3.11/3.12。→ 默认降。

---

## 六、附录

### 开源库落点速查

| 库 | 位置 | 接在哪个文件 |
|---|---|---|
| pyod | `opensource/pyod` | `fraud_engine.py`（反欺诈异常检测） |
| acc_fraud_detections | `opensource/acc_fraud_detections` | 参考 `sequence_number_mismatches.py` |
| search_benford_law_compatibility | `opensource/search_benford_law_compatibility` | `authenticity_engine.py`（Benford 定律） |
| instructor | `opensource/instructor` | `llm_reply.py`（结构化输出） |
| outlines | `opensource/outlines` | `llm_reply.py`（备选约束解码） |
| selfcheckgpt | `opensource/selfcheckgpt` | 报告生成后离线抽检 |

### 复用的现有框架（不重写）

`main.py` / `deps.py` / `session.py` / `responses.py` / `auth_service.py` / `rate_limiter.py` / `session_store.py` / `email_service.py` / `report_generator.py`（改模板）/ `llm_reply.py`（改 prompt）/ 前端 `App.tsx`（改路由）/ 宇宙主题背景。

### 数据字典

字段解释查 `D:\projects\risk-assessment\data\财税票数据字典-20250417.xlsx`，表结构查 `建表.sql`。
