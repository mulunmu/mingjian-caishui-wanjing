# 明鉴 2.0（明鉴01）上线改造方案

> 依据：《项目 3.0 企划书》 + 《AUDIT_REPORT.md §十九 v1↔v2 全量差异审计》
> 目标：让 2.0 达到**可上线**状态（可部署、可运行、安全、可演示、可复现）。
> **本次范围外（暂缓）**：报告模块（报告中心 / PDF 生成 / 邮件发送 / 报告溯源）——后续单独探讨。
> 状态：方案讨论稿，**尚未实施**。待关键决策点拍板后，再逐项落地。

---

## 一、上线目标与范围

### 1.1 目标定义（Definition of Done）

上线 = 以下全部满足：

1. **一键可部署**：给定一台机器，能按文档把前后端 + 数据库 + 缓存跑起来。
2. **数据是 live**：`GET /api/v1/health` 返回 `data_mode: live`、`enterprise_count ≥ 200`，前端不显示 mock 横幅。
3. **核心链路可用**：登录 → 研判中心对话 → 仪表盘 → 个体画像 → 反欺诈/真实性独立页，全通。
4. **安全达标**：生产配置校验（`validate_production_config`）通过；无明文密钥；鉴权/CORS 就位。
5. **构建/测试绿**：后端 `pytest` 全绿，前端 `npm run build` 产出可部署产物。

### 1.2 明确暂缓（报告模块）

以下模块**保留现状、不在本次上线范围内**，后续单独探讨：

| 模块 | 涉及文件（2.0） |
|---|---|
| 报告路由 | `backend/app/api/v1/report.py` |
| 切片报告/组章引擎 | `backend/app/services/slice_report.py`、`report_charts.py`、`report_templates.py`、`report_html.py` |
| 报告模板 | `backend/app/templates/report.html`、`slice_report.html` |
| 报告中心前端 | `src/pages/ReportCenter.tsx`、`ReportPage.tsx`、`src/components/report/*` |
| 报告结论存储 | `backend/app/services/conclusion_store.py` |

> 暂缓 ≠ 删除：上述文件仍在代码里、随前端一起构建，只是不投入精力修复/打磨。

---

## 二、现状问题清单（来自 §十九 审计，作为改造依据）

| 编号 | 级别 | 问题 | 位置 |
|---|---|---|---|
| P0-1 | 阻断 | `docker-compose.yml` 陈旧失效（引用不存在的 `./data/mysql-init` 与 `./frontend`） | `2.0/docker-compose.yml` |
| P0-2 | 阻断 | 前端无 Dockerfile，根目录前端无法被 compose 构建 | `2.0/`（无 `frontend/`、无前端 Dockerfile） |
| P0-3 | 阻断 | 后端不托管前端静态产物，prod 模式静态托管未接线 | `backend/app/main.py`（无 StaticFiles）、`start.sh prod` |
| P0-4 | 安全 | `.env` 含真实 LLM key 明文，且随 zip 分发 | `backend/.env` |
| P0-5 | 安全 | `AUTH_REQUIRED=false`、`JWT_SECRET=your-jwt-secret-here`（占位）、`CORS_ORIGINS=*` | `backend/.env`、`main.py` |
| P1-1 | 工程 | `package.json` 自引用依赖 `"tax-risk-analysis": "file:"` | `2.0/package.json` |
| P1-2 | 工程 | `vite.config.ts` 硬编码代理 `localhost:8000`，无环境变量加载 | `2.0/vite.config.ts` |
| P1-3 | 配置 | `.env.example` `LLM_DAILY_LIMIT=1000` 与应用默认 100 不一致 | `backend/.env.example`、`app/services/rate_limiter.py` |
| P1-4 | 一致 | `mock_data.py` 仍用旧字段名（`enterprise_name`/`public_revenue`/`market_cap`/`pe_ratio`/`roe`/`z_score`） | `backend/app/services/mock_data.py` |
| P1-5 | 质量 | 前端测试与 ESLint 全部缺失（12 个测试文件、vitest、eslint） | `2.0/` |
| P2-1 | 技术债 | `enterprise_name → display_label` 只读 property + 大量 `getattr` 兜底 | `app/models/core_metrics.py` 及多处 |
| P2-2 | 数据 | 缺少 `data/`（1.95GB 源库 + 建表 DDL + 数据字典），仅 200 家模拟种子 | `1.0/data/`（未迁入 2.0） |

---

## 三、上线改造方案（分阶段，按依赖排序）

### 阶段 A · 可部署（解决 P0-1/2/3，先让它跑起来）

**A1 确定部署形态（决策点 D1 见 §五）**
- 二选一后统一文档：
  - **方案甲（推荐）**：修复 `docker-compose.yml` —— 前端改为根目录构建，补 `Dockerfile`（multi-stage：`node:18` 构建 → `nginx` 或后端 `StaticFiles` 托管）；处理 `mysql` 服务（若无需真实源库则移除该 service 及其 `./data/mysql-init` 挂载）。
  - **方案乙**：弃用 compose，统一走 `start.sh/bat`（本机 Python + Node），并补齐文档与生产化脚本。

**A2 前端可构建、可托管**
- 补根目录 `Dockerfile`（或 nginx 配置）：`npm ci && npm run build` → 静态产物。
- 后端托管：在 `backend/app/main.py` 挂 `app.mount("/", StaticFiles(...))`（在 API 路由之后），或由 nginx 反向代理 `dist/`。

**A3 数据就位**
- 用 `backend/seed_data.py` 把 200 家匿名企业种子导入 PostgreSQL（`docker compose run backend python seed_data.py` 或本地 `python -m backend.seed_data`）。
- 验收：`/health` 返回 `data_mode: live`、`enterprise_count == 200`。

### 阶段 B · 安全加固（解决 P0-4/5）

**B1 密钥轮换**
- 轮换 DeepSeek key（旧 key 已泄漏），改为从部署环境注入，`backend/.env` 只留占位，并确认 `.gitignore` 覆盖 `.env`。

**B2 生产配置校验通过**
- 上线时设 `AUTH_REQUIRED=true` + 强随机 `JWT_SECRET` + 显式 `CORS_ORIGINS`（前端域名）。
- 注意 `main.py` 中 `validate_production_config()` 会在配置不达标时**启动即抛 RuntimeError**——这是有意设计，需把上面三项配到位才能启动。

### 阶段 C · 配置与工程化（解决 P1-1/2/3/4）

**C1 前端环境变量**
- `vite.config.ts` 恢复 `loadEnv`，用 `VITE_API_URL` 覆盖代理与端口，消除硬编码 `localhost:8000`。

**C2 package.json**
- 删除 `"tax-risk-analysis": "file:"` 自引用依赖。

**C3 LLM 配额对齐**
- `LLM_DAILY_LIMIT` 统一一个值并加注释（应用默认 100 vs 示例 1000）。

**C4 mock 字段收敛**
- `mock_data.py` 改用新 schema 字段（`display_label`/`invoice_revenue`/`finance_revenue`/`cash_flow_*` 等），与 `core_metrics` 对齐。

### 阶段 D · 质量验证（解决 P1-5）

**D1 后端测试**
- `cd backend && python -m pytest` 全绿（当前 ~215 用例）。

**D2 前端构建**
- `npm run build` 产出 `dist/`，无 TS 报错。

**D3 冒烟（可复现脚本）**
- 登录 → `/api/v1/health` live → 一次对话研判 → 一次个体画像，用脚本固化（可复用 1.0 的 e2e 思路，2.0 需重建 e2e 或至少冒烟脚本）。

### 阶段 E · 打磨（解决 P2，可选）

**E1 兼容层收敛**：`enterprise_name` 只读 property + `getattr` 兜底逐步替换为单一字段 `display_label`。
**E2 数据字典回填**：若上线需 live 真实数据，把 `1.0/data/`（源库 + 字典）迁回并按 ETL 管线入库（决策点 D2）。

---

## 四、验收清单（上线前逐项勾选）

- [ ] 一键部署命令可复现（README/方案甲或乙 的文档一致）
- [ ] `/health` → `data_mode=live`、`enterprise_count≥200`、`llm_configured=true`、`redis=connected`
- [ ] 登录/注册可用；`AUTH_REQUIRED=true` 下未授权请求 401
- [ ] 研判对话、仪表盘、个体画像、反欺诈页、真实性页 全通
- [ ] 生产配置校验通过（无启动 RuntimeError）
- [ ] 无明文密钥提交；`CORS_ORIGINS` 收敛
- [ ] 后端 pytest 全绿；前端 build 成功
- [ ] （暂缓）报告中心：保留可访问，不做深度验收

---

## 五、关键决策点（需你拍板后我再动手）

| 编号 | 决策 | 选项 | 我的建议 |
|---|---|---|---|
| D1 | 部署形态 | 甲）修复 Docker Compose / 乙）统一 start.sh 本机脚本 / 丙）裸机 + systemd | 甲（可复现性最好，1.0 已验证过这套） |
| D2 | 数据来源 | A）200 家匿名模拟种子（seed_data.py）/ B）回填 1.95GB 真实源库走 ETL | 先 A 保证可跑，B 作为后续 live 增强 |
| D3 | 认证模式 | A）强制登录 `AUTH_REQUIRED=true` / B）演示免登录 `false` | 上线走 A，演示可切 B |
| D4 | 部署环境 | 域名 / HTTPS / 端口（前端 3000 或 5173、后端 8000）/ 是否上 nginx | 需你提供目标环境 |
| D5 | 报告模块 | 完全搁置 / 仅修「能访问不报错」的阻断项 | 完全搁置，后续单独排期 |

---

## 六、建议的实施顺序（拍板后）

1. **D1+A1+A2+A3**（先跑起来）→ 2. **B1+B2**（安全）→ 3. **C1–C4**（工程化）→ 4. **D1–D3**（验证）→ 5. E（可选打磨）。

> 本方案只做规划，未改动任何代码。待你就 §五 决策点给出方向后，我们再逐项落地。
