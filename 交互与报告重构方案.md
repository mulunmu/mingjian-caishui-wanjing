# 明鉴 · 交互与报告重构方案（研究 + 方法 + 优先级）

> 依据：你的 P1–P4 问题清单 + 对话实录 + 报告模板/对话代码实读 + 网络调研（论文 / GitHub 开源 / BI / 可视化范式）。
> 一句话总括（沿用你的）：
> **卡顿来自首屏过载与同步重活；不会用来自「多入口 + 主体未绑定 + FAQ/建议复读闭环」；报告与对话的「糊弄感」都来自同一点——系统在复述模板，而不是沿用户上一步结论做可验证的下钻。**

---

## 〇、核心矛盾（先立住，方案全从它推导）

产品文案按「顾问演示（已有样例结论）」卖，运行时却按「空会话 / FAQ / 全库聚合」跑；追问是**字符串拼接**，不是「针对上一结论的结构化下钻」。

**解法的总钥匙 = 把对话里的每一条结论变成一个「可引用的对象」**，追问不再是重新理解一句话，而是对上一结论对象执行**有界操作**（按行业拆 / 拆名单 / 拆差额 / 回滚）。这正好是对话式 BI 与任务型对话框架已经解决的问题。

---

## 一、问题优先级

| 优先级 | 问题 | 理由（对照北极星「小白一眼看懂、一来会用」） |
|---|---|---|
| **P0** | P4 对话逻辑（复读/闭环/糊弄） | 直接摧毁「小白可用」，是「像复读机/糊弄」体感的根 |
| **P0** | P2 第一眼不会用（入口/引导/主体未绑定） | 决定「30 秒会不会用」，入口错则后面全白搭 |
| **P1** | P1 性能与体感卡顿 | 卡顿会把上面两条的努力全抵消（再懂也点不动） |
| **P1** | P3 报告看不懂 | 核心交付物不合格，但可独立改，不必阻塞对话 |
| **P2** | D 四场景无操作剧本 | 是 P2 的一个具体子项，随 P2 一起修 |

---

## 二、推荐方法（带外部类比）

### 2.1 对话：结构化下钻（结论对象引用 + 有界槽位）★ 最关键

**外部类比**
- **Rasa forms/slots**：任务型对话的标准做法——槽位（slots）记录「上一结论涉及什么」，追问通过 slot 填充/更新往下走，而不是重新猜意图。`FollowupAction` 还能在跑题后回到原任务。
- **NL4DV 多轮扩展**（arXiv 2207.00189）：把追问识别为对上一查询的 **add/remove/replace** 操作（属性/任务/可视化），而非全新查询。
- **BI-REC**（arXiv 2105.00467）：把分析步骤建模成 **DRILL-DOWN / ROLL-UP** 推荐，分析状态是「上一结论」的图嵌入，下一步推荐基于它。
- **WrenAI / SuperSonic 的语义层**：把「指标 + 维度 + 过滤」预先定义成受控对象，LLM 只做「意图 → 语义对象」的映射，不直接生成 SQL。

**落到本项目（不引入框架，只抄形态）**
- 现在 `ConclusionRecord` 已经存了 `claims_json` / `followups_json` / `evidence_hidden`，只是 `followups_json` 存的是**裸字符串**，前端 `chatStore.clickFollowUp` 直接 `sendMessage(question)` 重发——这就是复读/答非所问的根。
- 改成 **followup 是结构化对象**：
  ```json
  { "type": "drilldown", "claim_id": "c_62进销错配", "op": "group_by_industry" }
  { "type": "drilldown", "claim_id": "c_62进销错配", "op": "top_list", "entity": "主体", "limit": 22 }
  { "type": "action",    "action": "生成待查清单" }
  { "type": "navigate",  "target": "report_generate" }
  ```
- 后端 `chat_router` 加一条「沿 claim 下钻」分支：`claim_id` 指向上一轮结论，`op` 决定对那个集合做 group_by / topN / rollup；**数字仍读宽表/引擎出，LLM 只填 op 与参数，不编数**（守住铁律）。
- 三类控件拆分（对应你的问题「建议/追问/导航怎么拆」）：
  - **建议（action）**：祈使句「调这 62 家票据背景」→ 渲染成**动作卡**，点击触发动作，不进对话复读。
  - **追问（drilldown）**：带 `claim_id` 的结构化下钻，比如「62 家按行业」→ 真出「62 家 × 行业分布 + 名单」。
  - **导航（navigate）**：「生成报告 / 查看预警」→ 跳转/弹窗，不进对话。

### 2.2 报告：一页一个结论（三层结构 + 标题即结论）

**外部类比**
- **执行摘要三层结构**：1 页摘要（结论先行）→ 每页一个结论的支撑分析 → 详细数字表（专业读者才展开）。
- **「一页一个结论」**：每页一个 punchy 标题（说结论，不说主题名），整页内容服务这一个结论；指标明细默认折叠。
- **通俗化**：砍术语、量化风险、用「On Track / At Risk / Off Track」式白话状态标签，替代内部评级词。

**落到本项目（改 `report_html.py` + `slice_report.html`）**
- 封面已定稿为**极简（标题 + 日期 + 编号）**，不放结论（场景/主体/一句话结论/等级徽章一律后置到执行摘要）；问题在**正文**：现在是「维度归因 + 六维表现 + 关键数字表(最多 12 行) + 结论 bullets」的指标堆砌。
- 正文每章改成：**标题 = 白话结论（带好坏标签）→ 「所以呢」1 句 → 一张与结论对齐的图 → Top3 可照做动作**；指标明细表**默认折叠 / 次级章节**（禁用「附录」二字，见 §五）。
- 术语翻译层替换「维度 / 均分 / 命中分布 / 本维指数」→ 岗位话术（「放贷能不能批 / 哪里异常 / 该查谁」）。

### 2.3 第一眼会用：演示样例 onboarding + 单一主线

**外部类比**：渐进披露（一次只给一个核心动作）、首跑引导（first-run tour）。

**落到本项目**
- **登录后落「风险研判」**（你的决策 A），并**自动载入一个「演示样例」**：绑定一个虚拟企业 + 一条已算好的结论 + 一个输入框 + 最多 3 个场景入口。
- 30 秒最小路径：用户看到「引擎能给出一条什么结论」→ 点一个 followup 或打一句「这家能贷吗」→ 得到可下钻的回答。
- **无主体放贷/评级**：不弃权推 FAQ、不强制先选企业（都违背「一来会用」），而是**自动绑演示企业**，并明示「我先以演示企业 #482 为例」，想换就说「换一家 / 从列表选」。（符合你的「引擎演示」心智）

### 2.4 可视化契约：问什么配什么图

**外部类比**（图表选择契约）：漏斗→流程流失、横向条形→排名、表格→精确名单、KPI 大字→单指标、散点→关系。

**落到本项目（62 家案例）**
| 用户问 | 必须给的图/表 |
|---|---|
| 哪里可疑要查 | **漏斗**（193 → 中高风险 N → 进销错配 62 → 冲红 9） |
| 62 家按行业 | **横向条形**（各行业异常数，降序）+ 脱敏名单 TopN |
| 22 家主体与差额 | **表格**（企业 N + 差额，精确值） |
| 27 家对手方 | **表格/名单** |

**答非所问时**：系统应**换到正确契约**（明说「没有你要的这个数，但可以给你 X」），而不是硬凑一个「沾边指标」（如把「行业分布」答成「纳税准时率」）——这正是糊弄感的来源。

### 2.5 性能：减负顺序（不牺牲功能）

1. 落地**不自动弹报告向导**、**不拉全量 193**（改为按需/分页/演示样例）。
2. 报告生成改**异步 + 进度**（SSE 或轮询），前端不再长时 `isLoading` 阻塞。
3. 吉祥物 `mousemove` 状态**降到局部组件**（或 RAF 节流），不驱动整页重渲染。
4. 聊天列表**虚拟化**（react-window / @tanstack/react-virtual）。
5. 图表/报告**懒加载 + 骨架屏**。

---

## 三、适用边界（什么能抄、什么不能）

| 外部方案 | 结论 | 理由 |
|---|---|---|
| Vanna / WrenAI / SuperSonic 的 **text-to-SQL** | **不引入**，只借「语义层 + 有界槽位」形态 | LLM 直接生成 SQL 查库会**编数**，违反「数字只来自引擎」铁律；我们的数字必须仍从宽表/引擎出 |
| WrenAI **语义层(MDL)** / SuperSonic **Headless BI** | **抄思路** | 把指标/维度/过滤预定义成受控对象，LLM 只做意图→对象的映射 |
| Rasa 全套框架 | **不引入** | 太重，与现有 FastAPI + SQLAlchemy 引擎重复；只抄 forms/slots 思想，用现有 `ChatSessionRecord` 做轻量状态机 |
| BI-REC / NL4DV / Data Formulator 2 / Intelligent Drill-Down | **学思想** | 下钻/回滚、follow-up 操作集、data threads（分支历史） |
| react-window / @tanstack/react-virtual | **直接集成** | 列表虚拟化，纯前端、零风险 |
| 执行摘要三层 + 一页一结论 + 图表选择契约 | **直接采用** | 纯设计原则，无技术债 |

**反模式（你已圈定，守红线）**：不靠「再堆 FAQ」「再堆侧栏卡片」解决不会用；不让 LLM 编造名单/差额；不把学术指标解释写进用户主路径；名单脱敏、不假设能出真名。

---

## 四、先做哪 3 刀（体感变化最大）

### 刀 1：对话不再糊弄（追问结构化下钻 + 三类控件拆分）——治 P4
- 后端：`ConclusionRecord.followups_json` 从字符串改为结构化 `{type, claim_id, op, params}`；`chat_router` 加「沿 claim 下钻」分支，数字读引擎。
- 前端：`chatStore.clickFollowUp` 按 `type` 路由；`chatStore`/消息类型区分 action/drilldown/navigate。
- **一期只做 3 个 op**：`group_by_industry` / `top_list` / `show_funnel`，其余显示「暂不能下钻」。
- **数据侧已有半成品**：`judgment_service` 已有 `unique_affected`、`signal_funnel_chart`、`build_ranking/distribution/segmentation` 等语义查询——不是从零发明，是把已有聚合接到 claim 引用上。
- 体感：「调这 62 家」不再复读；「62 家按行业」真出「62 家 × 行业分布 + 名单」；FAQ 死循环消失。

### 刀 2：一来就会用（拆 2a / 2b / 2c）——治 P2 + P1 首屏

- **2a 入口 / 演示绑定（P0）**：登录落「风险研判」，自动载入绑定虚拟企业的演示样例 + 一条已算好结论 + 单一输入框 + 3 场景入口；四场景各配「操作剧本」。
  - 后端：加「演示样例」接口（虚拟企业 + 预计算结论，秒回，不拉全量）。
  - 体感：30 秒看到「这系统能干嘛」；「这家能贷吗」不再空答。
- **2b 首屏性能（半天级）**：去掉自动弹向导 / backdrop-blur / 全量 193 预载；吉祥物 mousemove 状态降到局部组件；企业列表按需加载 + 虚拟化。
- **2c 接入弹窗（随后）**：数据接入嵌进研判、做成与生成报告同构的弹窗；缺数弃权 → 打开接入弹窗。

### 刀 3：报告 3 秒看懂（一页一个结论）——治 P3
- 后端：`report_html.py` / `slice_report.html` 正文每章改为「标题=白话结论(好坏标签) → 所以呢 1 句 → 一张对齐图 → Top3 动作」；指标明细**默认折叠 / 次级章节**（禁用「附录」二字，见约束补丁 1）；术语翻译层替换「维度/均分/命中分布」。
- 体感：正文从「指标堆砌」变「结论页」，小白每章 3 秒看懂；结论回到执行摘要，封面保持干净。

---

## 五、落地约束补丁（采纳 Cursor 反馈，4 条，开刀前必读）

1. **报告禁用「附录」二字**：`report_templates.py` 的 `FORBIDDEN_MARKERS` 含「附录」，`scan_forbidden_in_text` + CI 机检会硬拒。刀 3 的「指标明细压附录」改为**「明细默认折叠 / 次级章节 / 展开查看数字表」**。顺带：现有对话 followup 已泄漏「下载后核对附录数据说明」（`chat_router.py:128/1010`），一并改成「下载后核对数据说明」。

2. **可视化契约要诚实（三档）**：
   - 必答且有数：62 家×行业分布、进销错配 TopN（企业 N）、信号漏斗。
   - 有数但弱：冲红次数 / 集中度分数。
   - 明确弃权：关联关系、真实货物凭证（属「建议人去调」，系统列不出名单）——弃权就明说，绝不硬凑「沾边指标」。

3. **claim_id 生命周期写死**：会话维护 `active_conclusion_id`（或每条消息挂 `conclusion_id`）；下钻失败时明说「上一结论已过期，请重问哪里可疑」，**禁止 silently 退回 FAQ**。否则多轮后 claim 对不上，又回到「哪里可疑要查 → 没有任何信息」。

4. **决策 B 接入弹窗进主线**：无数据弃权 → **打开「数据接入」弹窗**（与生成报告同构），而不是推 FAQ 三件套。否则 onboarding 主线断一截。

**刀拆分（减轻每刀重量）**：刀 1 一期只做 3 个 op；刀 2 拆 2a 入口/演示绑定、2b 首屏性能（半天级）、2c 接入弹窗；PDF 真异步放最后，先做「生成中可关弹窗 + 列表出一条生成中」的轻异步。

---

## 六、参考来源

**开源（GitHub）**
- [vanna-ai/vanna](https://github.com/vanna-ai/vanna) — 对话式 SQL，SSE 流式 + `<vanna-chat>` 组件（借工程形态，不借 text-to-SQL）
- [Canner/WrenAI](https://github.com/Canner/WrenAI) — 生成式 BI，语义层 MDL（借语义层思路）
- [tencentmusic/supersonic](https://github.com/tencentmusic/supersonic) — Chat BI + Headless BI 语义层
- [sqlchat/sqlchat](https://github.com/sqlchat/sqlchat) — 聊天式 SQL 客户端
- [RasaHQ/rasa](https://github.com/RasaHQ/rasa) — forms/slots 对话状态机（[多轮对话](https://rasa.com/blog/multi-turn-conversation)、[Forms and Slot Filling](https://deepwiki.com/RasaHQ/rasa/2.4-forms-and-slot-filling)）
- react-window / @tanstack/react-virtual — 列表虚拟化

**论文（arXiv）**
- NL4DV 多轮扩展：Facilitating Conversational Interaction in NLIs for Visualization（[arXiv 2207.00189](https://arxiv.org/abs/2207.00189)）
- BI-REC：Guided Data Analysis for Conversational BI（[arXiv 2105.00467](https://ar5iv.labs.arxiv.org/html/2105.00467)）
- Intelligent Drill-Down：LLM 驱动的视觉探索下钻（[arXiv 2604.17002](https://arxiv.gg/abs/2604.17002)）
- Data Formulator 2：迭代式可视化 + data threads（[arXiv 2408.16119](https://arxiv.org/abs/2408.16119)）
- InsightLens：从对话上下文提取洞察（[arXiv 2404.01644](https://arxiv.org/abs/2404.01644)）

**报告/可视化范式**
- 执行摘要三层 + 一页一结论：bank board 报告呈现（[Abrigo](https://www.abrigo.com/blog/how-to-present-stress-test-results-to-the-bank-board-part-ii/?sw)）、[FirmAdapt 非技术受众](https://firmadapt.com/blog/presenting-company-analysis-to-non-technical-stakeholders)、[One-Page Reports](https://www.glencoyne.com/guides/one-page-reports)
- 图表选择契约：[data-visualization-ux](https://github.com/vasilyu1983/AI-Agents-public/blob/main/frameworks/shared-skills/skills/software-ui-ux-design/references/data-visualization-ux.md)、[chart-selection](https://github.com/aref-vc/tufte-claude-skill/blob/main/chart-selection.md)、[Klipfolio 非技术用户可视化](https://www.klipfolio.com/blog/visualizing-data-non-technical-users)

---

*配套：`系统诊断与改进方向.md`（病根）→ `落地实施蓝图.md`（打磨+验证顺序）→ 本文档（交互与报告的重构方法与优先级）。*
