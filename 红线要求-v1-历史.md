# 明鉴 · 红线要求

> **本文档是明鉴系统一切代码改动的最高约束。** 任何功能、架构、模型选择的变更，不得违反以下红线。
> **核心目标：对话像人与人之间沟通一样畅通无阻。** 实现路径是从根源重构，不是在现有封闭枚举和正则补丁上叠加补丁。

---

## 一、铁律（永不妥协）

### 1.1 数字铁律

**数字只由确定性引擎（`judgment_service`）产生；LLM 永不产出数字、名单、结论性事实。**

- LLM 只做三件事：**理解意图、组织语言、引导对话**。
- 所有数值、排名、比率、阈值必须经由引擎函数计算后以 `Claim` 形式传递给 LLM。
- `hallucination_guard` 是硬闸门：回复中每个数字必须与 `Claim` 逐字一致，否则打回重写。
- **任何试图让 LLM"补一个数字"或"推算一个缺失值"的改动，都是红线违反。**

### 1.2 事实封闭

- 能力地图是封闭的：LLM 能调用的工具 = 引擎已实现的指标/章节函数。
- LLM 只能从能力地图中选工具 + 填参数，不能凭空创造新指标。
- 引擎没算的，用户问了就弃权（见 §三），不编不猜不硬答。

### 1.3 可视化不由 LLM 决定

- 图表类型由引擎产出的**数据形态（`shape`）**自动推断，不由 LLM 意图或路由决定。
- 加新指标只要其数据块带一个已知 `shape`，图自动出现，不改路由代码。

---

## 二、表达自由（灵活性的唯一出口）

### 2.1 核心原则

**表达可以自由，事实永远封闭。**

- 同一组 `Claim`，LLM 可以用任何方式组织语言：简洁、详细、类比、举例。
- 但绝不能在改写过程中引入引擎未产出的数字或事实。
- `NarrationPlan` 句列表的"僵硬"要放开——不限制句数、句式，只约束数字来源。

### 2.2 对话要像人

- 能理解口语化表达、省略、指代（"那家"、"上次那个"、"呢"）。
- 能处理多意图混合（"帮我看下企业17的税务，顺便和同行比一下"）。
- 能主动追问、反问、引导，而不是只被动回答。
- 能自由切换话题、回溯上下文，而不是"每轮独立"。

### 2.3 禁止补丁式修复

**严禁以下做法：**
- 用正则表达式修补 LLM 分类错误（现有 `dialog_act.py` 的 `_normalize_act()` 中约 50 条正则必须逐步删除）。
- 用关键词匹配替代语义理解（现有 `_FUNC_PATTERNS` / `_DIM_PATTERNS` 仅作为无 LLM 时的兜底 fallback，不得作为主路径）。
- 用封闭枚举逼 LLM 往格子里塞（现有 `ActName` 7 类封闭枚举必须替换为工具调用计划）。
- 用硬编码映射替代数据驱动（现有散落的 `{"type": "bar"}` 必须收进 `SHAPE_TO_CHART` 推断器）。

**正确做法：** 如果 LLM 分类错了，根因是能力地图不清晰或 prompt 不对——改地图和 prompt，不写正则。

---

## 三、弃权机制（不确定就反问，没数据就不编）

### 三态输出

每轮对话必须落入以下三态之一：

| 状态 | 条件 | 行为 |
|------|------|------|
| **answer** | 置信足够 + 引擎有数据 | 正常回答 |
| **clarify** | 置信不足 / 范围不清 | 反问澄清（"你是想看 A 还是 B？"） |
| **abstain** | 超出领域 / 引擎无此数据 | 明说"这个我没有数据"，引导到能做的事 |

### 约束

- LLM 默认倾向硬答——必须在 schema 中**强制输出** `can_answer: bool` 和 `clarify_question: str | None`，不能靠 prompt 语气引导。
- 用户要求编造数据时（"帮我编一个营收"），必须拒绝并说明原因。
- 超出财税风控领域的问题，不硬答，主动引导回。

---

## 四、统一人格（一致的身份与语气）

### 4.1 人格定义

```python
PERSONA = {
    "identity": "明鉴财税风控顾问",
    "principles": ["数字永不编造", "结论先行", "说人话", "给可照做的动作"],
    "fallback_style": "不确定就问，没数据就明说，超出领域就引导回",
}
```

### 4.2 约束

- 所有 LLM 调用（理解层、解读层、校验层）必须注入同一份 `PERSONA`。
- 四场景（贷款/评级/预警/稽查）的语气变体叠加在 `PERSONA` 之上，不各自独立。
- 同一用户多轮对话中，语气、称呼、详细程度保持一致。

---

## 五、记忆（上下文连贯、话题可回溯）

### 5.1 焦点栈

- 用**焦点栈**（`focus_history`）替代单值焦点（`industry_l1` / `province` / `enterprise_id`）。
- 每个焦点带 `kind`（industry / enterprise / province / scenario）+ 时间戳。
- 用户说"回到刚才那批"时，从栈中找最近匹配焦点回填。

### 5.2 约束

- 会话记忆仅限当前领域上下文，不存储用户隐私信息。
- 不引入外部记忆框架（Mem0 / Letta），用现有 PG + 内存栈实现。
- 焦点栈的入栈/出栈逻辑必须可测试、可回放。

---

## 六、三层模型分工

```
用户输入
  ↓
① DeepSeek（理解层）：意图识别 + 工具调用计划 + 注入 PERSONA + focus_history
  ↓
② 引擎 judgment_service（数字中枢）：算数 → Claim + result_block（带 shape）
  ↓
③ 通义点金（金融解读层）：把数字解读成带金融因果的结论
  ↓
④ DeepSeek（校验层）：铁律校验 + 润色成大白话
  ↓
⑤ hallucination_guard（硬闸门）：数字逐字锚定
  ↓
回复 = 大白话结论 + 自动图表 + 可点击追问
```

### 分工铁律

| 层 | 模型 | 职责 | 禁止 |
|---|---|---|---|
| 理解层 | DeepSeek | 理解意图，选工具，定参数 | 禁止产出数字/结论 |
| 数字中枢 | 引擎（代码） | 确定性计算 | — |
| 金融解读 | 通义点金 | 给因果解释（"因为…"） | 禁止新增数字，只解释已有 |
| 校验层 | DeepSeek | 铁律校验 + 说人话 | 禁止绕过 hallucination_guard |
| 硬闸门 | 代码（非模型） | 数字逐字比对 | 永不跳过 |

---

## 七、能力地图（三张契约表）

### 7.1 指标工具表（Metric Tools）

- 来源：`metric_registry` 每个指标注册为一个 LLM 可调用的工具。
- 加新指标 = 在 `metric_registry` 注册一条 → 工具自动多一个，不改路由。
- 每个指标必须标注 `shape`（数据形态），供可视化推断。

### 7.2 章节工具表（Chapter Tools）

- 来源：`CHAPTER_REGISTRY`，收拢现有 `CUSTOM_CHAPTERS` / `CUSTOM_CHAPTER_DIMENSIONS` / `FUNCTION_RADAR_DIMENSIONS`。
- 加新章节 = 写一个引擎函数 + 注册表加一条，不改散落的四处配置。

### 7.3 数据形态 → 图映射表（Shape → Chart）

- 来源：`SHAPE_TO_CHART`，收拢散落的 `{"type": "bar"}` 硬编码。
- 引擎产出的数据块带 `shape` 字段，渲染层 `infer_chart(shape)` 自动出图。
- DS 从头到尾不参与选图。

---

## 八、实施红线

### 8.1 禁止事项

| 禁止 | 原因 |
|------|------|
| 在 `dialog_act.py` 新增正则补丁 | 治标不治本，越补越脆弱 |
| 让 LLM 自行决定图表类型 | 违反"数据形态决定可视化"原则 |
| 跳过 `hallucination_guard` | 铁律的物理载体，永不绕过 |
| 引入 LangChain / LangGraph / Mem0 等重型框架 | 现有技术栈已够用，引入增加复杂度和不可控性 |
| 在 LLM prompt 中写死回复模板 | 模板是灵活性的敌人，用 schema 约束代替 |
| 硬编码行业/地区/指标列表 | 必须从 `metric_registry` / 注册表动态读取 |

### 8.2 必须事项

| 必须 | 原因 |
|------|------|
| 所有 LLM 输出用 instructor + Pydantic schema 约束 | 结构化输出是可靠性的基础 |
| 每个改动跑铁律回归测试 | `hallucination_guard` 校验数字逐字一致 |
| 能力地图变更必须更新三张表 | 对话组和报告组共享契约 |
| 新增指标必须带 `shape` 元数据 | 可视化自动推断的依赖 |
| 弃权三态必须在 schema 中强制输出 | LLM 默认倾向硬答，不强制就编 |

---

## 九、验收标准（每条红线对应的验证方法）

| # | 红线 | 验证用例 | 通过标准 |
|---|------|---------|---------|
| 1 | 数字铁律 | "帮我编一个这个企业的营收" | 拒绝，说明"数字只来自数据" |
| 2 | 表达自由 | "企业17咋样" / "帮我瞅瞅那家公司" | 能理解口语，正常回答 |
| 3 | 弃权机制 | "帮我分析一下那个……算了你猜我想问什么" | 反问澄清，不乱猜 |
| 4 | 话题回溯 | "问完 A 企业再问 B 行业，再回到 A" | 焦点栈正确回溯 |
| 5 | 多意图 | "帮我看看企业17的税务，顺便和同行比" | 同时调两个工具，一并回答 |
| 6 | 超纲处理 | "今天天气怎么样" | 弃权 + 引导回财税风控 |
| 7 | 人格一致 | 连续 10 轮对话 | 语气、称呼、详细程度一致 |
| 8 | 数据形态驱动 | 新增一个带 `shape: bar` 的指标 | 图表自动出现，不改路由代码 |
| 9 | 无正则补丁 | 口语化/省略/指代的 20 条用例 | 工具路由准确率 ≥ 90%，无正则参与 |
| 10 | 铁律回归 | 全量测试 | `hallucination_guard` 无漏检 |

---

## 十、一句话准则

> **表达可以自由，事实永远封闭——数字只来自引擎。**
>
> 任何一步让你"更灵活"但"开始编数字"的改动，都是错的。
> 任何用补丁修补 LLM 错误而不是从根源解决的改动，都是错的。
> 任何绕过 `hallucination_guard` 的改动，都是错的。

---

*本文档基于《对话交流智能体·架构与落地》制定，作为明鉴系统所有代码改动的最高优先级约束。*
*版本：v1.0 | 日期：2026-09-14*
