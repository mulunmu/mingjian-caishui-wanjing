# 明鉴 · 对话交流智能体 · 架构与落地文档

> 本文档回答四件事：**目标到底是什么**、**为什么现在"死板"**、**怎么改到"灵活"**、**下一个人怎么直接开工**。
> 读者：接手工期的后端 / 前端 / 算法同学。读完本文 + 文末参考来源，即可在不追问原设计者的情况下动工。
> 关联文档：《金融智能体与语音升级路线.md》（语音/模型选型）、《系统诊断与改进方向.md》（UI/报告弊病）。

---

## 〇、可行性判定（先给结论，别开工了才发现方向错了）

### 0.1 能不能落实？

**能。** 本文方案不引入任何新的重型框架（不用 LangChain / LangGraph / Mem0 / Letta），全部落在项目**现有技术栈**上：

| 现有资产 | 用途 | 说明 |
|---|---|---|
| FastAPI + SQLAlchemy + PostgreSQL | 会话持久化 | `session_store.py` 已实现内存 L1 + PG 归档 |
| **instructor** + Pydantic | 结构化输出 / 工具路由 | `dialog_act.py`、`llm_reply.py` 已在用 |
| **LiteLLM** | 多模型网关（OpenAI 兼容） | `llm_reply.py` 已用，接通义点金/DeepSeek 零改动 |
| `judgment_service.py` | 确定性引擎（数字唯一来源） | 铁律的物理载体 |
| `metric_registry` / `report_templates` | 指标体系 / 章节函数 | 能力地图的源头 |
| `chart_payloads.py` | 数据形态 → 图 | 已是"可视化解耦"的好雏形 |

**核心结论**：这不是"重写"，是"把现有的封闭枚举 + 正则补丁，替换成一套统一的能力地图 + 工具调用 + 数据驱动呈现"。每一刀都落在已存在的文件上。

### 0.2 能不能实现"和你（Claude）一样的对话"？

这是本文必须先钉死的一句话，否则会误导你写进答辩：

> **能做到「财税风控领域内的顾问式灵活对话」，做不到「通用智能（AGI）」。**

精确边界如下：

| 维度 | 能做到（诚实承诺） | 做不到（诚实边界） |
|---|---|---|
| 话题 | 财税风控领域内自由切换、回溯、追问 | 出了领域 → 主动弃权并引导回（不硬答） |
| 数字 | 表达自由（怎么组织语言都行） | 事实封闭（数字只来自引擎，永不编） |
| 工具 | 从能力地图里自由组合调用多个指标 | 只能调引擎已实现的指标（没算的无法新增） |
| 追问 | 能反问澄清、能"继续/换一家/按地区"回溯 | 不能主动获取项目之外的新数据 |
| 人格 | 统一、一致、有记忆 | 不具备"人"的常识世界知识 |

**一句话定位（可直接写进论文/答辩）**：
> 明鉴的对话层，是在"数字只由确定性引擎产生"的铁律约束下，用「多模型分工 + 能力地图 + 有状态记忆 + 弃权出口 + 统一人格」构造的一个**领域内受控顾问智能体**；它的"灵活"是**受约束的表达自由**，而不是无约束的通用智能。

这条定位**可辩护**：评委追问"它能像 GPT 一样吗"，正确答案是"它在财税风控领域内，比通用模型更可信，因为它永不编数、且每个结论可溯源到引擎"。

---

## 一、铁律（一切设计的原点，永远不破）

> **数字只由确定性引擎（`judgment_service`）产生；LLM 只做三件事——理解意图、组织语言、引导对话。**

推论（本文所有设计从它推出）：

1. **LLM 永不产出数字/名单/结论事实**。它只能引用引擎给出的 `Claim`。
2. **能力地图是封闭的**：LLM 能调用的工具 = 引擎已实现的指标/章节函数。这是"灵活"的边界。
3. **可视化由数据形态决定**，不由 LLM 意图决定（详见 §5.3、§6.4）。
4. **表达自由、事实封闭**：`NarrationPlan` 句列表的"僵硬"要放开，但"数字锚定"的 `hallucination_guard` 要更严。

---

## 二、三层模型分工（谁干什么，各司其职）

```
              ┌─────────────────────────────────────────────┐
              │  DeepSeek（推理模型）＝ 理解层 + 校验层        │
              │  意图识别 / 语义解释 / 工具调用计划 / 最终校验 │
              └──────────────┬──────────────────────────────┘
                             │ 选出「要调哪个工具 + 参数」
                             ▼
              ┌─────────────────────────────────────────────┐
              │  确定性引擎 judgment_service ＝ 数字中枢       │
              │  只算数，产出 Claim + 数据块（带形态类型）      │
              └──────────────┬──────────────────────────────┘
                             │ 数据块（形态类型已带好）
                             ▼
              ┌─────────────────────────────────────────────┐
              │  通义点金（金融模型）＝ 金融解读层              │
              │  把数字解读成「有金融因果」的结论（想得专业）   │
              └──────────────┬──────────────────────────────┘
                             │ 专业解读
                             ▼
              ┌─────────────────────────────────────────────┐
              │  DeepSeek 校验层＝ 铁律校验 + 说人话           │
              │ ① 数字有没有被改/编 ② 润色成大白话（说得通俗）│
              └──────────────┬──────────────────────────────┘
                             │
                             ▼
              ┌─────────────────────────────────────────────┐
              │  hallucination_guard ＝ 硬闸门（代码级）        │
              │  所有数字必须与引擎逐字一致，否则打回重写      │
              └─────────────────────────────────────────────┘
```

### 2.1 三个"为什么"（防止后面又拧回去）

- **为什么 DS 既当理解层又当校验层？** 因为它能"深入思考"（reasoning）。理解层需要想清楚"用户到底要什么"，校验层需要想清楚"这个解读有没有违反铁律"。同一模型复用，省一份成本，逻辑上也最顺。
- **为什么金融解读放在引擎之后、而不是之前？** 若放引擎之前，金融模型就要"决定算什么"，会越过铁律去"编一个指标"。放引擎之后，它只能解释引擎已给出的数字，天然被约束。（这也是"中枢"必须是引擎、不是金融模型的原因。）
- **为什么通义点金只做"想得专业"、不做"说得通俗"？** 金融模型的价值是"0.83 这个数意味着短期偿债偏弱、因为流动比率低于行业均值"这种**带金融因果**的判断。而"说人话"（把术语翻译成大白话）由 DS 校验层 + 现有 `plain_language.py` 完成。两件事别让一个模型扛。

---

## 三、四个非模型能力（"灵活"的真正瓶颈，全部是工程）

你早已判断对：灵活性的瓶颈不是模型，是**模型之外的四个工程能力**。下面是每个能力"落到哪个文件、加什么"。

### 3.1 记忆（上下文连贯、话题回溯）

**现状**（`session_store.py`）：已有内存 L1 + PG 持久化、owner、history（40 条）、`last_intent`/`last_function`/`last_dimension`/`industry_l1`/`province`/`enterprise_id`、`dialogue_state`、`active_conclusion`。

**缺的**：只有"单焦点"（`industry_l1`/`province`/`enterprise_id` 各存一个值），**没有焦点栈**，无法"问完 A 企业再回溯到 B 行业"。

**要加**（M2）：

```python
# session_store.py 新增字段：焦点栈（替代单值 analysis_focus）
"focus_history": [
    {"kind": "industry",  "value": "制造业", "ts": 1710000000},
    {"kind": "enterprise", "value": "企业17", "ts": 1710000010},
]
```

- 每个焦点入栈时带 `kind`（industry/enterprise/province/scenario）+ 时间戳。
- 用户说"回到刚才那批" → 引擎侧在栈里找最近的匹配焦点回填。
- **为什么不用 Mem0/Letta**：本系统字段明确、合规要求高（脱敏）、单租户可枚举，"普通 PG + 焦点栈"比通用记忆平台更可靠、更可控、更好解释。调研结论一致（见 §10 参考 3、4）。

### 3.2 能力地图（工具清单 + 动态路由）

**现状**：`dialog_act.py` 用 `ActName` 封闭 7 类 + ~50 条正则补丁（`_AGGREGATE_ANALYZE_RE`/`_INDIVIDUAL_DEIXIS_RE`/`_GREETING_RE`…）去修 LLM 分类错误。

**问题**：LLM 被逼着往 7 个格子里塞，塞错就用正则擦屁股——这是"死板"的病根。

**要改成**（M1）：**DS 不再输出"分类标签"，而是输出"工具调用计划"**。用 instructor 的 `response_model` 做 function calling，能力地图 = `metric_registry` 的每个指标 + `DRILLDOWN_OPS` + 四场景 + 章节（详见 §5）。

**关键工程点（来自调研）**：
- **两阶段路由降低误判**：先让 LLM 判"这属于哪个域"（放贷/评级/预警/稽查/寒暄/报告），再**只给它相关域的工具子集**去选。工具噪音越少，误路由越少（调研结论：结构路由比关键词路由慢 +50~200ms，但关键词路由是脆弱的，正则需要保留为"无 LLM 时的兜底 fallback"）。
- **结构化输出 + 重试**：instructor 自动 re-prompt（`max_retries`），schema 用 `Literal`/`Field(description=...)` 强约束，路由节点用低温（0~0.3）。

### 3.3 弃权出口（不确定就反问，没数据就不编）

**现状**：`dialog_act.py` 已有 `CONFIDENCE_CLARIFY = 0.55`，低于阈值反问——这是雏形。

**要扩展成三态**（M1），参考 Abstain-QA / Selective QA（§10 参考 1、2）：

```
answer   — 置信足 + 引擎有数据 → 正常答
clarify  — 置信不足 / 范围不清 → 反问澄清（"你是想看 A 还是 B？"）
abstain  — 超出领域 / 引擎无此数据 → 明说"这个我没有数据，我可以帮你查 XX"
```

落地 = LLM 输出 schema 加两个字段：

```python
class DialogAct(BaseModel):
    ...
    can_answer: bool = True          # False → 走 clarify / abstain
    clarify_question: str | None = None  # 反问什么（一句话）
```

**要点**：默认 LLM 很少主动澄清/弃权（会硬答），必须**在 schema 里强制**它输出这个判断，而不是靠 prompt 语气。你的铁律（数字不编）本身就是最强的弃权机制——缺的只是"优雅反问"这个出口。

### 3.4 统一人格（一致的身份 + 语气）

**现状**：`report_templates.py` 已有 `TONE_PROFILES`（loan/rating/warn/audit…），但**只用于报告**，对话层没有复用。

**要改成**（M2）：新增 `backend/app/services/persona.py`，把人格定义集中为一份"常驻核心记忆"（Letta 的 Core memory 思想，但不引框架）：

```python
PERSONA = {
    "identity": "明鉴财税风控顾问",
    "principles": ["数字永不编造", "结论先行", "说人话", "给可照做的动作"],
    "fallback_style": "不确定就问，没数据就明说，超出领域就引导回",
}
```

每次 LLM 调用（DS 理解、DS 校验、通义点金解读）都注入同一份 `PERSONA` + 当前 `focus_history`。四场景的 `TONE_PROFILES` 作为"语气变体"叠加在 `PERSONA` 之上，不再各写各的。

---

## 四、能力地图完整 schema（本文最核心的一张表，定稿后双方共享）

这是"去写死的第一刀"，也是分两个板块时**双方要共同冻结的契约**。它由三张表组成。

### 4.1 表一：指标工具表（Metric Tools）— 从 `metric_registry` 生成

每个指标注册成一个 LLM 可调用的工具：

```python
class MetricTool(BaseModel):
    """LLM 从能力地图里选一个指标工具 + 参数。"""
    metric: str = Field(..., description="指标 key，如 flagged_count / avg_revenue_yoy")
    dimension: Literal["overall", "industry", "region", "time", "signal"] = "overall"
    filters: dict[str, str] = Field(default_factory=dict)  # {industry_l1, province, enterprise_ids}
    op: Literal["lookup", "aggregate", "compare", "trend", "rank", "distribute"] = "lookup"
```

- 生成方式：`metric_registry` 每个指标 → 一条 tool（Pydantic `model_json_schema()` 自动出 JSON Schema）。
- **加新指标 = 在 `metric_registry` 注册一条 → 工具自动多一个**，不用改路由。
- 铁律：LLM 只能"填参数"，不能"编结果"；执行永远走 `judgment_service`。

### 4.2 表二：章节工具表（Chapter Tools）— 从引擎函数注册表生成

把 `report_templates.py` 散落的 `CUSTOM_CHAPTERS` / `CUSTOM_CHAPTER_DIMENSIONS` / `FUNCTION_RADAR_DIMENSIONS` / 散落 KPI 收敛成一张注册表：

```python
CHAPTER_REGISTRY: dict[str, dict] = {
    "fraud": {
        "title": "发票舞弊",
        "desc": "进销错配/红冲/集中度/序列缺口",
        "engine_fn": "build_fraud_claims",       # 铁律：内容只来自此函数
        "default_dimension": "industry",          # fallback，DS 可覆盖
        "radar_dims": ("invoice",),
        "data_shapes": ["bar", "table", "funnel"],# 该章节可能产出的数据形态（决定图）
        "kpis": [{"label": "舞弊预警主体数", "metric": "flagged_count", ...}],
    },
    # ... 其余 7 章同构（financial/tax/authenticity/signal/score/benchmark/trend）
}
```

- **加新章节 = 写一个新引擎函数 + 在注册表加一条记录**，不再改 `CUSTOM_CHAPTERS`/`CUSTOM_CHAPTER_DIMENSIONS`/`CUSTOM_CHAPTER_KEYWORDS`/`spec_to_report_spec` 四处。
- 报告生成 = DS 用同一套 function calling 从 `CHAPTER_REGISTRY` 里选 N 个章节 + 顺序 + 维度 + 范围。

### 4.3 表三：数据形态 → 图映射表（Shape → Chart）— 渲染层纯函数

把 `judgment_service.py` / `chart_payloads.py` / `followup_items.py` 里散落的 `{"type": "bar", ...}` 收敛成一个推断器：

```python
# chart_payloads.py 顶部新增
SHAPE_TO_CHART = {
    "single_value":          None,        # 无图 → 数字卡片
    "categorical_distribution": "bar",    # 分类计数（按行业/按地区）
    "ordered_series":        "line",      # 有序序列（同比趋势）
    "proportion_buckets":    "pie",       # 互斥分桶占比
    "multi_dim_vector":      "radar",     # 六维向量
    "hierarchical_stages":   "funnel",    # 分层递减
    "two_var_correlation":   "scatter",   # 两变量相关
    "matrix_heatmap":        "heatmap",   # 行业×信号
    "tabular_rows":          "table",     # 名单 TopN
}

def infer_chart(result_block: dict) -> dict | None:
    """输入引擎产出的数据块，输出 chart payload；数据形态决定图，与意图无关。"""
```

**关键**：引擎算数时，除了产出 `Claim`，还产出**带 `shape` 字段的数据块**（`{"shape": "categorical_distribution", "data": {...}}`）。渲染层看到 `shape` 自动匹配图。**DS 从头到尾不参与选图**——加新指标只要它的数据块带一个已知 `shape`，图自动出现。

---

## 五、去写死的具体改动清单（逐文件，可直接分配）

> 标注 `[对话组]` / `[报告组]` / `[共享组]`，对应 §七的分工。

### 5.1 `metric_registry`（[共享组]，最高优先冻结）

- [ ] 为每个指标补 `shape`（数据形态）元数据，供 §4.3 推断图。
- [ ] 导出 `to_tool_schema()`：把每个指标转成 Pydantic tool（§4.1）。
- **冻结接口契约**：`metric_registry` 的字段 schema 一旦定稿，对话组和报告组都只读不改。

### 5.2 `judgment_service.py`（[共享组]，最高优先冻结）

- [ ] 每个 `build_xxx_claims` 返回的 `meta` 里，`charts` 改为「带 `shape` 的 `result_block`」，由 `infer_chart` 统一出图（现在 `:207` 手写 `{"type":"line"}` 这类要收进来）。
- **冻结接口契约**：`(claims, meta)` 的输出结构，双方依赖。

### 5.3 `dialog_act.py`（[对话组]，M1）

- [ ] `ActName` 封闭枚举 → 工具调用计划（`DialogAct` 加 `tools: list[MetricTool]` 字段，替代单一 `act` 标签）。
- [ ] 删掉 `_normalize_act()` 里的 ~50 条正则补丁，降级为 `_soft_fallback`（仅无 LLM 时兜底）。
- [ ] 加 §3.3 的三态字段 `can_answer` / `clarify_question`。

### 5.4 `followup_items.py`（[对话组]，M1）

- [ ] `DRILLDOWN_OPS` 的 `run_drilldown` 里 `op → chart type` 硬编码（`:320`/`:368`/`:434`）改为「产出带 `shape` 的数据块，交给 `infer_chart`」。

### 5.5 `custom_report.py` + `report_templates.py`（[报告组]，M3）

- [ ] `CUSTOM_CHAPTERS`/`CUSTOM_CHAPTER_DIMENSIONS`/`FUNCTION_RADAR_DIMENSIONS` 收敛成 `CHAPTER_REGISTRY`（§4.2）。
- [ ] 删 `CUSTOM_CHAPTER_KEYWORDS`（~50 条正则），章节识别改 DS function calling。
- [ ] `spec_to_report_spec` 里的硬编码（单章 fraud KPI、`data_focus`、`cover`、`tier`）移入注册表。

### 5.6 `chart_payloads.py`（[报告组]，M3）

- [ ] 新增 `SHAPE_TO_CHART` + `infer_chart`（§4.3），把散落的 bar/line/table 并入；保留现有 radar/pie/funnel/scatter/heatmap 构造器作为 `infer_chart` 的底层实现。

### 5.7 `session_store.py`（[对话组]，M2）

- [ ] 加 `focus_history` 焦点栈（§3.1），替换单值 `analysis_focus`。

### 5.8 新增 `persona.py`（[对话组]，M2）

- [ ] 集中人格定义（§3.4），所有 LLM 调用统一注入。

---

## 六、统一流水线（对话 + 报告 + 可视化，一套机制三处解套）

```
用户输入
  ↓
① DS 理解意图（自由理解，不逼分类）+ 注入 PERSONA + focus_history
  ↓
② 命中金融领域？ ──否──> DS 自由回答 + 引导回（弃权出口，不编数字）
  ↓ 是
③ DS function calling：从能力地图选 1..N 个工具 + 参数（不查死路由）
  ↓
④ 引擎 judgment_service 算数 → 产出 claims + result_block（带 shape）
  ↓
⑤ 渲染层 infer_chart(shape) → 自动出图（DS 不参与选图）
  ↓
⑥ 通义点金：金融解读（想得专业）
  ↓
⑦ DS 校验：铁律有没有破 + 润色成大白话（说得通俗）
  ↓
⑧ hallucination_guard 硬校验：数字逐字锚定
  ↓
回复 = 大白话结论 + 自动长出的图 + 可点击追问
```

**对话 vs 报告的区别只有第 ③ 步**：对话选 1 个工具即时答；报告选 N 个工具（章节）组装成文档。其余全部共用。

---

## 七、分两个板块的工程方案（对话组 / 报告组 并行）

**可行，且正好顺势拆掉现有"对话↔报告紧耦合"。** 关键在**先冻结共享地基**。

### 7.1 共享地基（[共享组]，必须最先冻结，谁动谁冲突）

| 模块 | 为什么是地基 | 要冻结什么 |
|---|---|---|
| `metric_registry` | 能力地图的源头（对话组用它选工具，报告组用它出章节） | 字段 schema + 每个指标的 `shape` |
| `judgment_service` | 引擎（对话和报告都调它算数） | `(claims, meta)` 输出结构 |
| `hallucination_guard` | 硬闸门（两边都要数字锚定） | 校验接口 |
| `plain_language` | 术语→大白话（两边都要说人话） | 术语表接口 |
| 铁律 | 数字不编 | 设计红线 |

### 7.2 分工与依赖

- **共享组（先做，1~2 天）**：冻结 `metric_registry` schema + `judgment_service` 契约 + 定稿三张表（§4）。
- **对话组（依赖共享组，M1→M2）**：`dialog_act` 去写死、弃权三态、记忆焦点栈、统一人格。
- **报告组（依赖共享组，M3）**：`CHAPTER_REGISTRY`、`infer_chart` 收敛、章节自由组合。
- **集成（M4）**：两边通过 `metric_registry` + `judgment_service` 的契约对接，天然拼上，几乎无需额外整合。

### 7.3 一句话

> **先花 1~2 天把 `metric_registry` 的 schema 定稿成契约，两个人就能真正并行、互不阻塞。** 不先冻结地基就分人，是后面整合冲突的根源。

---

## 八、实施路线（分阶段，每阶段有交付物 + 验收）

### M0 · 冻结共享地基（1~2 天）
- 交付：`metric_registry` 字段 schema 定稿 + 每指标 `shape`；`judgment_service` 契约；三张表（§4）定稿。
- 验收：对话组和报告组各自按契约写一段 demo，能互相对上。

### M1 · 对话去写死 + 弃权（3~5 天）
- 交付：`dialog_act` 工具调用计划（§5.3）、`followup_items` 走 `infer_chart`（§5.4）、弃权三态。
- 验收：构造 20 条"以前正则会漏/会错"的用例（口语化、多意图、模糊指代），DS 工具路由准确率 ≥ 90%；超出领域的问题能反问/弃权而非硬答。

### M2 · 记忆 + 人格（2~3 天）
- 交付：`focus_history` 焦点栈（§3.1）、`persona.py`（§3.4）。
- 验收：跨轮回溯用例——"问完 A 企业再问 B 行业、再回到 A"链路通；同一用户多轮回答语气一致。

### M3 · 报告组件化（3~5 天）
- 交付：`CHAPTER_REGISTRY`（§4.2）、`infer_chart`（§4.3）、章节自由组合。
- 验收：加一个"新章节"只需注册一条 + 一个引擎函数；章节组合/顺序/维度由对话自由决定；图随数据形态自动出现。

### M4 · 集成 + 灵活性验收（2~3 天）
- 交付：对话 + 报告 + 可视化全链路打穿。
- 验收：跑一套"灵活性测试集"（§九），全部通过。

---

## 九、验收标准（怎么证明"活起来"了）

**灵活性测试集**（每条对应一个"非模型能力"）：

| # | 用例 | 验证能力 | 通过标准 |
|---|---|---|---|
| 1 | "早上好" → 寒暄；下一句"帮我看看企业17" | 记忆/无图 | 寒暄自由回复、无图、能接上 |
| 2 | "上次那批制造业的，按地区再看一眼" | 记忆/焦点栈 | 回溯到"制造业"焦点，换 region 维度 |
| 3 | "这家企业风险大不大，为什么" | 工具/解读 | 引擎算数 + 通义点金给"因为流动比率低于行业均值"式因果 |
| 4 | "帮我分析一下那个……算了你猜我想问什么" | 弃权 | 反问澄清，而非乱猜或硬答 |
| 5 | "给我一份制造业的发票舞弊 + 财务健康报告" | 报告组件化 | DS 选 2 章节 + 顺序 + 范围，图自动配 |
| 6 | "你帮我编一个这个企业的营收" | 铁律/弃权 | 拒绝并说明"数字只来自数据，无法编造" |

**铁律回归**（每次改动必须过）：`hallucination_guard` 校验数字逐字一致；任何用例里 LLM 不得出现引擎没给的数字。

---

## 十、参考来源（论文 / 开源仓库 / 文档，均已核实）

### 10.1 弃权 / 澄清 / 置信度（对应 §3.3）

- **Abstain-QA**（"Do LLMs Know When to NOT Answer?"，COLING 2025，arXiv:2407.16221）：提出 answerable/unanswerable 混淆矩阵与弃权能力评估；发现强模型（GPT-4 等）也难主动弃权，需结构化强制。
  https://aclanthology.org/2025.coling-main.627/
- **Selective QA 置信度阈值**（"Is That Your Final Answer? Test-Time Scaling Improves Selective Question Answering"，ACL 2025，arXiv:2502.13962）：从 token log-prob 抽置信度，0/0.5/0.95 阈值做接受/拒答。
  https://huggingface.co/papers/2502.13962
- **多 LLM 协作弃权**（"Don't Hallucinate, Abstain"，ACL 2024）：知识缺口检测，多模型协作提升弃权准确率最高 19.3%。
  https://aclanthology.org/2024.acl-long.786/

### 10.2 工具调用 / 结构化输出（对应 §3.2、§4）

- **Instructor**（结构化输出 + 自动重试 + `response_model`，本项目已在用）：
  https://github.com/instructor-ai/instructor
- **LiteLLM**（多模型网关，OpenAI 兼容，本项目已在用）：
  https://github.com/BerriAI/litellm
- **LangGraph**（结构化输出 + self-correcting loop 参考，`with_structured_output`；本项目**不引框架，只借鉴其两阶段路由 + 重试模式**）：
  https://github.com/langchain-ai/langgraph
- **ReAct**（推理与行动协同，工具调用的思想源头，Yao et al. 2022）：
  https://arxiv.org/abs/2210.03629
- **Toolformer**（LLM 自学用工具，Schick et al. 2023）：
  https://arxiv.org/abs/2302.04761
- 两阶段路由降噪 / 结构化路由比关键词慢但更稳的实践结论（调研）：
  https://www.e-com-net.com/article/2046938965549834240.htm

### 10.3 记忆（对应 §3.1）

- **Mem0**（用户级长期记忆，ADD/UPDATE/DELETE + 冲突检测；本项目不引，参考其"用户/会话/作用域"分层）：
  https://github.com/mem0ai/mem0
- **Letta（原 MemGPT）**（自管理三层记忆 Core/Recall/Archival；本项目借鉴其"常驻人格=核心记忆"思想，不引框架）：
  https://github.com/letta-ai/letta
- **MemGPT 论文**（"MemGPT: Towards LLMs as Operating Systems"，arXiv:2310.08560）：
  https://arxiv.org/abs/2310.08560

### 10.4 金融大模型（对应 §二）

- **通义点金（Qwen DianJin）**：阿里云金融大模型，开源 DianJin-R1-7B/13B/32B，API 约 0.01 元/千 token；文档见百炼帮助中心。
  https://github.com/aliyun/qwen-dianjin
  https://help.aliyun.com/zh/model-studio/tongyi-dianjin-overview
- **XuanYuan（度小满轩辕金融大模型）**：
  https://github.com/Duxiaoman-DI/XuanYuan
- **FinGPT**（数据为中心的金融 LLM，AI4Finance；本项目"不重训、注入结构化数据"的思路同源）：
  https://github.com/AI4Finance-Foundation/FinGPT

### 10.5 校验 / 抗幻觉（对应 §二）

- **Self-Refine**（自反馈迭代精炼，Madaan et al. 2023）：DS 校验层的模式。
  https://arxiv.org/abs/2303.17651
- **LLM-as-a-Judge**（"Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena"，Zheng et al. 2023）：校验层评估方法。
  https://arxiv.org/abs/2306.05685
- **Pydantic**（schema 定义与 `model_json_schema()`）：
  https://github.com/pydantic/pydantic

---

## 附：一句话给下一个开工的人

> 先读 §〇（可行性边界）和 §一（铁律），再冻结 §四（三张表），然后按 §五的清单逐文件改。**记住一句话：表达可以自由，事实永远封闭——数字只来自引擎。** 任何一步让你"更灵活"但"开始编数字"的改动，都是错的。
