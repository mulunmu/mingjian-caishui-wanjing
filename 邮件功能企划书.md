# 邮件交付功能企划书 —— 明鉴·财税票·万景（v5）

> 依据：通读 2.0 代码库（`email_service.py` / `slice_report.py` / `report.py` / `deps.py` / `auth_service.py` / `intent_engine.py`）+ 检索 OWASP ASVS / NIST SP 800-63B 规范 + 逐文件核实 GitHub 开源实现（FastForge OTP 认证，见 §六）。
> 目标：**邮箱验证码认证**（注册验证 / 验证码登录）+ **报告交付**（两场景差异化发送）+ **报告删除** + **发送审计**。
> 状态：方案讨论稿，**尚未实施**。决策点已拍板（见 §七）。

---

## 一、定位与目标

> **前提约束（重要）**：系统当前是「静态数据快照」——`数据.sql` 一次性 ETL 导入，应用运行期只读 PostgreSQL，**不支持实时数据接入 / 实时监控**。**订阅功能不在本邮件方案范围内**：现有订阅代码（`subscription_service.py` 手动检测）原样保留、不改动，仅作为**数据未来定期刷新后的可发展场景**，当前与邮件功能零交集（见 §7.3）。

### 1.1 一句话

以「**注册邮箱即受信邮箱**」为锚点：注册时验证码验证邮箱 → 该邮箱入库、与登录信息绑定 → 报告发送默认直达该邮箱；发往**其他邮箱**必须验证码验证。邮件覆盖 PDF 交付型的**聚合报告（批量筛查·贷后监测）**与**个体深度报告（单户准入·深度研判）**；对话交互式的「风险评估」只在对话流内触发发送，不独立成页。

### 1.2 目标（Definition of Done）

1. **邮箱验证码认证**：注册需验证码验证邮箱；支持「邮箱 + 验证码」登录；注册邮箱入库并与 `AppUser` 绑定。
2. **两场景发送**：风险评估（对话式意图触发）与报告中心（表单式单/批量）各自独立 UI，共用同一发送内核。
3. **受信邮箱机制**：注册邮箱直达；其他邮箱验证码验证后可发送，可选「记住」为受信邮箱。
4. **报告删除**：报告中心可删除单份报告（PDF + 元数据 + 快照），权限仅限本人。
5. **验证码安全**：TTL 合理、可重发、单次使用、哈希存储、防爆破、防枚举。
6. **审计可溯**：每次发送落 `EmailLog`，失败可重试。

### 1.3 衔接原则：补全而非推翻

保留 `email_service.py`（yagmail）、`subscription_service.py`、`generate_enterprise_report` / `generate_slice_report`、`require_plan("subscriber")` 全部不动，在其上叠加认证、受信邮箱、验证码、删除能力。**订阅（`subscription_service.py`）不在本方案改动范围，保持现状。**

---

## 二、现状盘点（依据代码，非推测）

### 2.1 已具备（复用）

| 能力 | 位置 | 说明 |
|---|---|---|
| SMTP 发信 | `backend/app/services/email_service.py` | `send_slice_report(recipient, title, pdf_path)`，yagmail，全局每小时 10 封 |
| 发送接口 | `backend/app/api/v1/report.py:242` `POST /report/email` | 强制收件人 = 登录邮箱；**即时重生成切片报告后发送**，拒绝 `enterprise_id`（匿名模式），**不接收既有 report_id** |
| 报告生成 | `backend/app/services/slice_report.py` | `generate_enterprise_report` / `generate_slice_report` → `(report_id, pdf_path, context)` |
| 报告归属/访问 | `slice_report.write_report_meta` / `can_access_report` | 报告有 `owner`，按归属过滤 |
| 权限守卫 | `backend/app/api/deps.py` | `require_plan("subscriber")` |
| 阻塞 IO 转移 | `backend/app/services/sync_runner.py` | `run_blocking` = `asyncio.to_thread` |
| 用户模型 | `backend/app/models/engine_store.py` `AppUser` | `email`(PK) / `password_hash` / `role` / `plan` |
| 注册/登录 | `backend/app/services/auth_service.py` | `register_user` / `authenticate_user`（**无邮箱验证**） |
| 意图解析 | `backend/app/services/intent_engine.py` | 对话意图识别（风险评估发送可复用） |

### 2.2 缺口（本次要补）

| 缺口 | 现状 | 影响 |
|---|---|---|
| 注册无邮箱验证 | `register_user` 直接落库 | 邮箱真假无法保证 |
| 无验证码登录 | 仅密码登录 | 缺「邮箱+验证码」第二登录方式 |
| 无受信邮箱概念 | 只有登录邮箱 | 无法区分「已验/未验」收件地址 |
| 收件人锁死 | `POST /report/email` 强制登录邮箱 | 无法交付给客户/机构 |
| 无报告删除 | 报告只生成、可下载 | 用户无法清理自己的报告 |
| 纯文本邮件 / 无审计 / 无重试 | 见 v1 盘点 | 专业度与合规不足 |

---

## 三、总体设计

### 3.1 功能全景（三大块）

```
① 邮箱验证码认证（新增）
   注册验证码 / 验证码登录 / 受信邮箱管理

② 报告交付（两场景，共用发送内核）
   风险评估（对话式意图触发） ┐
   报告中心（表单式 单/批量） ┘→ EmailService → Provider → EmailLog

③ 报告删除（新增）
```

### 3.2 验证码机制设计（核心）

**（1）有效期（TTL）——分场景取值，兼顾安全与体验**

| 用途 | TTL | 依据 |
|---|---|---|
| 登录验证码 | **10 分钟** | OWASP ASVS V2.7.1「OOB 验证器请求后 ≤10 分钟授权」；业界主流（OneCode/Ash/Nhost 均 10 分钟） |
| 发信验证码（发到其他邮箱） | **10 分钟** | 同上 |
| 注册验证码 | **15 分钟** | 注册为低频操作，给新用户更多缓冲 |

> **为什么不用 5 分钟**：邮件有网络延迟、可能落入垃圾箱、用户未必即时查看，5 分钟偏短易误伤。10 分钟是 OWASP 授权上限与业界主流的交集。

**（2）可重发，重发作废旧码**
- 重发冷却：**60 秒**（服务端强制，返回 `429` + `Retry-After`）。
- 每次重发**作废上一个未使用验证码**（同邮箱同一 purpose 只存在一个有效码）。

**（3）失败重试上限**
- 每个验证码最多 **5 次**，超过作废 + 15 分钟冷却；按**邮箱 + purpose** 计数。
- **计数键与码键分离**：失败计数用独立键（如 `otp_attempts:{email}:{purpose}`，独立 15 分钟 TTL），与验证码本体分开存储——防止「重发新码就重置计数」的绕过（参考 FastForge `otp_attempts:{phone}` 模式）。

**（4）生成、存储与单次使用**
- **生成**：用 `secrets.token_*` / `secrets.randbelow`（加密安全随机），6 位数字（≈100 万组合，契合 NIST SP 800-63B 下限），**禁用 `random`**。
- **只存哈希**（HMAC-SHA256 / bcrypt），不存明文，日志不打印验证码。
- 验证成功即 `consumed`，不可二次使用（防重放）。
- **存储分层**：优先 **Redis**（自带 TTL，天然适合验证码）；无 Redis 时降级到 PostgreSQL 表 `email_verifications`（带 `expires_at`）——延续项目「Redis 可选」现状，不引入硬依赖。启动时探测 Redis 可用性，同一份 `verification_service` 对上层透明。
- **降级一致性**：PG 降级时「同邮箱同一 purpose 只一个有效码」由 **事务 + `(email, purpose, consumed)` 唯一索引**保证，与 Redis `SET NX` 语义对齐。

**（5）防枚举与绑定**
- 验证接口返回中性提示（「若邮箱有效，验证码已发送」），不暴露邮箱是否已注册。
- 每个验证码绑定 **purpose（register / login / send_email / bind_email）+ 邮箱 + challenge_id**。
- 向客户端只返回 `challenge_id`，**不返回验证码本身**。

**（6）发送请求频率（双维度限流）**
- 同一邮箱每 15 分钟最多 5 次发送请求，重发冷却 60 秒。
- 同一 IP 每 15 分钟发送请求上限可配（防单 IP 批量枚举），与邮箱维度叠加；IP 限流可用 `slowapi`（FastAPI 生态标准）或复用 `main.py` 现有限流中间件。

### 3.3 两场景发送流程（差异化）

**受信邮箱规则（通用）**：
- **注册邮箱** = 已受信 → 直接发送，无需验证码。
- **其他邮箱** = 未受信 → 引导验证（发验证码）→ 通过后发送。
- **「记住此邮箱」交互**：验证通过后提供「记住该邮箱」勾选；勾选则写入 `TrustedEmail`（source=`verified`），**下次直达**；不勾选则仅本次有效。注册邮箱在注册成功时自动写入（source=`register`）。

**场景 A · 风险评估（对话式）**

```
用户在研判中心生成定制报告
  → 自然语言表达意图：「把这份报告发到我的邮箱 / 发给 xxx@yyy.com」
  → intent_engine 识别「email_report」意图（`intent_engine.py` 已内置）+ 抽取收件地址
  → 收件地址 == 受信邮箱 ? 直达 : 触发验证码验证（对话内引导）
  → 验证通过 → 发送 → 对话流内回执
```

- 交互形态：**对话气泡内嵌**（验证码输入、发送结果都在对话里）。
- 前端入口：`ResearchCenter.tsx` 聊天流，复用 `intent_engine` + `chat_router`。

**场景 B · 报告中心（表单式）**

```
报告中心列表 → 勾选单份/多份报告 → 点「发送邮件」
  → 弹窗填收件人（默认预填注册邮箱）
  → 收件地址 == 受信邮箱 ? 直达 : 触发验证码验证（弹窗内输码）
  → 单量发送 1 份 / 批量逐份或合并发送
  → 展示成功/失败汇总
```

- 交互形态：**独立弹窗 + 发送记录页**。
- 前端入口：`ReportCenter.tsx` / `ReportPage.tsx`。

**两处 UI 差异汇总**：

| 维度 | 风险评估 | 报告中心 |
|---|---|---|
| 触发方式 | 对话意图（自然语言） | 勾选 + 按钮 |
| 交互载体 | 对话气泡 | 弹窗 + 记录页 |
| 发送粒度 | 单份（当前对话报告） | 单份 / 批量 |
| 验证码输入 | 对话内引导 | 弹窗内 |

> 两场景共用同一 `EmailService`（Provider + 模板 + 审计），仅「入口与交互」不同。

### 3.4 数据模型（新增）

**新增 `EmailVerification`（验证码，`backend/app/models/email_verification.py`）**

```python
class EmailVerification(Base):
    __tablename__ = "email_verifications"
    id: int                # PK
    email: str             # 目标邮箱（index）
    purpose: str           # register / login / send_email / bind_email
    code_hash: str         # HMAC-SHA256 / bcrypt，不存明文
    challenge_id: str      # 唯一，返回客户端，不返回 code
    expires_at: datetime
    attempts: int = 0
    max_attempts: int = 5
    resend_count: int = 0
    consumed: bool = False
    created_at: datetime
```

**新增 `TrustedEmail`（受信邮箱，`backend/app/models/trusted_email.py`）**

```python
class TrustedEmail(Base):
    __tablename__ = "trusted_emails"
    id: int
    user_id: str           # 归属 AppUser.email（index）
    email: str             # 受信邮箱（index）
    source: str            # register（注册即受信）/ verified（验证码验证后记住）
    verified_at: datetime | None
```

**新增 `EmailLog`（发送审计，`backend/app/models/email_log.py`）**

```python
class EmailLog(Base):
    __tablename__ = "email_logs"
    id: int
    sender: str
    recipient: str         # index
    subject: str
    report_id: str | None  # 关联报告；批量/系统通知可空
    kind: str              # slice / enterprise / batch / verify_code
    provider: str          # smtp / api
    status: str            # pending / sent / failed
    message_id: str | None
    error: str | None
    retry_count: int = 0
    created_at: datetime | None
    sent_at: datetime | None
```

### 3.5 邮件 Provider 抽象（沿用决策）

```python
# backend/app/services/email_provider.py
class EmailProvider(Protocol):
    def send(self, msg) -> SendResult: ...
```

- `SMTPProvider`（迁移现有 yagmail，默认）/ `HttpApiProvider`（第三方邮件 API 预留）。
- **yagmail 停更风险**：yagmail 多年无维护、依赖 SMTP 应用专用口令；面向「任意邮箱 + 批量」时送达率与进垃圾箱风险高，**量产默认走 `HttpApiProvider`（Resend / SendGrid / Mailgun）或标准库 `smtplib`**，yagmail 仅作开发兜底。
- `EMAIL_PROVIDER=smtp|api`；未配置时整体降级「未配置」提示。
- 验证码邮件与报告邮件共用同一 Provider 与模板引擎；**共用一个 Header / Footer / 免责声明**（品牌与合规一致，不各自为政）。
- **异步发信 + 重试**：投递层用 `aiosmtplib.SMTP(hostname, port, use_tls)`（connect → login → send_message → quit）替代 yagmail 阻塞调用；失败**指数退避**重试 `await asyncio.sleep(2**attempt)`（`EMAIL_RETRY_MAX` 默认 3），仍失败则落 `EmailLog(status=failed)` 供 `/emails/{id}/resend` 手动补发。

---

## 四、分阶段实施

### 阶段 A · 邮箱验证码认证

- **目标**：注册验证 + 验证码登录 + 受信邮箱入库。
- **涉及文件**：新增 `verification_service.py`、`models/email_verification.py`、`models/trusted_email.py`、`templates/email_verify_code.html`；改造 `auth_service.py`、`api/v1/auth.py`。
- **步骤**：建表 → 验证码服务（Redis 优先/PG 降级）→ 注册加验证 → 验证码登录签发 JWT → 注册邮箱写入 `TrustedEmail`。
- **门控适配**：注册验证码仅当 `ALLOW_SELF_REGISTER=true` 时生效（现有 `auth.py` 已在关闭时 403）；`AUTH_REQUIRED` 与 `demo-login` 演示登录路径保持不变，不破坏既有鉴权开关。
- **验收**：注册必须验证码；验证码登录签发 JWT；哈希存储、单次使用、TTL/重试/冷却生效。

### 阶段 B · 报告删除 + 发送内核

- **目标**：报告可删除；`EmailService` 编排（Provider + 模板 + EmailLog）。
- **涉及文件**：新增 `email_provider.py`、`email_templates.py`、`models/email_log.py`，重构 `email_service.py`；`slice_report.delete_report()`；改造 `api/v1/report.py`。
- **步骤**：EmailService 分层 → 删除函数（**校验归属 `can_access_report`，联删 PDF 文件 + meta + snapshot，不留孤儿文件；`EmailLog` 为审计留痕保留，`report_id` 置空/标「已删除」**）→ 发送接口写 EmailLog。
- **验收**：本人可删报告、他人 403/404；删除后 PDF/meta/snapshot 全部清除、`EmailLog` 留痕可查；发送走 Provider 并落 EmailLog。

### 阶段 C · 两场景发送 + 受信邮箱规则

- **目标**：风险评估（意图触发）与报告中心（表单单/批量）均可发送，受信直达 / 未受信验证。
- **涉及文件**：`api/v1/report.py`、新增 `api/v1/email.py`、`intent_engine.py` + `chat_router.py`、`src/api/email.ts`、`ReportCenter.tsx`、`ReportPage.tsx`、`ResearchCenter.tsx`。
- **步骤**：意图识别 → 两场景入口 → 受信判定 → 验证码引导 → 发送。
- **验收**：注册邮箱直达；其他邮箱验证后可发；「记住」后成为受信；批量有汇总。

---

## 五、API 与前端

### 5.1 后端 API（前缀 `/api/v1`）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/auth/request-code` | 公开 | 发送验证码（purpose + email + challenge_id） |
| POST | `/auth/verify-code` | 公开 | 校验验证码 |
| POST | `/auth/login-by-code` | 公开 | 邮箱 + 验证码登录，签发 JWT |
| POST | `/auth/register` | 公开 | 改造：注册需先验证邮箱 |
| POST | `/report/email` | `subscriber` | **保留不动**：匿名切片报告「即时重生成 + 发送」（锁登录邮箱） |
| POST | `/emails/send` | `subscriber` | **新增**：按 `report_id` 发送既有报告（单量），任意 `recipient`（未受信先验证） |
| POST | `/emails/batch` | `subscriber` | 批量发送（`report_ids[]` / 筛选） |
| GET | `/emails/logs` | `subscriber` | 我的发送记录（分页） |
| POST | `/emails/{id}/resend` | `subscriber` | 重发失败记录 |
| DELETE | `/report/{report_id}` | `subscriber` | 删除报告（PDF+meta+snapshot） |

### 5.2 前端改动

- `src/api/email.ts`、`src/api/auth.ts`（验证码接口）新增。
- 报告中心：报告卡片加「删除」「发送邮件」按钮；发送弹窗（收件人 + 预览 + 验证码 + 「记住」）；批量勾选。
- 风险评估：对话流内识别「发报告」意图，气泡内完成收件人 + 验证码 + 回执。
- 新增「发送记录」页；账号页展示「受信邮箱」列表（可增删）。

---

## 六、规范依据与开源参考

**规范**：
- OWASP ASVS 4.0.3 — V2.2.1（每小时失败 ≤100）、V2.7.1（OOB 验证器 ≤10 分钟）、V2.8.1（OTP 生命周期）。[General Authenticator Security](https://raw.githubusercontent.com/OWASP/cornucopia/refs/heads/master/cornucopia.owasp.org/data/taxonomy/en/ASVS-4.0.3/02-authentication/02-general-authenticator-security/index.md)
- OWASP Secure Coding Cheat Sheet — 邮箱验证码「首次使用或 8 小时后」过期。[Secure Coding Cheat Sheet](https://wiki.owasp.org/index.php?title=Secure_Coding_Cheat_Sheet&direction=next&oldid=203799)
- NIST SP 800-63B §5.1.3.2 — 验证码最少 100 万组合。

**开源实现参考（FastAPI + Redis + OTP，与本栈同构）**：
- [jishnu70/FastAPI-OTP-Auth-Backend](https://github.com/jishnu70/FastAPI-OTP-Auth-Backend)
- [JimmyPowell/fastapi-project-template](https://github.com/JimmyPowell/fastapi-project-template)
- [dfm88/fastapi-two-factor-authentication](https://app4.secure.forcepoint.com/dfm88/fastapi-two-factor-authentication)
- [javadzarezadeh/fastforge](https://github.com/javadzarezadeh/fastforge)
- [JaganReddy-dev/sentinel](https://github.com/JaganReddy-dev/sentinel)（Argon2id + 刷新轮换 + 账户锁定）
- [MacCracken/agnostic — scheduled_reports.py](https://github.com/MacCracken/agnostic/blob/982507161101f8d64a6d4954e0807e869c8fe5ef/webgui/scheduled_reports.py)（aiosmtplib 异步发信 + 指数退避——用于 §3.5 发信层）

**已核实源码 · 代码级对照（FastForge OTP，实际读过）**：

| 我方设计点 | FastForge 实际写法 | 结论 |
|---|---|---|
| 验证码生成 | `secrets` 6 位 | ✅ 我方 §3.2 已写对 |
| 验证码存储 | ⚠️ Redis **明文**(`setex` TTL 5 分钟) | 我方存**哈希**，更安全 |
| 失败计数 | `otp_attempts:{id}` 独立键 + 15 分钟窗口 | ✅ 已并入 §3.2(3) |
| 限流 | `slowapi` IP 级 + 身份级双层 | ✅ 已并入 §3.2(6) |
| 邮箱验证 | `email_verification:{email}` 键 + `is_email_verified` 字段，单次使用 | 我方用 `TrustedEmail` 表，更富(多受信邮箱) |

> **结论**：我方 §3.2 安全设计(哈希存储 / 60s 冷却 / 5 次上限)严于 FastForge 样板，不落后；FastForge 的「独立失败计数键」「slowapi 双维度限流」已吸收进 §3.2。

**邮件库选型依据**：
- [How to Send Emails in Python (2026)](https://www.sequenzy.com/blog/send-emails-python)
- [5 Best Email API for Python Developers (2026)](https://mailtrap.io/blog/best-email-api-for-python-developers/)

**重发/防重放实践**：
- [OTP Code Reuse — transilienceai/communitytools](https://github.com/transilienceai/communitytools/blob/main/skills/authentication/reference/scenarios/2fa/code-reuse.md)
- [Building Safer Email OTP Verification in Node.js](https://dev.to/anishhajare/building-safer-email-otp-verification-in-nodejs-expiry-retries-and-lockouts-558n)

---

## 七、决策点与合规铁律

### 7.1 已拍板决策

| 决策点 | 结论 |
|---|---|
| 一期范围 | **完整产品化**（认证 + 两场景发送 + 删除 + 审计 + 合规） |
| 收件人范围 | **放开到任意邮箱**（受信直达 / 未受信验证码验证） |
| 邮件 Provider | **抽象双 Provider**（SMTP 默认 + 第三方 API 预留） |
| 验证码 TTL | 登录/发信 10 分钟，注册 15 分钟，重发冷却 60s，重试 5 次 |
| 订阅 | **保留现状不动，未来可发展场景，当前与邮件零交集**（见 §7.3） |

### 7.2 合规铁律（不可妥协）

1. **脱敏红线**：邮件正文与附件仅含脱敏报告，不出现企业真实名称/税号/法人等可定位信息。
2. **免责声明**：每封固定署名「本报告由明鉴·财税票·万景自动生成，仅供参考，不构成审计意见、投资建议或信贷审批依据」。
3. **验证码安全**：`secrets` 生成、哈希存储、单次使用、重发作废旧码、按邮箱 + IP 双维度限流、防枚举。
4. **频率限制防滥发**：重构为「按身份（用户/收件人）限额 + 全局配额」双层（现状 `_EMAIL_HOURLY_LIMIT=10` 为进程内全局硬编码，需改造），批量按配额排队。
5. **送达与信誉**：生产发信域名配好 SPF / DKIM / DMARC，降低进垃圾箱与被拒风险。
6. **全程审计**：所有发送（含验证码、报告）落 `EmailLog`。
7. **不改变结论**：邮件只是「表达 + 交付层」，报告结论仍由规则引擎产出。

### 7.3 范围外 · 订阅（保留现状，未来可发展）

- **现状**：`Subscription` / `subscription_service.py` / `POST /subscriptions/detect`（手动检测）均已存在，**本方案不改动、不叠加**。
- **定位**：数据当前为静态快照（`数据.sql` 一次性导入、运行期只读 PG），「定期投递」只会重发内容相同的报告，无增量价值，故**不与邮件功能产生交集**。
- **未来**：若接入 ETL 定期刷新（日/周），订阅可升级为「数据更新后自动重出报告并投递」，届时再单独设计（可复用 §六 agnostic 的 APScheduler job store 分层 + `aiosmtplib` 退避）。

---

## 八、验收标准

1. 注册必须邮箱验证码；验证码登录签发 JWT；验证码哈希存储、单次使用、TTL/重试/冷却/防枚举全部生效。
2. 风险评估（对话意图）与报告中心（表单单/批量）两个独立 UI 均可用，共用同一发送内核。
3. 注册邮箱直达发送；其他邮箱经验证码验证后可发，勾选「记住」后成为受信邮箱。
4. 报告中心可删除本人报告，他人报告返回 403/404。
5. `pytest` 全绿（含新增验证码/发送/删除测试），前端 `npm run build` 通过。
6. 邮件内容不含可定位企业标识，含固定免责声明；`EmailLog` 完整留痕、失败可重试。
