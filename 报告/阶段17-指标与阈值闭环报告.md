# 阶段 17 指标与阈值闭环报告

日期：2026-09-17

候选标签：`rag-v2-stage17-accepted-20260917`

## 一、结论

阶段 17 已完成指标终态闭环：

```text
registry_metrics_total=103
metrics_validated=67
metrics_unsupported=36
metrics_planned=0
validated_thresholds=16
planned_thresholds=0
```

原有 48 个 planned 指标已全部完成分类：12 个进入 validated，36 个因缺失源字段、缺失时间序列或与既有指标重复而进入 unsupported。交叉偏差新增 2 个 validated 指标，用于关闭原先 2 个 planned 阈值。

## 二、新增 validated 指标

```text
cross_avg_deviation
cross_max_deviation
customer_count
customer_hhi
customer_top5_concentration
income_tax_effective_rate
invalid_invoice_ratio
ocf_to_revenue
red_invoice_count_ratio
supplier_count
supplier_hhi
supplier_top5_concentration
violation_recency_days
void_invoice_ratio
```

真实数据执行审计：

```text
total=14
passed=14
failures=[]
```

关键覆盖：

```text
客户数量：193/193
客户HHI：193/193
前五客户占比：193/193
供应商数量：193/193
供应商HHI：193/193
前五供应商占比：193/193
现金流质量：157/193
实际所得税率：78/193
异常凭证占比：185/193
作废率：185/193
红票张数比例：179/193
最近违规距今天数：84/193
多口径平均偏差：180/193
多口径最大偏差：180/193
```

## 三、unsupported 指标

36 个 unsupported 指标均写入 `metric_definition.edge_cases`，且：

```text
retrieval_enabled=false
tool.enabled=false
executor_registered=false
unsupported_enabled=[]
```

主要缺失类型：

- 缺少注册资本、员工人数、成立日期、经营状态、纳税人类型。
- 缺少逐月开票、月度税负、社保人数序列、零申报月份和现金流多年序列。
- 缺少欠税金额、更正金额、红字金额、进项税额、销项税额和发票税额。
- 缺少跨地区交易、发票查验、新增客户/供应商、逐票高风险等明细。
- 场景综合指标因必需输入不完整，未用主观权重强行合成。

重复口径不重复注册：

```text
change_count 与已验证 change_cnt 重复
invoice_amount_total 与已验证 invoice_revenue 重复
vat_declared_revenue 与已验证 vat_revenue 重复
```

## 四、阈值闭环

```text
validated_thresholds=16
planned_thresholds=0
thresholds_missing_required=[]
```

新增已验证阈值：

```text
cross_avg_deviation >= 0.25
cross_max_deviation >= 0.40
```

边界测试覆盖等于、低于和高于阈值。报告 Block 矩阵：

```text
31/31 passed
```

## 五、语义与 RAG 接入

新增指标已进入：

- `SUPPORTED_METRIC_KEYS`
- 语义 metric alias 白名单
- DialogAct 分类能力地图
- validated tool/RAG snapshot
- 确定性 extended metric executor
- 严格覆盖率审计

真实 HTTP 验证：

```text
客户HHI是多少：answered
前五客户占比多少：answered
现金流质量怎么样：answered
最近违规距今多久：answered
异常凭证占比多少：answered
```

本轮同时关闭了两个暴露出的全局问题：

- 未知企业即使被上游误判为 analysis，也会由高置信未知主体策略强制进入 `unknown_entity`。
- 比例指标同时接受 `0.21` 与 `21%` 两种等价表达，避免正常百分比表述被事实闸门误删；大比例值不会再次乘 100。

## 六、最终验证

```text
backend full regression: 816 passed, 8 skipped
frontend build: passed
stage17 metric data audit: 14/14
strict module coverage: passed
semantic readiness: ok=true
```

105 条生产对话矩阵：

```text
run A: 105/105, P50 1154.55ms, P95 3681.55ms
run B: 105/105, P50 1223.13ms, P95 4236.25ms
```

## 七、边界说明

- 36 个 unsupported 指标不是系统能力，不会进入 RAG，也不会对用户开放。
- 若要提升后续指标覆盖率，必须先补充真实源字段或时间序列，再新增执行器和边界测试。
- 不允许把 unsupported 直接改成 validated；必须完成源字段、公式、执行器、别名、RAG 和审计闭环。
- Sentry DSN 仍未配置，与本阶段指标闭环无关，但不能宣称 Sentry 已实际上报。
