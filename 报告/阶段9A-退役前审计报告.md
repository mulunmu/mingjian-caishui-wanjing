# 阶段 9A 退役前审计报告

日期：2026-09-16

## 结论

当前不具备删除旧路径的条件。

原因：

- 生产影子评估门禁尚未通过。
- 旧对话入口仍在使用。
- 旧正则兜底仍存在于 `intent_engine` 和 `dialog_act`。
- 章节兼容别名仍被多个模块导入。

因此本阶段只生成红线和审计证据，不删除旧路径、不切换流量。

## 实际扫描结果

```json
{
  "_FUNC_PATTERNS": 2,
  "_DIM_PATTERNS": 2,
  "_normalize_act": 4,
  "_SOFT_FABRICATE_KW": 2,
  "_SOFT_OOD_KW": 2,
  "CUSTOM_CHAPTERS": 8,
  "CUSTOM_CHAPTER_DIMENSIONS": 5,
  "FUNCTION_RADAR_DIMENSIONS": 3,
  "route_chat": 10
}
```

涉及的主要模块：

- `services/intent_engine.py`
- `services/dialog_act.py`
- `services/chat_router.py`
- `services/custom_report.py`
- `services/report_templates.py`
- `services/slice_report.py`
- `services/llm_reply.py`
- `services/legacy_retirement_audit.py`
- `api/v1/chat.py`

## 阻断项

```text
shadow_gate_not_passed
legacy_markers_remain
legacy_entrypoint_still_active
chapter_compatibility_still_imported
```

## 正确退役顺序

1. 通过影子评估门禁。
2. 在功能开关后替换旧入口。
3. 删除没有外部引用的兼容别名。
4. 全量回归通过后删除旧正则兜底。
5. 更新红线、部署和回滚文档。
6. 完成匿名数据端到端验证后再清理旧模块。

## 本阶段新增

- `backend/app/services/legacy_retirement_audit.py`
- `backend/tests/test_legacy_retirement_audit.py`
- `红线要求.md` v2
- `红线要求-v1-历史.md`

## Sentry 审计状态

当前环境未配置 `SENTRY_AUTH_TOKEN`，因此无法查询生产 Sentry 问题。没有伪造 Sentry 接入状态。